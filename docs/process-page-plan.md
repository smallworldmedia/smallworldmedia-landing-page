# Process Page — Build Plan

> Grilled & solidified 2026-07-12.
> Phased implementation plan for the `/process` page — the v2 workstream deferred at launch (`docs/v1-launch-plan.md` F2).
> **Vocabulary:** `CONTEXT.md` § "Process Page — concepts". **Spec:** `docs/process-page-spec.md`. **Decisions:** `docs/adr/0004-process-scroll-driver.md` (+ lineage ADR-0002, ADR-0003).

## Summary

The process page walks a prospective client through how SWM builds a visual world, and the brand mark performs the walk: one persistent scene morphs through five **Stages** — a belt of drifting **Fragments** (discovery), **the Thread** connecting the dots until the Fragments pull inward and construct **the Core** (visual language), the electric-blue `rows` cascade light-up (core identity), per-panel emanation into a larger world (build-out), and `?bpm`-anchored rhythmic pattern loops (living world) — driven by discrete, scroll-triggered, time-domain transitions on a normal Lenis document route, closing on `start_project`. Everything is built from the home globe's primitives (`buildGlobeGeometry`, `createPanelMaterial`, `buildCascadeTimeline`) with zero textures and one route-scoped canvas; the belt and the globe are the same 84 panel meshes.

## Architecture notes

- **Route-scoped canvas** (ADR-0002): the page is the site's third WebGL scene; full teardown with `forceContextLoss()` on nav.
- **Primitives, not forks** (ADR-0003 grammar): a new thin `useProcessScene` consumer; `useGlobeScene` stays untouched (it is fused to CMS textures, the live scheduler, and drag).
- **ScrollTrigger is scoped to this island** (ADR-0004): first use in the codebase; the bespoke accumulator remains the idiom for single-commitment viewport-locked gestures elsewhere.
- **This branch is docs-only.** Branch base = `feature/v1-launch`; the build rebases onto wherever the site's mainline sits when work starts (post-launch, that is `main`).
- **Pre-existing token gaps** are fixed at the point of first real use (P0): define `--tracking-tight: -0.02em` and `--lh-body` at `:root`, promote `--color-near-black` out of `.project-detail` scope. Also correct the stale `GAP_COLOR` comment (`globeConfig.js:88` — value is electric blue, comment says black).
- **Deterministic scatter:** the Fragment belt reuses the seeded PRNG utilities (`mulberry32`/`hashSeed`) from `src/components/work/world/seededLayout.js` — same belt every visit, no `Math.random` in the scene.
- **Content prerequisite: none** — no CMS, no media. Copy ships in `src/components/process/processContent.js`.

## Phases

| # | Workstream | Status | Notes |
|---|---|---|---|
| P0 | **Route scaffold + gate** | ✅ `4fd1e4f` | `process.astro` (redirect stub, `v2:` re-enable comment; dev stays viewable for tuning) + netlify forced 302 + `SITEMAP_EXCLUDE` entry + `ProcessPage.jsx` island + `route-process` body class + full `processContent.js` deck + token-gap fixes (values mirror PR #13 — either merge order resolves identical). Page exists, is dark in production. |
| P1 | **Scroll-driver skeleton** | ✅ `83be9e3` | ScrollTrigger registration (island-only) + Lenis bridge (`getLenis()?.on('scroll', ScrollTrigger.update)`, refresh on `astro:page-load`/fonts) + symmetric `[top 60%, bottom 60%]` section boundaries + mid-page arrival sync + stage machine stub logging `goTo` states. |
| P2 | **Scene core (S3–S5)** | ✅ `80d6553` | `useProcessScene` on the full 84-mesh globe: blue light-up (`uFallbackColor` + `uPower` cascade, `?cascade`), emanation (`?emanate` on `?emanateorder`), rhythm loops (`?bpm`, equator-out delay model). `panelDelay` exported from `cascade.js` per spec §3. See Build notes: `PULSE_MIN` deviation. |
| P3 | **Fragments + Thread + assembly (S1–S2)** | ✅ `f88e958` | Seeded belt scatter + drift (`seededLayout.js` PRNG exports), Thread chain draw + `s2.captions` pings, drift damping on connect, pull-in assembly (scatter→home transforms, inner-sphere reveal), zoom-out. Shard geometry re-baked to local origin (position/quaternion = placement/tumble; home = `centerDir·R·k`). RM stage-02 still = connected belt, Thread pre-drawn. |
| P4 | **Copy & chrome** | ✅ `8d321b3` | Full copy deck render, per-section OS-boot entrances (0.7× leave-back exits), arrival choreography (`swm:fill-release` + hero sequence + `materializeBelt`), CTA section (`swm:open-overlay` primary, `/work` secondary), `?debug` panel — upgraded to **live tuning** in the follow-up commit: sliders/selects mutate the shared `TUNING` object (scene reads it at use-time), `↻ replay` re-runs the current stage's transition, `copy_url` serializes the dialed feel. Feel extras beyond §9: `?idlepower` `?pulsemin` `?s3fill` `?s45fill`. |
| P4.5 | **Refinement round (Nathan, 2026-07-13)** | ✅ | Seven briefs from the phone-preview tuning round — see Build notes below. Staged background (blue field → contract-to-core → black → home-hero gradient), inked blue-on-blue Fragments (shader edge stroke), straight-line Thread whose hop order drives the assembly, core dissolve at build-out (occlusion fix), musical S5 engine (hold + steep expo falloff, deeper dark, ripple/checker/random/cycle patterns), one-section-per-swipe quantizer (ADR-0004 amended), Nathan's `copy_url` dial-in baked as defaults. Verified headless 29/29 (stage pixel walk, swipe stepping, RM, mobile). |
| P4.6 | **Refinement round 2 (Nathan, 2026-07-15, at-desk)** | ✅ `698f5e9` + `0084013` | Second `copy_url` dial-in baked as defaults (all 18 knobs verbatim — scatter 1.8, stroke 2, full-thread 84-hop assembly @ 100ms, 141 bpm, 650 ms stages, poles emanation; `?drift` slider widened to 0.8, was pinned). **Y-jitter fixed**: copy-column lean/release/settle collapsed to one owning tween + `force3D` + a 100 ms momentum-tail arming gap (np-band idiom) — translateY probe dead-still (span 0, 0 flips) through burst+tail. **Poster-scale type**: hero → 12 rem, stage headlines → 6.75 rem escaping the prose column, CTA → 10 rem, leading 0.85; prose register = `.project-blurb__text` verbatim incl. mobile cut. **New chrome**: `ProcessStepCtas` ([previous]/[next] — the /work CTA chip family re-cut small/subtle/left-aligned, `swm:process-step` → quantizer commit, RM instant centered jump, hidden ≤768) + `ProcessMeter` (the carousel osboot ░▒▓█ block meter promoted to site chrome, fractional-cell shading, `NN / 07` counter, data-bg-aware). Nav `⊙ process` re-added (P6 scope pulled forward; gate still stands). |
| P4.7 | **Refinement round 3 (v2 revision deck, 2026-07-16)** | ✅ | Nathan's Notion "SWM Website Revision Notes" executed as B1–B9 of `docs/refinements-master-plan.md` (confirmed calls: hero h1 → "PROCESS"; only [previous] moves top-left; meter = 20 cells; caps words = stage headlines). **B1** copy deck v2 (caps headlines, Notion blurbs verbatim, CTA line, bottom-right tagline). **B2** chrome: [previous] → top-left below nav, [next] stays bottom-left (debug shove retargeted), meter → bottom-center ×20 cells, tagline fixed bottom-right; corners read `--nav-inset`. **B3** Thread → in-scene `Line2` (depth-tested, inside-normal attachment, occluded INTO the globe at assembly — no fade; spec §4 amended). **B4** S1 suspended flood: 36 decoys as one InstancedMesh (draws ≤88), zero-collision relaxed seeding, linear varied-speed self-rotation (wobble cut), flicker-in arrival, 4 blob-tracking labels on the rolling 17-term vocabulary. **B5** S2 opens with a 0.82× push-in while decoys flicker out. **B7** S4 emanation lag 0.16s + wider/jittered stagger. **B8** S5: `?s5zoom` 1.06, `?s5tilt` 28° (tick-owned tiltState), brown-blue inner stroke inverse to the pulse (`?s5stroke`, S5_STROKE_A/B swatches) + per-panel offset color drift, `?decaycurve` expo|linear A/B (expo default). **B9** hero splash: globe alone on blue → PROCESS chars random-in → 1.5s hold → out → auto-glide to stage-01 (guarded); globe-O overlay (SVG placeholder; Lottie = separate asset task). Also A2: driver on house TOUCH_GAIN/RELEASE_MS, `?swipepx` own param. Verified headless: 20/20 suite (stage walk fwd/back, interrupt storm, jitter probe span 0.000px/0 flips, RM stills, mobile chrome). Nathan's first round-3 dial-in baked same day (16 knobs: scatter 2.05, drift 0.09, idlepower 0.54, stroke 1.75, fillfrac 0.98, s3fill 0.31, zoomout 0.6, assemble 2.5, emanate 1.7, bpm 123, pulsemin 0.06, s5tilt 33, s5stroke 1, dropy 0.02, swipepx 500 via motion.js SCROLL_TRIGGER_PROCESS_PX — now LIGHTER than home//work's 600, swipems 800) — checkpoint; refinement continues. |
| P5 | **RM / mobile / a11y / perf gate** | ☐ | `setStageInstant` stills path, native-scroll verification, device pass (≥ home-globe 42–44fps iPhone baseline), draw-call/idle budgets, focus-visible + heading audit. RM paths + ≤90 draw calls (84–85 measured) already verified headless; **the real-device pass needs local hardware** — Nathan is back at his machine, so this is now unblocked. |
| P6 | **Launch (un-gate)** | ☐ | Un-gate as **one 3-file commit** (redirect line, netlify block, sitemap entry), SEO verify, acceptance checklist run. Nav re-add already landed in P4.6 (`0084013`) — note the live nav link 302s home in production until the un-gate ships. |

## Build notes (2026-07-12, P0–P4)

- Built on `claude/load-session-rkxhn0` — based on the PR #14 docs branch tip (`feature/v1-launch` + these docs; v1 had not merged to `main` at build time). Each phase verified with headless-WebGL Playwright suites (pixel-sampled stage states, boundary walks both directions, RM stills, teardown context release).
- **Deviation — S5 pulse dips (`PULSE_MIN` 0.7):** spec §3's `uPower 1.0 ↔ 1.12` over-brighten pulse is invisible on lit panels — pure `0x0000ff` saturates blue at power 1 (textures gave the home flicker its headroom). The pulse dips first (the visible traveling wave), keeping 1.12 as the peak. *(Superseded by the P4.5 musical engine — the dip became the whole envelope: hold on blue, steep expo falloff to `?pulsemin` 0.12.)*
- **Review flag — S3 rest reads as a solid orb:** lit panel color == gap/inner-sphere color, so the settled Core is a featureless blue disc — arguably the literal "goes solid" beat. *(Resolved in P4.5: structure now comes from the black edge strokes, and their cascade burn-off IS the "goes solid" event; the solid disc on black is the intended rest. The `uTint` fallback is retired.)*

## Build notes (2026-07-13, P4.5 — the refinement round)

- Built on `claude/load-session-wippng` (fast-forwarded from `rkxhn0`); Nathan's seven briefs from remote phone tuning, his `copy_url` baked as the new `TUNING_DEFAULTS` (two knobs re-baked with rationale in the `processConfig.js` header: `?idlepower` was dialed for the retired near-black look, `?pulsemin` was pinned at the old slider floor).
- **Staged background:** three DOM layers under the canvas, scene-owned (`pose.bg` + `data-bg` accent re-skins). The S2→S3 contraction reprojects the Core's disc per frame against the live dolly and hands off to the WebGL inner sphere; jumps/compressed catch-ups crossfade. Gotcha for posterity: the morph endpoints must be `tl.call()`s — a `tl.set(blueEl, …)` is itself a tween of the layer, and any `killTweensOf(blueEl)` silently eats it (the stuck-clipped-circle bug found in verification).
- **Inked Fragments:** `panelMaterial` gains a screen-constant edge stroke fed by `aEdgeUv` (natural spherical param recovered per panel pre-re-bake; pole wedges' planar UVs can't hug the silhouette). Panel color is now constant `LIT_COLOR` — the page speaks through `uPower` + stroke + background. Home globe unaffected (mix 0, attribute absent).
- **Thread-as-assembly:** straight `L` segments; assembly stagger = hop order (chained beads first, unchained swept up by center distance); claimed-stamp blips now dip *darker* (blue-on-blue idiom).
- **Core dissolve:** `pose.innerScale` — the filled core shrinks under the S4 emanation (it blocked the expanded lattice), returns on reverse.
- **Swipe quantizer:** ADR-0004 amended (no-snap superseded at Nathan's direction). House accumulator constants; Lenis free-wheel stopped while the island owns input; `?swipe=off` escape; RM untouched.
- Verified headless 29/29: per-stage background pixel walk (incl. contraction hand-off + S4 lattice mix), swipe stepping exact to the pixel on desktop, sub-threshold rubber-band, lock-window burst = one step, keyboard grid, RM native scroll + stills, mobile touch + stills. Suite: session scratchpad `verify_process.py` (screenshots reviewed by eye for the belt/ink/thread/S5 reads).

## Cuts (deliberate)

- **Per-stage deep links** (`#stage-03`) — cheap, decide during build; not a gate.
- **Three-globe trio ("Seed Worlds")** — cut in the grill (2026-07-12): presenting three candidate worlds implies SWM always pitches three concepts. Replaced by the Fragment belt → Thread → Core assembly: one unified concept built from what the client gives us.
- **Panel media texturing** (Fragments sampling client artwork) — the abstract read is the point; revisit only if the page feels too austere in P5.
- **Envelopment bridge into `/process`** — arrivals are plain nav + `swm:fill-release` insurance; a `RouteFill`-lite fade for non-passage swaps is a site-wide polish item (brand-polish audit §4.3), not this page's scope.
- **Sanity-driven copy** — no churn owner; `processContent.js` is the single source.

## Launch gate

- Acceptance checklist (spec §11) passes in full.
- Device pass green: live scene on iPhone ≥ home-globe baseline; RM path verified on-device.
- Un-gate commit reviewed against the 3-file rule; production `/process` resolves (no 302), sitemap includes it, nav links live in both menus.
- Docs updated: spec + this plan flipped to shipped status; `CONTEXT.md` terms confirmed against the built component names.
