# Session Context — 2026-08-27 (2) (fp-grid WORKTREE: DRUM blessed + full refinement wave SHIPPED)

> Auto-generated by `/save-session`. Read by `/load-session`.
> This is the `smallworldmedia_fp-grid` WORKTREE's context (branch
> `refactor/fp-grid-media`, based on the DO-NOT-MERGE snapshot de49839).
> The main checkout (`smallworldmedia_landing-page`, :4321) keeps its own
> session context — do not confuse the two.

## Last Session Summary

Nathan blessed DRUM (`?fpgrid=3`) and drove five refinement rounds in one
session, all shipped + verified: composition balance pass, deck/album WALL
plates, the ripple element (chosen from three pitched trim toggles, then
refined through four dials to a final bake), spine tabs (left-edge asset-name
spines), brand-black service tags + fill-pulse enter_world (extended to the
home hero, blue-rest polarity), the persistent SiteTagline pill + footer
settle choreography (Figma Frame 19), pill-line type tokens + a tracking
sweep, and a repaced overviews_loading bar. Mid-session the Dropbox mount
WEDGED (dev server unstartable) — verification moved to a static copy of
`dist/` served from the session scratchpad on :4322.

## Completed Work (This Session)

- **DRUM blessed + defaults baked** (a09181a): `fpgrid=3`, `platedeg=12`,
  `fpwin=1.1`, desktop `max=8`.
- **Balance pass** (`?fpbal`, in `placeDrumBlocks`): deterministic 3×3
  visible-zone occupancy → variance-minimizing cell nudges + drop rescue +
  size demotion. rossi 0.385→0.010, munchietown 0.321→0.005. `?fpvis` models
  the 0.85 lens crop. Zero PRNG draws; blocks array stays index-aligned.
- **Wall plates** (`fpDrumWall.js`): decks + album art as the detail page's
  orthographic DeckScroller wall on a canvas texture riding the drum (same
  column math; |Δroll|·`?wallgear` stands in for Lenis velocity; `?walldrift`
  idle; canvas 1152 desktop / 704 mobile, page requests 800, motion
  resolution-compensated). BAND_PAGE_CAP 5→12 in index.astro (register
  plates still slice BAND_MAX_PAGES).
- **Ripple** (`fpDrumTrim.js`, Nathan's chosen element, FINAL BAKE 5bcf98f):
  `?fpglow=1` cell-quantized shader ripple; launches INDEPENDENT (uAge[8],
  cap 8) each `?ripevery` house periods; variations `?ripvar` 1 ring /
  2 train / 3 droplet (default); `?ripshade` 0 flat-to-gridlines (default) /
  1 bevel-inset; SHIPPED DIAL: `ripspeed=0.15 · ripevery=4 · ripfall=0.13 ·
  ripw=5 · ripalpha=1` (full ink, falloff does the dimming). `?fpglow=2` =
  lens-corrected pointer trace (forward-apply the pass's backward map, then
  into drum.body local space); `?fpglowa` is the trace's basis.
- **Spine tabs** (`?fptab`, default on): accent tab OUTSIDE the media frame
  off the LEFT edge at the bottom corner, 3×1 cells, asset Sanity title
  (uppercase mono, −90°, truncate 18ch, coords fallback), nav YIQ ink.
- **Furniture** (`?fpfurn`, default off): FORME crosses/captions/floods
  riding the drum — pitched, unchosen, kept as a toggle.
- **Card chrome** (b10811a): `.fp-tag` persistent brand-black fill, light
  accents used AS ink (`--project-color-on-black`, `accentOnBlack()`);
  enter_world fill-pulses toward new `--color-dim-gray` (`?fp1mix`), CTA
  opts out of the .cta-primary transition; fp1Tune dim slider vestigial.
- **Detail album wall** (3a3cd01): AlbumArtViewer = DeckScroller wall
  (BandPager + per-release chips TABLED, file kept); `album_catalog` scramble
  + release-count chip in the side column.
- **SiteTagline** (6df9752, Figma Frame 19 in "SWM Design System", node
  35:178): persistent island in BaseLayout (own transition:persist, OUTSIDE
  .site-shell — the footer-reveal nav slide would carry it); black VISUAL
  WORLDS pill fixed at the links footer's inner inset (settle-by-
  construction); one-time intro (sessionStorage) = clip-path wipe + seated
  word fades (y rise removed 5791c7c), keyed on NEW `swm:hero-lockup-done`
  (Hero fires it as runLockupBeats ends, rAF-deferred); footer reveal ≥0.85
  +0.25s → ©year inline + white lockup (nav 7rem svg, art ≈28% of viewBox)
  slides in; reverses on scroll-out. SiteFooter left column + hero no-fill
  footer bar RETIRED (⚠ homepage now has NO privacy link — flagged to
  Nathan, no call yet). Footer type unified (754eae5): pill + copyright on
  the footer-link 16/22 tier, privacy closes the row at --weight-medium,
  brand blue rest / white hover.
- **Type tokens** (c6d9c5c + 9fbace7): minted `--text-pill`/`--lh-pill`
  (18/22 — nav info pill), `--weight-medium`, `--tracking-body`; ALL
  −0.005em literals swept (12 sites: global/process/project-detail).
- **Home hero CTA** (be6614a → 5791c7c): rest = BLUE pulsing to
  --color-dim-gray (`--cta-pulse` var looped on .hero__enter-wrap, folded
  UNDER the pour in --cta-bg); hover/charge pour WHITE, label → blue.
- **Loading bar repaced** (5791c7c): loader = fixed SIBLING of RouteFill;
  new `swm:loader-start` raised `?loaderlead` ms (350) after commit start;
  `?loaderend` (500) paced close to 100% that the fill reveal WAITS for —
  both on the `?committune` bench, read live (RouteFill imports heroConfig
  TUNING). Verified: loader up ~350ms into the commit, bar 0.11→1.00 across
  the passage.
- **Docs**: `docs/fpgrid-plan.md` STATUS (2) + round-2 + bake + POST-MERGE
  notes current. Memory (`fpgrid-media-in-grid`) current.

## Current Branch & Git State

- Branch: `refactor/fp-grid-media` (worktree; no remote push expected until
  Nathan merges a direction).
- Last 5: 5bcf98f ripple final bake · 5791c7c home CTA/tagline/loader ·
  754eae5 ripple bake + footer type · 9fbace7 tracking sweep · c6d9c5c pill
  tokens. (15 commits this session from a09181a.)
- Uncommitted: none (`.astro` churn restored before each commit, as always).
- Build: GREEN (22 pages). Console clean across all verified routes.

## Pending Tasks (Next Session)

1. **Nathan's next feel pass** on the shipped defaults (plain `/work` = the
   full bake) — open taste calls: `?ripshade` bevel vs flat, `?fpfurn`
   on/off, wall `?walldrift`/`?wallgear`, `?fp1mix` depth.
2. **Homepage privacy link** — removed with the hero footer bar; Nathan has
   not ruled whether it needs a new home on `/`.
3. **Per-release metadata** (catalog #, dates, stream links) — tabled with
   the BandPager; needs a new home if wanted.
4. **POST-MERGE token sweep** (Nathan's standing call, in fpgrid-plan.md):
   after this branch folds in, replace literals with variables SITE-WIDE,
   minting tokens where none match (model = the pill-line pass). Known
   fodder: footer/nav 16/22 literals, undefined `--color-charcoal` in
   work.css.
5. Standing deferrals: chrome keep-outs for [PREVIOUS]/[NEXT] bands,
   enter-dive re-bake (`?entertune`), ATLAS idle beat, mobile pass (HR-6
   gate). Tab neighbor-overlap edge case (1-cell gutter vs 3-cell tab) —
   accepted for now, flag if a composition annoys.
6. If tags need it: strip a leading client-name prefix before the 18ch
   truncation (COCO titles read samey) — offered, no call yet.

## Key Files Reference

| File | Purpose |
|------|---------|
| `docs/fpgrid-plan.md` | STATUS header = the canonical bake + knob reference |
| `src/components/work/world/worldConfig.js` | ALL fp-grid + ripple knobs and baked defaults |
| `src/components/work/world/fpDrum.js` | DRUM: placement + balance pass, plates, tabs, wall/trim wiring |
| `src/components/work/world/fpDrumWall.js` | Canvas deck/album wall plates (DeckScroller math on the drum) |
| `src/components/work/world/fpDrumTrim.js` | Ripple shader (uAge launches), pointer trace, tab texture, furniture |
| `src/components/SiteTagline.jsx` | Persistent pill + intro + footer-reveal choreography |
| `src/components/RouteFill.jsx` | Envelopment fill + repaced overviews_loading (sibling loader) |
| `src/components/hero/heroConfig.js` | TUNING store incl. loaderLeadMs/loaderEndMs (?committune bench) |
| `src/components/Hero.jsx` | Blue-rest CTA pulse, swm:hero-lockup-done, swm:loader-start dispatch |
| `src/components/SiteFooter.jsx` | Links panel, left column retired (min-height band in global.css) |
| `src/lib/projectColor.js` | accentOnBlack() + --project-color-on-black |

## Decisions & Context

- **Nathan's calls this session**: DRUM is THE direction; ripple = the
  chosen trim element (trace + furniture stay as toggles); run-off past the
  frame is good, bare corners are not (the balance pass's brief); walls
  everywhere decks/albums appear; tags black to differentiate from the CTA;
  the CTA must never show the grid through it; home CTA rest = blue; no
  per-word y motion; tagline/copyright on the footer-link type tier; the
  loading bar must visibly RUN.
- **Balance-pass invariants**: index-aligned blocks (nulls = drops),
  finalize AFTER mutation, self-excluded overlap scan, reserved strips
  immovable, zero PRNG draws.
- **Ripple architecture**: one window-sized sector, fragment quantizes to
  the fine lattice (flat per-cell fill meets the lines exactly because the
  lines draw over the glow at order 30 > 28); per-launch uAge[8] ages on the
  CPU (prune when trail exits; wrap-safe), launches every
  RIPPLE_EVERY × HOUSE_PULSE_PERIOD_S.
- **Cross-island events**: dispatch via rAF (gsap-context adoption trap);
  new events this session: `swm:hero-lockup-done`, `swm:loader-start`.
- **SiteTagline placement doctrine**: its OWN island, never inside
  .site-shell (footer-reveal translates the shell); resting inset == the
  links footer's inner padding = settle-by-construction.
- **Figma**: figma-console bridge to "SWM Design System"
  (6dkfmlkatkKOvuAxkuNhyj); Frame 19 = node 35:178 (tagline + footer
  end-state mock — NOTE the built pill is now 16px vs the mock's 18px,
  Nathan's call, mock not updated).
- **TRAPS hit this session**: Dropbox File Provider mount WEDGE — reads of
  some files hang minutes (node_modules/astro/dist/cli), VS Code writes
  ETIMEDOUT, python http.server served truncated JS from dist; recovery =
  copy `dist/` to the session scratchpad and serve from there. Playwright:
  `browser.newPage({ viewportSize })` is silently ignored — the key is
  `viewport`. Sanity image CDN CORS allows :4321/:4322 only.

## Environment / How To Run

- **Dev server is DEAD** (Dropbox wedge): `astro dev` fails with "cli is not
  a function" while `node_modules/astro/dist/cli/index.js` hangs on read.
  `npm run build` WORKS. Current serve: `python3 -m http.server 4322 -d
  <scratchpad>/dist` from a LOCAL copy of `dist/` (all fp-grid knobs are
  client-side, so a static build tunes fine). When Dropbox settles:
  `lsof -tiTCP:4322 | xargs kill`, then `npm run dev -- --port 4322`.
  Root fix: exclude `node_modules` from Dropbox sync.
- Playwright: /opt/homebrew/lib/node_modules/@playwright/mcp/node_modules/
  playwright, `channel:'chrome'`, `viewport` key, page via pager buttons,
  `?debug=1` → `__fpDrum` (has `.balance` stats).

## Blockers

None on the work itself. Environment: the Dropbox mount wedge (above) —
work around in place; needs Nathan to exclude node_modules from sync (or a
Dropbox settle) before the real dev server runs again.
