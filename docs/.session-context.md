# Session Context — 2026-06-24

> Auto-generated by `/save-session`. Read by `/load-session`.

## Last Session Summary

All-frontend session on the immersive `/work` Featured Projects experience: built **P3 — the World Turn** (World-to-World transition) and iterated its motion to Nathan's taste, then built the **OS-window identity card** entrance + a full **PREV/NEXT CTA restyle**, and absorbed a parallel **year-range data-model** change. Checkpoint committed as `698c352`. Still mid-refine — lots of live tuning left.

## Completed Work (This Session)

### P3 — World Turn (the World-to-World transition)
- Restructured `useWorldScene` into **two render slots** (A/B) that roll as one **rigid pair** on the X-axis — outgoing center→off-top, incoming off-bottom→center (mirrored for back) — with a **lens-distortion spike** + **depth recede** + crossfade. One World mounted at rest; both co-present only during a Turn.
- **Motion dialed in over several passes:** staggered two-move w/ overshoot → single rigid roll on a steep-launch cubic-bezier (no overshoot) → fixed the "abrupt stop" → **final: a single GSAP `CustomEase` (`TURN_EASE_PATH`) drives the roll AND the recede/lens (keyed off the eased progress)**, so the whole field settles smoothly with the curve. Nathan hand-authored the curve; `turnms` now 1700ms (slow/theatrical).
- **CTA rubber-band fill:** `ctaMode` = drag / release / commit-pin / commit. Below-threshold release **rubber-bands back**; on commit the CTA **pins full instantly (double-rAF), holds, then eases back over the Turn**. Scroll-to-trigger resistance is live-tunable (`?scroll`).

### WorldCard.jsx (NEW) — OS-window card entrance
- GSAP timeline (via `@gsap/react` `useGSAP`, `SplitText`, `ScrambleTextPlugin` — all free in gsap 3.15): **`PROJECT_##` unicode scramble → panel wipe top→bottom → split-line text reveal → `enter_world` fades in + pulses persistently → service tags stagger up.**
- Card **rides in from the bottom / out the top with the media** on the same CustomEase + duration; **incoming + outgoing cards co-present** during a Turn (small card stack in `FeaturedProjects`). Panel wipes open early so it's a visible "booting window," not an empty gap.
- **Bug fixed:** entrance silently did nothing because `ref` wasn't attached to the card root → `useGSAP` callback bailed on null `ref.current` (no error thrown).

### CTA restyle (part 3)
- **`[NEXT]` / `[PREVIOUS]` / `[MORE]`** bracket labels (dropped "PROJECT"). Looping **caret strip** (`CtaArrows`, 16 carets, constant linear rate) clipped + **mask-feathered to 0 opacity toward the label**. Colour states: **black/white default → lerps to white/black on fill → flashes blue/white at threshold → eases back to black over the Turn.**

### Year-range data model (Nathan's parallel work, committed in this checkpoint)
- `project.yearStart` / `yearEnd` / `isOngoing` + `src/lib/formatYearRange.js`; threaded through schemas (`project.ts`, `mediaAsset.ts`), `queries.js` (`coalesce(yearStart, year)` — backward-compatible), the card, and the detail page. **10 / 16 featured projects migrated** (rest still resolve via legacy `year`). Migration script `scripts/migrate-year-fields.mjs` is **untracked / WIP**.

## Production Scorecard

| Metric | Count |
|--------|-------|
| Clients | 59 |
| Service Tags | 15 |
| Media Assets | 784 |
| Featured Projects (`isFeatured`) | 16 |
| Projects migrated to `yearStart` | 10 / 16 |
| Assets w/ `project` ref | 460 |
| Mux Video Assets | 313 |
| Manifests | 82 |
| Affiliations wired | 0 |

## Current Branch & Git State

- Branch: **`feature/featured-projects-preview`** (off `develop`; **local-only — not pushed**)
- Last commits:
  - `698c352` feat(work): P3 chrome — World Turn curve, OS-window card, bracket CTAs
  - `19caeaa` feat(work): CTA-driven paging + dock pager for Featured Projects
  - `89a13ce` feat(work): WorldScene — inside-sphere WebGL field (P2)
  - `8dce466` feat(work): /work Featured Projects experience skeleton + route restructure
  - `d048edf` feat(cms): P0 data prep — project-ref backfill + orderable ordering
- **Uncommitted / untracked** (intentionally left out of the checkpoint): `docs/.session-context.md`, `docs/cms-backend-roadmap.md`, `.astro/types.d.ts` (generated); untracked `CLAUDE.md`, `AGENTS.md`, `docs/agents/`, `.claude/skills/fable-mode-main/`, **`scripts/migrate-year-fields.mjs`**, mockup PNGs (`COCO …Mockup 01.png`, `orbit-mockup.jpg`).
- ⚠️ Commit author auto-derived as `nathangorey@Nathans-M2.local` (global `user.email` unset).

## Pending Tasks (Next Session)

1. **Keep refining P3 chrome** (Nathan flagged "still need to tune"): scramble speed/chars, `enter_world` pulse rate, `CARD_TRAVEL` (70%), caret-strip visibility (`.fp-cta__arrows` height/feather), entrance timeline positions in `WorldCard.jsx`, and the Turn curve via `?ease=`.
2. **Finish the year-range migration**: 6 featured projects still on legacy `year`; run/commit `scripts/migrate-year-fields.mjs` (currently untracked).
3. **P3 remaining — live-video Near tier**: only Near-tier Tiles play live HLS (reuse the globe's `LivePanelScheduler`); suspend live during a Turn. **Not started.**
4. **P4 — sockets**: `AlbumArtOrbit` (see `orbit-mockup.jpg`) + `BrandDeckViewer`, gated on `buildContentFlow`.
5. **P5 — Envelopment**: home globe → `/work` route swap under a persistent fill (ADR-0002).
6. **CMS Phase 13 — Affiliation Wiring** (0 wired) — still not started.
7. Optional: **push `feature/featured-projects-preview`** to `origin` for backup.

## Active Phase

**Frontend: Featured Projects Preview** — P0 (data) ✅, P1 (route + orchestrator) ✅, P2 (WorldScene + CTA paging) ✅, **P3 (World Turn + OS-window card + CTA chrome) ✅ built, in active tuning.** Next within P3: live-video Near tier. See `docs/featured-projects-preview-plan.md`.
CMS Backend roadmap: Phase 13 (Affiliation Wiring) not started.

## Key Files Reference

| File | Purpose |
|------|---------|
| `src/components/work/FeaturedProjects.jsx` | `/work` orchestrator — CTA paging (`ctaMode` rubber-band/commit), dock pager, co-present card stack, `CtaArrows`, `ctaColor` |
| `src/components/work/WorldCard.jsx` | **NEW** — OS-window card entrance timeline (scramble → wipe → split lines → pulsing CTA → tags); rides in/out with media |
| `src/components/work/world/useWorldScene.js` | three.js — two slots, rigid-roll World Turn via `turnRollEase` (CustomEase), recede + lens spike, parallax |
| `src/components/work/world/worldConfig.js` | All tunables incl. `TURN_EASE_PATH`, `TURN_DURATION`, `TURN_EXIT/ENTER_ANGLE`, `TURN_RECEDE`, `TURN_LENS_SPIKE` (URL-overridable) |
| `src/styles/featured-projects.css` | Card OS-window + CTA (bracket labels, feathered caret strips, `--cta-bg/--cta-fg` colour states) |
| `src/lib/formatYearRange.js` | **NEW** — yearStart/yearEnd/isOngoing → display string |
| `src/lib/queries.js` | GROQ incl. `FEATURED_WORLDS_QUERY` + `coalesce(yearStart, year)` |
| `src/schemas/project.ts` | `isFeatured`, `orderRank`, **`yearStart`/`yearEnd`/`isOngoing`** |
| `scripts/migrate-year-fields.mjs` | **Untracked/WIP** — year → yearStart migration |
| `docs/featured-projects-preview-plan.md` | Phased build plan P0–P5 |

## Environment

- Sanity Project: `b60h4u7o` / dataset: `production`; Token: `.env.local` (`SANITY_WRITE_TOKEN`); Mux: `MUX_TOKEN_ID`/`MUX_TOKEN_SECRET`
- Project Directory: `media/` (gitignored; manifests tracked) — 82 manifests
- Dev: `npm run dev` (Astro, :4321). Studio embedded at `/studio`.
- Deps used this session: `@gsap/react`, `gsap/SplitText`, `gsap/ScrambleTextPlugin`, `gsap/CustomEase` (all free in gsap 3.15; already installed)
- **Live tuning:** every WorldScene knob + the Turn is a `/work?key=value` URL param — `?ease=<path>` (Turn curve), `?turnms`, `?exit`, `?enter`, `?recede`, `?spike`, `?scroll` (CTA resistance), plus `?lens`, `?tile`, `?cluster`, etc.

## Decisions & Context

- **Turn motion = one curve.** A single hand-authored GSAP `CustomEase` (`TURN_EASE_PATH`) drives the roll; the recede + lens spike are keyed off the *eased* progress, so a curve that's flat (zero-velocity) at its ends makes everything settle smoothly — no per-effect window hacks. (See memory `motion-curve-preference`.)
- **No overshoot, momentum-carrying transitions** with a smooth glide into rest — Nathan's standing motion preference.
- **Card design language** (memory `featured-card-design-language`): old-OS-window aesthetic, unicode scramble reveals (ala Claude Code), bracket CTAs; mockup at repo root `COCO …Mockup 01.png`.
- **Project-doc-first** (ADR-0001) + **Envelopment** (ADR-0002) still hold.
- Year model uses `coalesce(yearStart, year)` so the migration can be incremental without breaking unmigrated projects.

## Blockers

- **P3 chrome is mid-tuning** — not final; many values are first-pass / live-tuned.
- **Year migration incomplete** — 6/16 projects unmigrated; `migrate-year-fields.mjs` untracked.
- **Branch is local-only** — push to `origin` for off-machine backup when ready.
- **Live video (Near tier) not built** — the other half of P3.
- Commit author email unset (cosmetic; `git config --global user.email` + `--amend --reset-author` if desired).
