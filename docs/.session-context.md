# Session Context — 2026-05-29

> Auto-generated by `/save-session`. Read by `/load-session`.

## Last Session Summary

Completed the full CMS media ingestion (Phase 12 — full-scale re-run with all 83 manifests), deployed Mux video integration, and uploaded all image and video assets to their respective CDNs. 681 media assets are now live in the Sanity production dataset — 201 with wired Mux video playback. The entire media library is production-ready for frontend integration.

## Completed Work (This Session)

- **Phase 12 (Full Ingestion):** Ran `scripts/ingest-all.mjs` across all 83 manifests — 681 media assets ingested (images uploaded to Sanity CDN with metadata documents).
- **Mux Video Integration:** Installed `sanity-plugin-mux-input` and `@mux/mux-node`. Created Mux Production environment and API credentials.
- **Video Pipeline:** Built `scripts/ingest-videos.mjs` — a batch upload script that discovers motion_* assets without video fields, uploads to Mux via Direct Upload, creates `mux.videoAsset` documents, and wires references to `mediaAsset` docs.
- **Batch 1 Upload:** Uploaded 178 videos under 100 MB to Mux with automatic document wiring.
- **Compression Workflow:** Identified 23 large videos (>100 MB), staged them to `media/_TO_COMPRESS`, user compressed them externally, then programmatically moved compressed versions back to original directories.
- **Batch 2 Upload:** Uploaded all 23 remaining compressed videos (including 4.5 GB → 160 MB `TOBEHONEST x Welker — Too Bright`).
- **Manifest Hardening:** Updated 5 manifests to normalize `.mov` → `.mp4` extensions for compressed video assets.
- **Batch Runner:** Created `scripts/ingest-all.mjs` to orchestrate all 83 manifests sequentially.
- **Mux Studio Secrets:** Created `secrets.mux` document in Sanity dataset so `sanity-plugin-mux-input` can authenticate for Studio video playback.

## Production Scorecard

| Metric | Count |
|--------|-------|
| Clients | 59 |
| Service Tags | 15 |
| Media Assets | 681 |
| Mux Video Assets | 201 |
| Videos Wired to MediaAssets | 201 |
| Manifests | 83 |
| Affiliations wired | 0 |

## Current Branch & Git State

- Branch: `cms-media-architecture`
- Last 5 commits:
  - `234fac0` chore: add agent workflows (session commands + skill library)
  - `f16668f` chore(cms): mark Phases 8/9/12 complete, add Sanity Astro integration + session context
  - `783d5b5` feat(cms): Phase 8 — ingestion pipeline + 169 assets seeded
  - `cd83b87` fix: use Netlify predefined %{submissionId} for unique email subjects
  - `9cd716d` add dynamic subject field for unique Gmail threads per inquiry
- Uncommitted changes: Yes — significant (new scripts, manifest updates, schema changes, video pipeline)

## Pending Tasks (Next Session)

1. **Phase 13: Affiliation Wiring** — Connect client relationship graph (WIKKA → D'Witches, Pulse Artists → managed artists, label → signed artists)
2. **Phase 14: Record Crate Component Integration** — Wire album art data to the frontend Record Crate Browse component
3. **Phase 15: Portfolio Filtering Integration** — Connect serviceType data to Work page filter system
4. **Content Cleanup** — Review the 28 skipped (file-not-found) video assets and resolve or clean up their Sanity metadata documents
5. **Git Commit** — Stage and commit all Phase 12 work (new scripts, manifest updates, ingestion results)

## Active Phase

Phase 13: Affiliation Wiring
- What's done: All pre-requisite phases (7.5–12) are complete. Full media library is live.
- What's next:
  - Wire `WIKKA` → `D'Witches` (label-of)
  - Wire `Pulse Artists` → managed artists (managed-by)
  - Wire label affiliations (Andhera, Front Left, Short Circuit)
  - Verify bidirectional GROQ queries resolve correctly

## Key Files Reference

| File | Purpose |
|------|---------|
| `src/schemas/client.ts` | Client document schema (types, affiliations) |
| `src/schemas/mediaAsset.ts` | Media asset schema (services array, mediaType, mux.video) |
| `src/schemas/serviceTag.ts` | Service tag taxonomy |
| `scripts/ingest.mjs` | Single manifest → Sanity ingestion pipeline |
| `scripts/ingest-all.mjs` | Batch runner — orchestrates all 83 manifests sequentially |
| `scripts/ingest-videos.mjs` | Mux video upload + Sanity wiring (supports `--dry-run`, `--max-size`) |
| `scripts/prepare-compress.mjs` | Utility: gather large videos into staging dir with mapping.json |
| `scripts/seed.mjs` | Client + service tag seeder |
| `docs/cms-backend-roadmap.md` | Master roadmap (Phases 7.5–15) |
| `docs/_manifest-template.md` | Manifest authoring guide (Mode 1/Mode 2) |

## Environment

- Sanity Project: `b60h4u7o` / dataset: `production`
- Sanity Token: loaded from `.env.local` (`SANITY_WRITE_TOKEN`)
- Mux Token: loaded from `.env.local` (`MUX_TOKEN_ID`, `MUX_TOKEN_SECRET`)
- Mux Environment: Production (free tier)
- Project Directory: `media/` (relative to workspace root, gitignored)
- Staging Directories: `media/_TO_COMPRESS/` (originals with mapping.json), `media/_compressed/` (user-compressed versions)

## Decisions & Context

- **Mux Integration Pattern:** Videos are stored as `mux.videoAsset` documents in Sanity, referenced via `video.asset._ref` on `mediaAsset` documents. This uses `sanity-plugin-mux-input` for Studio playback and `@mux/mux-node` for programmatic uploads.
- **Size Threshold:** Initial upload used a 100 MB cap (`--max-size 100`) to manage Mux free tier storage. User compressed large files externally (H.264, targeting 4-8 Mbps). Full library is now uploaded.
- **Extension Normalization:** Some videos changed from `.mov` to `.mp4` during compression. Manifests were updated to reflect the new filenames.
- **File-Not-Found Assets:** 28 video `mediaAsset` documents exist in Sanity without corresponding source files. These need review — they may reference assets that were renamed, moved, or not yet exported.
- **Idempotent Pipeline:** Both `ingest.mjs` and `ingest-videos.mjs` are idempotent — they only process assets that don't already have images/videos wired. Safe to re-run anytime.

## Blockers

None. The full media library (images + videos) is live in Sanity production. Ready for frontend integration.
