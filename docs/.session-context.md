# Session Context — 2026-06-11

> Auto-generated by `/save-session`. Read by `/load-session`.

## Last Session Summary

Built the complete Featured Project Detail page (`/work/[slug]`) from the Figma draft: a dynamically populated editorial page driven by the new Content Population Hierarchy. The page enumerates one route per hero-flagged curated collection (9 pages), lays out media via an aspect-aware full-bleed/split rhythm, and ships with new site chrome (SiteNav, ClientPanel, SiteFooter) and the canonical ServiceTag pill. CONTEXT.md was updated to make the population-hierarchy intent explicit.

## Completed Work (This Session)

- **FeaturedProjectDetail** (`src/components/work/detail/FeaturedProjectDetail.jsx`): orchestrator for `/work/[slug]` — SiteNav → ClientPanel → hero slot → project blurb → content flow → SiteFooter
- **buildContentFlow** (`src/components/work/detail/buildContentFlow.js`): the Content Population Hierarchy engine — hero always first; showcase assets alternate full-bleed → split pair; portrait never full-bleed; `album-art` held out for future AlbumArtOrbit; `process`/`supporting` reserved for future BTS section
- **MediaSlot** (`src/components/work/detail/MediaSlot.jsx`): full/split 16:9 media containers — full Mux HLS loops (no preview cap), lazy viewport attach, load-gated fade
- **SiteNav** (`src/components/work/detail/SiteNav.jsx`): fixed black nav — globe gif, info pill, sitemap links with glyph/SVG icons
- **ClientPanel** (`src/components/work/detail/ClientPanel.jsx`): blue info band — squeezed uppercase title (`client / collection`), client_type + based_in chips, social icons; degrades gracefully when client fields are unpopulated
- **SiteFooter** (`src/components/work/detail/SiteFooter.jsx`): simple variant — near-black bar, SWM globe mark, dynamic copyright year
- **ServiceTag** (`src/components/work/ServiceTag.jsx`): canonical blue display pill, shared component
- **Route** (`src/pages/work/[slug].astro`): getStaticPaths from hero assets grouped by `sourceFolder`, dedupe guard; per-page fetch via `FEATURED_PROJECT_DETAIL_QUERY`
- **Queries** (`src/lib/queries.js`): added `FEATURED_PROJECT_PATHS_QUERY` + `FEATURED_PROJECT_DETAIL_QUERY` (assets + client + optional `project` doc by slug)
- **Slug helper** (`src/lib/projectSlug.js`): `toProjectSlug(clientSlug, collection)` → e.g. `heavy-house-society-live-visuals-2026`
- **Stylesheet** (`src/styles/project-detail.css`): scoped to `.project-detail`, design tokens, `--color-near-black: #0a0a0a` band color, responsive breakpoints
- **Assets** (`public/icons/`): spotify/instagram/beatport SVGs + SWM globe mark downloaded from Figma
- **CONTEXT.md**: new "Content Population Hierarchy" section; vocabulary table additions (FeaturedProjectDetail, SiteNav, ClientPanel, MediaSlot, ServiceTag, SiteFooter, future AlbumArtOrbit/NextProjectCard); route map `/work/[slug]` → FeaturedProjectDetail
- **Verified**: Playwright render of HHS page matches Figma (zero console errors); full `astro build` passes — 9 detail pages generated

## Production Scorecard

| Metric | Count |
|--------|-------|
| Clients | 59 |
| Service Tags | 15 |
| Media Assets | 681 |
| Mux Video Assets | 201 |
| Manifests | 83 |
| Affiliations wired | 0 |

## Current Branch & Git State

- Branch: `Featured-Project-Detail-Page`
- Last 5 commits:
  - `1d56efc` refactor(work): rename header to project_directory, switch to mono typeface
  - `2a7bff1` feat(work): complete /work route — editorial media grid, filter system, HLS video
  - `8e5939e` feat(cms): Phase 12 complete — 681 assets, 201 Mux videos, full media pipeline
  - `234fac0` chore: add agent workflows (session commands + skill library)
  - `f16668f` chore(cms): mark Phases 8/9/12 complete, add Sanity Astro integration + session context
- Uncommitted changes: Yes — entire Featured Project Detail build (new `detail/` components, `[slug].astro` route, `project-detail.css`, `projectSlug.js`, queries, `public/icons/`, CONTEXT.md updates, `.claude/commands/` untracked)

## Recommended Next Action

Create the first `project` document in Sanity Studio — slug `heavy-house-society-live-visuals-2026`, with `title` (display title for the ClientPanel, e.g. "Visual System 2026 Update") and `description` (the 50px overview blurb) — so the editorial copy layer populates end-to-end on `/work/heavy-house-society-live-visuals-2026`. Then verify the blurb band renders and repeat for the remaining featured collections.

## Pending Tasks (Next Session)

1. **Commit the Featured Project Detail build** on `Featured-Project-Detail-Page`
2. **Create `project` documents in Studio** for featured collections (slug = `{client-slug}-{collection-slug}`) so the 50px overview blurb populates — start with `heavy-house-society-live-visuals-2026`
3. **Data hygiene**: review `isHero` on root-manifest assets (e.g. Bedouin "Saga Brand Guidelines" → produces stray `/work/bedouin-bedouin` page; glossary says root assets are never heroes)
4. **NextProjectCard** — scroll-to-next-project component (3 scroll states mocked in Figma, skipped this session per scope)
5. **Phase 13: Affiliation Wiring** — WIKKA → D'Witches, Pulse Artists → managed artists, labels → signed artists
6. **Phase 14: Record Crate Component Integration** — album art → frontend
7. **Phase 15: Portfolio Filtering Integration** — serviceType × Work page filters

## Active Phase

Frontend: Featured Project Detail (`/work/[slug]`) — core structure ✅
- What's done: dynamic route + population hierarchy + nav/panel/blurb/media-flow/footer, verified against live CMS data
- What's next:
  - Project docs for editorial copy (blurb band)
  - NextProjectCard (3-state scroll transition)
  - AlbumArtOrbit (populates when directory carries album-art)
  - BTS section (process/supporting assets)
  - Back-to-featured "← all feat. projects" buttons

CMS Backend: Phase 13 (Affiliation Wiring) — not started

## Key Files Reference

| File | Purpose |
|------|---------|
| `src/components/work/detail/FeaturedProjectDetail.jsx` | `/work/[slug]` orchestrator |
| `src/components/work/detail/buildContentFlow.js` | Content Population Hierarchy engine |
| `src/components/work/detail/MediaSlot.jsx` | Full/split media containers (HLS + CDN images) |
| `src/components/work/detail/SiteNav.jsx` | Fixed top nav bar |
| `src/components/work/detail/ClientPanel.jsx` | Blue project info band |
| `src/components/work/detail/SiteFooter.jsx` | Simple footer |
| `src/components/work/ServiceTag.jsx` | Canonical service tag pill (shared) |
| `src/pages/work/[slug].astro` | Detail route — getStaticPaths from heroes |
| `src/lib/projectSlug.js` | Collection → route slug helper |
| `src/lib/queries.js` | All GROQ queries (incl. detail-page pair) |
| `src/styles/project-detail.css` | Detail page stylesheet |
| `src/components/work/ProjectDirectory.jsx` | `/work` orchestrator |
| `src/schemas/project.ts` | Project doc — editorial copy + contentBlocks override |
| `src/schemas/mediaAsset.ts` | Media asset schema (isHero, contentRole, sortOrder) |
| `docs/cms-backend-roadmap.md` | Master roadmap (Phases 13–15 pending) |
| `CONTEXT.md` | Domain glossary + Content Population Hierarchy |

## Environment

- Sanity Project: `b60h4u7o` / dataset: `production`
- Token: loaded from `.env.local` (`SANITY_WRITE_TOKEN`)
- Mux: `.env.local` (`MUX_TOKEN_ID`, `MUX_TOKEN_SECRET`)
- Project Directory: `media/` (relative to workspace root, gitignored)
- Dev Server: `npm run dev` (Astro, port 4321)
- Figma: SWM Website April 2026 — "Featured Project Detail Page" frame `27:134` (file `eB3w1SfwUHXyAxf90jPv90`)

## Decisions & Context

### Content Population Hierarchy (documented in CONTEXT.md)
- **Pages are populated, not authored** — manifest/tagging metadata is the layout instruction set
- `sourceFolder` = grouping key; `isHero` = sizzle reel, always slot 1; `sortOrder` = flow sequence; aspect ratio = slot sizing; `contentRole` = flow membership
- Flow rhythm after blurb: full-bleed → split pair → full-bleed → …; portrait (< 1.2 ratio) never full-bleed
- Editorial copy layers in from an optional `project` document whose slug matches the route; `project.contentBlocks` page-builder remains a manual-override path (not wired)

### Detail page implementation
- Detail route does NOT import `work.css` — `project-detail.css` is self-contained (scroll unlock scoped to `.project-detail`)
- All media slots are 16:9 cover-cropped per Figma (both full and split variants)
- Detail videos play full HLS loops (no 4s preview cap, unlike MediaCard grid previews)
- Figma band color `#0a0a0a` scoped as `--color-near-black` inside `.project-detail` (not added to global tokens)
- Slot image widths: full = 2000px, split = 1200px (Sanity CDN `?w=` param)
- ClientPanel chips/socials render conditionally — client `city`/`country`/`links` are still null in production (Phase 13+)
- Social icon set available: Spotify, Instagram, Beatport (`public/icons/`)

### Naming
- Components follow CONTEXT.md Frontend Component Vocabulary (newly added rows approved-by-build this session)
- Route slugs: `{client-slug}-{slugified-collection}` via `toProjectSlug()`

## Blockers

- **No `project` documents exist in Sanity** — overview blurb band renders without editorial text until they're created (graceful fallback in place)
- **Stray hero flags on root-manifest assets** generate unintended detail pages (`/work/bedouin-bedouin`) — needs Studio cleanup
- **28 missing video sources** (carried over) — 28 mediaAsset docs reference video files absent on disk
