# Session Context — 2026-08-29 (FOLD EXECUTED + site deployed to stable preview; CORS + domain fixed)

> Auto-generated by `/save-session`. Read by `/load-session`.

## Last Session Summary

Ran the fold-in runbook to completion: `feature/v1-launch` fast-forwarded
016b343 → 77ab5c3 (losslessness proven — exactly the 8 expected pure
deletions, each byte-compared to its committed blob before rm), build green
(22 pages), 3-agent Playwright verify green on all three pages. Step-5
cleanup done on Nathan's go (worktree + both fp-grid branches deleted, stale
:4322 server killed) and pushed. Then took the site to a **stable Netlify
preview**: discovered the real hosting (live = smallworld.media, prod branch
`main`, still the OLD site), fixed the dead `smallworldmedia.co` baked into
astro.config/robots (515fd89), enabled branch deploys via API, and fixed the
deck-wall-breaking Sanity CORS 403 on deployed origins. Preview verified
clean end-to-end.

## Completed Work (This Session)

- **Fold-in runbook executed** (all 5 steps): prove (8 pure deletions vs
  16581ad, byte-identical blobs) → drop duplicate dirt → `merge --ff-only`
  → verify (build 22 pages; Playwright: /work DRUM+black field+__fpDrum
  balance, /process live O, home globe+tagline+settle-fade 1→0.34→1; zero
  console errors) → cleanup (worktree removed, `refactor/fp-grid-media` -d,
  `backup/fp-grid-pre-fold` -D — pre-rewrite history gone) + push.
- **Hosting mapped** (was recorded nowhere): Netlify site
  `smallworldmedia-landingpage` (id `6f030001-4c81-4b23-8b74-ac8f778a3b2a`),
  custom domain **smallworld.media**, production branch `main` (still the
  old single-page landing, 226+ commits behind). Only a merge to `main`
  replaces the live site.
- **Dead-domain fix** (`515fd89`): `smallworldmedia.co` is UNREGISTERED at
  the .co registry; astro.config `site` + robots.txt sitemap now point at
  `https://smallworld.media`.
- **Stable preview URL live**:
  **https://feature-v1-launch--smallworldmedia-landingpage.netlify.app** —
  branch deploys enabled via `updateSite` API
  (`allowed_branches: ["main","feature/v1-launch"]`); auto-rebuilds on
  every push to the branch. First build `d07206f` (empty trigger commit —
  deliberately avoided the API build endpoint, which could default to
  rebuilding main). CI build needs NO env (sanityClient hardcodes
  project/dataset; token optional; Mux/PUBLIC_ vars unused by src).
  A one-off CLI draft deploy also exists (6a933c91…) — superseded.
- **Deck-wall CORS fix** (Nathan's bug report: decks blank on /work at the
  preview, fine on detail pages): `cdn.sanity.io` images are
  origin-ALLOWLISTED per project — disallowed origins get a hard 403. Only
  crossOrigin loads enforce it (drum wall → WebGL canvas textures); detail
  pages use plain DOM `<img>` → immune; localhost was allowlisted → dev
  clean. Added via Sanity MCP (project `b60h4u7o`, credentials false):
  `https://smallworld.media` + `https://*--smallworldmedia-landingpage.netlify.app`
  (wildcard verified against draft-style hashes). Re-tested: preview /work
  zero errors, walls render (screenshot-verified).
- **Memory**: new `netlify-hosting` memory (site facts, draft recipe, CORS
  trap); `fpgrid-media-in-grid` closed out as FOLDED.

## Current Branch & Git State

- Branch: `feature/v1-launch` @ `d07206f`, origin in sync.
- Last 5: d07206f branch-deploy trigger (empty) · 515fd89 domain fix ·
  77ab5c3 session save (rode the merge) · fd20f92 opad bake · 09e8886 O
  pole fidelity.
- Uncommitted: `.astro/*` churn only (`git checkout -- .astro`, as always)
  + the 5 personal untracked strays (fable-mode skill, 2 root logo SVGs,
  2 stale PNGs).
- Worktrees: only `swm-pass-2` (refine/site-pass-2) remains besides this
  checkout. fp-grid worktree and branches are gone.

## Pending Tasks (Next Session)

1. **Nathan's taste pass on the shipped bakes** — now reviewable ON THE
   STABLE PREVIEW URL (all knobs live: fpglow/fptab/fpfurn, wall
   drift/gear, ?fp1mix, ripple dials, camlag, ospin/ostroke/opad).
2. POST-MERGE token sweep (Nathan's standing call): site-wide
   literal→variable sweep, minting tokens where none match (model = the
   pill-line pass). Known fodder: footer/nav 16/22 literals, undefined
   `--color-charcoal` in work.css.
3. Homepage privacy link — homeless since the hero footer bar retired.
4. Per-release metadata (catalog #, dates, stream links) — tabled with the
   BandPager; needs a home if wanted.
5. Standing deferrals: chrome keep-outs for [PREVIOUS]/[NEXT] bands,
   enter-dive re-bake (`?entertune`), ATLAS idle beat, mobile pass (HR-6
   gate; IS_MOBILE is a load-time snapshot in worldConfig AND processConfig
   — live cross-768px resize doesn't re-branch), saga deck wall starts at
   page 2 (cover IS the bedouin hero — flagged, no call).
6. **Launch runway** (when Nathan calls it): merge to `main` = production.
   CORS + domain + sitemap already launch-ready. Open gates from
   v1-launch-plan: real-device mobile pass, F15 analytics decision, F9 real
   form submission test (the stable preview URL now exists for exactly
   this), /process un-gate = the 3-file commit (redirect line + netlify
   block + sitemap entry).

## Key Files Reference

| File | Purpose |
|------|---------|
| `docs/fpgrid-plan.md` | STATUS header = canonical DRUM bake + knob reference (now marked FOLDED) |
| `astro.config.mjs` | `site: https://smallworld.media` (fixed 515fd89) |
| `netlify.toml` | Build cmd + the forced-302 route gates (/process etc.) — rides every deploy |
| `src/lib/settleResize.js` | The site's settle-debounce (two-tier resize doctrine) |
| `src/components/work/world/worldConfig.js` | fp-grid knobs: wallgear 12, vtiles 6, camlag 0.7, fpfade 0, maxTilesFor() |
| `src/components/work/world/fpDrumWall.js` | Deck/album wall plates — the crossOrigin loads the CORS fix unblocked |
| `src/components/process/liveLockupGlobe.js` | The live O — home scroll-globe at rest, mark colorway |
| `src/components/globe/useGlobeScene.js` | Home globe settle fade; the O borrows this globe's build |
| `src/lib/sanityClient.js` | Hardcoded projectId/dataset; SANITY_WRITE_TOKEN optional — why CI builds need no env |

## Decisions & Context

- **Deploy law**: production = `main` on Netlify site
  `smallworldmedia-landingpage` → smallworld.media. Branch pushes build the
  stable preview automatically. Draft recipe (one-offs):
  `npx netlify-cli deploy --dir=dist --no-build --site <id>` — never
  `--prod`. Netlify CLI is authed on this machine; repo NOT linked — pass
  `--site` explicitly.
- **Sanity CORS trap**: cdn.sanity.io 403s non-allowlisted origins (vary:
  Origin) — bites ONLY crossOrigin/WebGL image loads, so it presents as
  "walls broken, detail pages fine, dev clean". Any new deployed origin
  needs `add_cors_origin` (credentials false) or the drum walls break.
- **Resize doctrine (site law)**: cheap per-event; expensive waits for
  settle; camera chases on `?camlag`; NEVER repopulate media on resize.
- **camLag invariant** (process): shadow state re-syncs from the live rig
  at every retarget; transitions own the camera; aspect defers to no one.
- **The O = the home globe** — one source of truth; home dial-ins flow in.
- **/work field is BLACK** (fpfade 0 + `:has` root paint); process field is
  electric blue → the mark runs inverted (white ring) there.
- **Playwright traps**: `newPage({viewportSize})` silently ignored — key is
  `viewport`; locator.evaluate auto-waits on missing elements — use page.$
  for optionals; WebGL `toDataURL` blind — composited page.screenshot.
  Bundled at `/opt/homebrew/lib/node_modules/@playwright/mcp/node_modules/playwright`,
  `channel:'chrome'`.
- **Bench corners**: /work entertune TR · texttune TL · fp1tune+deckdebug
  BR; home herotune TR · committune BR; lenistune BL · footertune TL.

## Blockers

None. Fold landed, preview live and verified, production untouched until
the deliberate merge to `main`.
