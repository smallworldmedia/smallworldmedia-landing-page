# Session Context — 2026-07-18 (home-hero rework + refinement pass 1; cascade PIVOT queued)

> Auto-generated by `/save-session`. Read by `/load-session`.

## ⚡ Start here — the ONE big open task

**The "cascade" (note 6) must be REWORKED. The current implementation is wrong.**
Nathan's exact correction: *"the cascade is just a rotation of the globe — i can see the
poles rotate into view and away from the camera, instead of the globe being fixed, and the
panels emanating from the upper pole (out of view) downward before being 'consumed' by the
lower pole."*

- **What shipped (commit `8408788`):** a rigid PITCH rotation of the whole globe (ambient
  yaw→pitch). This tumbles the sphere, so the poles roll through the front view. **Wrong
  mechanism.**
- **What Nathan actually wants:** the globe geometry stays **FIXED** (keep the underside
  framing, elev ~70). The **CONTENT** flows down the meridians — assets are born at the
  **top/far pole (out of view)**, migrate **downward** across the fixed latitude bands, and
  are **"consumed" at the bottom/near pole** (which faces the camera). A content-conveyor,
  not a rotation. Topologically a north→south meridian flow (source at top pole, sink at
  bottom pole) — a rigid rotation CANNOT produce it (rotation moves content on circles;
  Nathan wants pole-to-pole flow). Distinction confirmed with Nathan at end of session.

**Implementation notes gathered this session (read `LivePanelScheduler.js` in full):**
- Panels are a 12-lon × (5 lat-bands + 2 pole rings) grid; each `panel` has `.asset`,
  `.centerDir`, `.mesh.material.uniforms` (texA thumbnail, texB video, uMix thumb↔video,
  uPower, uBlueMix, uCornerR).
- `LivePanelScheduler` (~2Hz) owns `panel.asset` mutation for **hidden-hemisphere thumbnail
  cycling** (`swapHidden`: loads a new thumbnail into texA, advances `panel.asset`, only on
  panels with score < SWAP_SCORE so swaps are never visible) AND live-video promote/demote
  (uMix crossfade thumb↔video on prominent panels, MAX_LIVE budget).
- **The conveyor rework must reconcile with the scheduler**: both want to control
  `panel.asset`/textures. The conveyor changes assets on VISIBLE panels (the whole point),
  which the scheduler deliberately avoids (it only swaps hidden). Design decisions for the
  next session:
  1. Revert the pitch rotation (`InteractionController` ambient back to still/tiny; globe
     fixed at the brand tilt). `cascadeSpeed` knob re-purposes to conveyor pace.
  2. Build a **content conveyor**: order panels into columns by longitude, rows by latitude
     (top pole → 5 bands → bottom pole). On a timer (pace ∝ `cascadeSpeed`), shift each
     column's assets DOWN one row; new asset enters the top pole from the pool, bottom pole's
     asset retires. **Crossfade** each panel old-asset→new-asset — likely needs a NEW
     crossfade channel in `panelMaterial` (e.g. `texPrev` + `uConveyorMix`) separate from the
     video `uMix`. Reconcile who owns `panel.asset`: the conveyor drives it, the scheduler
     follows (a shifted live panel demotes; the new asset may re-promote).
  3. Pace slow enough that video loading isn't overwhelming (Nathan's stated constraint —
     `cascadeSpeed` is the video-load knob). 5 lat-bands is coarse; a staggered per-latitude
     crossfade ripple reads smoother than a synchronized whole-globe swap.
- **Consider proposing the approach + trade-offs to Nathan before a huge build** (crossfade
  channel + scheduler reconciliation is real design work). Or build the cleanest first version
  and let him react/dial.

## Last Session Summary

Executed the **full home-page hero rework** (16 commits on `refine/home-hero-rework` off
`01f4af1`): camera rig, nav normalization, off-right grandeur composition, logo→globe intro
with the **real SWM lockup**, panel-by-panel blue commit, blob labels. Then a **refinement
pass** from Nathan's first bench bake covering his 6 notes (ring→button, camera roll, rounded
tiles, blue-fill-leads commit, downward cascade, viewport labels), plus **2 adversarial-review
fixes**. Session ended with Nathan clarifying the **cascade needs a fundamental rework**
(fixed globe + content conveyor, see top) — queued for the next dedicated session.

## Completed Work (This Session)

**Original rework (7 commits `3a5d4a3`…`ac0948c`):** camera rig + overlay bridge + `?herotune`
bench (identity = bit-identical framing); nav normalized (home pill variant deleted → standard
links); resting comp + circular ring CTA (LATER RETIRED); commit transition (recenter +
panel-blue); logo→globe intro variants A+C; blob labels (shipped OFF then turned ON); **real
inline lockup wired** (`src/assets/swm-lockup-inline.svg`, blue `#0000FF`, globe glyph = the
"o" of world, live WebGL globe frames in the hidden glyph slot). Perf fix `1b63d8e`. Full
adversarial review found 1 (refuted) finding.

**Refinement pass 1 (`edf765e`…`04a2888`) — Nathan's 6 notes + bake:**
- `edf765e` — baked dialed comp (fill 1.26/offsetX 0.46/offsetY -0.35/elevDeg 70); **retired
  the scroll RING for a `scroll_to_enter` BUTTON** below the left tagline (tagline `--lead-max`
  written from the globe disc's left edge → clears globe + wraps); camera **ROLL** knob
  (`?heroroll`, + = tilt right, sign verified); **rounded panel TILES** (`panelMaterial`
  `uCornerR` uniform; note 5).
- `25537cc` — bench scaffolding (blueLead / cascadeSpeed / labelStroke params + sliders).
- `03f58e3` — **commit blue-fill LEADS** then camera dives (note 4): one linear timeline split
  by `?bluelead`; panels blue over [0,blueLead] while camera holds, then recenter+dolly on
  `commitEase(seg(p,blueLead,1))`. Verified blue-then-dive.
- `8408788` — downward cascade as pitch rotation (note 6) — **NOW SUPERSEDED, see top**.
- `1af4c27` — labels retargeted to **VIEWPORT membership** (front-facing + on-screen, not a
  region), **bracketed tags** `[tag]`, radial-outward chip by `?labelstroke` (note 3).
- `1704b4e` — master-plan HR-R row.
- `04a2888` — **2 review fixes**: (1) pitch clamp keyed on cascade *configured* not *rolling*
  → under RM/`cascadespeed=0` a drag could flip the globe upside-down permanently; gated on
  `cascadeActive`. (2) rounded tiles leaked onto `/lab/globe` (shared `useGlobeScene`); threaded
  `cornerRadius` as a hero option (home passes `PANEL_CORNER_RADIUS=0.12`, lab/process get 0).

Verified 13/13 on the unified flow (intro→cascade→blue-leads commit→/work→FP-3 return, RM
static, /process untouched, 0 console errors) + both fixes re-verified.

## Nathan's latest bake `copy_url` (apply next session, AFTER the cascade rework)
`?herofill=1.31&heroroll=2.5&textgap=70&bluelead=0.3&cascadespeed=4.5&labelmax=7&labelhold=6&labelstroke=126&bluecascade=rows`
- Bake into `TUNING_DEFAULTS` (heroConfig): fill 1.31, roll 2.5, textGap 70, blueLead 0.3,
  labelMax 7, labelHold 6, labelStroke 126, blueCascade 'rows'.
- **`cascadespeed=4.5` — DON'T bake as-is**: its meaning changes with the conveyor rework
  (was rotation deg/s → becomes conveyor pace). Re-derive after the rework.

## Current Branch & Git State

- Branch: **`refine/home-hero-rework`** — tip `04a2888`, **16 commits ahead of `01f4af1`**,
  NOT pushed, NOT yet PR'd toward `feature/v1-launch`.
- Uncommitted: only the standing baseline (`.astro/*` generated, `docs/.session-context.md`
  this write) + untracked strays (`SWM-globe_white.svg`, `breadcrumb_move.png`,
  `homepage_refine_01.png` = Nathan's reference screenshot, `Small World Media Logo Inline
  Blue-01.svg` = his source lockup [prepped copy = `src/assets/swm-lockup-inline.svg`],
  `.claude/skills/fable-mode-main/`). Build clean (23 pages).

## Pending Tasks (Next Session) — priority order

1. **⚡ CASCADE REWORK** (note 6, see top) — fixed globe + content conveyor. The single biggest
   task. Read `LivePanelScheduler.js` + `useGlobeScene.js` texture assignment first; likely add
   a `panelMaterial` crossfade channel + a conveyor in `useGlobeScene`; revert the pitch
   rotation in `InteractionController`.
2. **Bake Nathan's latest `copy_url`** (values above) — after the cascade rework settles
   `cascadeSpeed`'s new meaning.
3. **HR-6 device gate + PR:** iPhone perf pass (cascade video-decode churn + mobile comp), then
   PR `refine/home-hero-rework` → `feature/v1-launch`. Also the standing A2b Lenis-bake
   follow-ups (NP_ARM flick-tail, touch parity) on the same device pass.
4. Housekeeping: the intro's variant A vs C and fill-mode panels/circle are still both live —
   bake will delete the losers (not flag-rot).

## Two-session workflow + git strategy (Nathan, end of session)

Two work streams run in tandem, then **fold into a SINGLE commit** for the v1 PR:
- **Session 1 — cascade rework** (dedicated, deep, single-threaded): the content-conveyor
  above + the bake. Home hero only.
- **Session 2 — pass-2 site-wide refinements** (fans out): a LIST of independent items, to be
  run via **Claude Code agent teams** (coordinator + parallel teammates) — decided this
  session because the list parallelizes well (the cascade does NOT, so it stays its own focused
  stream). Task source = the **SECOND Notion page** under **"SWM Website"** (the FIRST list,
  mirrored in `docs/refinements-master-plan.md` waves A–5, is now **[done] on Notion**; use the
  Notion MCP / connector — search "SWM Website").

**Agent teams — SET UP this session (Claude Code 2.0.56, experimental):**
- **Flag ENABLED** in `~/.claude/settings.json` → `env.CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS: "1"`
  (global, off-repo, reversible — remove the `env` block to disable). Just launch `claude`;
  Claude proposes teammates when a task benefits.
- **Teammates SHARE one working tree + branch** (they do NOT get isolated worktrees by
  default) — this *matches* the single-commit goal (all work → one branch → squash), BUT
  **two teammates editing the same file overwrite each other.** So the coordinator MUST
  **partition the pass-2 list by FILE/COMPONENT ownership** before spawning (each teammate owns
  distinct files; don't let them self-discover overlapping work).
- **Run pass-2 in the `refine/site-pass-2` WORKTREE** so it can run parallel to the cascade
  session without fighting the checked-out branch: `git worktree add ../swm-pass-2
  refine/site-pass-2` then `cd ../swm-pass-2 && claude`. The team's teammates share THAT
  worktree; the cascade session stays in the main dir on `refine/home-hero-rework`.
- **Team UX:** agent panel below the prompt (arrows select, Enter opens/messages a teammate,
  Ctrl+T task list, Esc interrupts). Start 3–5 teammates, discrete tasks, monitor for file
  overlap. Known rough edges: task-status lag (nudge to mark done), no in-process teammate
  `/resume`, one team per session, token cost scales per teammate. Coordinator squashes the
  teammate commits into a clean set at the end.

**SHARED BASE + SINGLE-COMMIT git strategy (Nathan's directive):**
- **Both sessions fork from the SAME committed tip** = the commit this save creates on
  `refine/home-hero-rework` (the immutable base/fork point). Do NOT let either session rebase
  the other's base out from under it.
- **Separate working lines off that base** (two parallel writers cannot share one branch ref /
  working tree): cascade continues on **`refine/home-hero-rework`**; pass-2 gets branch
  **`refine/site-pass-2`** (created off the base this session, ready to check out).
- **To run truly in parallel in the same repo**, use a **git worktree** for one stream so they
  don't fight over the checked-out branch — e.g.
  `git worktree add ../swm-pass-2 refine/site-pass-2` — and open the pass-2 (agent-teams)
  session there. (Agent-teams teammates may also self-isolate into per-teammate worktrees.)
- **Fold to ONE commit after BOTH approved:** integrate the two lines and **squash-merge to
  `feature/v1-launch` as a single commit** (GitHub squash-and-merge, or locally: merge
  `refine/site-pass-2` into `refine/home-hero-rework`, resolve, then `git merge --squash` /
  squash-PR to `feature/v1-launch`). The whole home-hero-rework + cascade + pass-2 diff lands
  as one commit on v1-launch. Neither stream pushes to v1 alone — both gate on approval.

## Key Files Reference

| File | Purpose |
|------|---------|
| `src/components/globe/useGlobeScene.js` | Scene hook: rig (`applyRig`), tick, rotation apply (pitch cascade to REVERT), `createPanelMaterial` call (cornerRadius home-only), scheduler construction. THE cascade-rework file. |
| `src/components/globe/InteractionController.js` | Ambient drift (yaw→pitch cascade to REVERT to fixed) + drag/inertia. |
| `src/components/globe/LivePanelScheduler.js` | Live-video promote/demote + hidden thumbnail cycling. Owns `panel.asset`/textures — the conveyor must reconcile with it. |
| `src/components/globe/panelMaterial.js` | Panel shader: texA/texB/uMix (video), uPower, uBlueMix, uCornerR (rounded tiles). Conveyor likely needs a new crossfade channel here. |
| `src/components/globe/buildGlobeGeometry.js` | Panel geometry — needed to order panels into the lon/lat conveyor grid. |
| `src/components/hero/heroConfig.js` | `TUNING_DEFAULTS` + params + copy_url. Bake target. Cascade/blueLead/labelStroke knobs. |
| `src/components/hero/HeroTunePanel.jsx` | `?herotune=1` bench (comp/commit/globe/intro/labels sections). |
| `src/components/Hero.jsx` | Home hero conductor: intro/commit/gesture/rig, `PANEL_CORNER_RADIUS=0.12`, passes `cascadeSpeed`/`cornerRadius` to VideoGlobe. |
| `src/components/hero/HeroLabels.jsx` | Viewport-targeted blob labels (bracketed tags, `?labelstroke`). |
| `src/components/hero/HeroIntro.jsx` | Logo→globe intro; `buildLockup`/`placeGlyphRect` = the asset seam. |
| `src/assets/swm-lockup-inline.svg` | The prepped real lockup (globe glyph id `swm-lockup-glyph`). |
| `docs/refinements-master-plan.md` | Status board — Wave 5 (HR-0…HR-6) + HR-R refinement pass. |

## Decisions & Context

- **Cascade = content conveyor, NOT rotation** (the session's key pivot — see top).
- **Ring retired for a button** — Nathan judged the circular ring CTA over-engineered.
- **Rounded tiles are home-only** (`uCornerR` uniform, threaded via hero option; lab/process = 0).
- **Pitch-clamp bypass gated on `cascadeActive`** (nonzero speed AND motion allowed) — a still
  globe keeps the ±40° clamp so drags can't strand it upside-down (RM contract).
- **Blue-fill leads the commit** — `?bluelead` splits the timeline; camera holds until blue.
- **Verify recipe (unchanged):** scratch playwright-core at `/tmp/pw-verify`, headless shell
  `chromium_headless_shell-1228/chrome-headless-shell-mac-arm64/chrome-headless-shell`, args
  `--use-angle=metal --autoplay-policy=no-user-gesture-required`; **prod preview on a scratch
  port (4399)** for motion checks (never re-import modules after an edit — HMR fork trap);
  React range inputs via native prototype setter + bubbled `input`. **`:4321` is Nathan's dev
  server — never kill it;** scratch previews get 4399 and are killed after.
- **⚠️ Subagent flakiness this session:** two `general-purpose` Agent-tool subagents returned
  injection-shaped garbage (0 tool calls, 0 edits) mid-session — I implemented notes 4/6/3 by
  hand instead. The `Workflow` tool path worked fine (both adversarial reviews ran clean). If
  Agent-tool subagents flake again, fall back to direct implementation or Workflow.

## Blockers

- None hard. The cascade rework is a design-heavy task (conveyor + scheduler reconciliation) —
  consider proposing the approach to Nathan before a large build. Bake waits on the rework.
  Device gate needs Nathan's iPhone. Not yet pushed/PR'd.
