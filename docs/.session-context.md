# Session Context — 2026-09-04: scale pager rounds 6–10 DONE + CHECKPOINT COMMITTED (2d59980); next = further refinement on Nathan's device pass

> Auto-generated by `/save-session`. Read by `/load-session`.

## Last Session Summary

Nathan drove five more revision rounds on the GRATICULE (`?pager=scale`) —
rounds 6 through 10, each built + probe-verified before his next look — then
called the CHECKPOINT: the whole pager arc (shared engine + all three arms +
ten rounds of scale refinement) is now COMMITTED at `2d59980` on
`feature/v1-launch`. He said plainly: **"we still need to refine further after
this"** — the next session continues the revision loop from his device pass.

## Completed Work (This Session)

- **Round 6** — `?magnet` 2.2 + `?fliptau` 0.005 BAKED (clamp floor dropped to
  0.001 so the bake isn't silently clamped); **the Turn evaluates at DISENGAGE
  only** (`stallMs: Infinity` — the engine's pointerleave-only contract;
  `SCALE_STALL_COMMIT_MS` deleted) killing the frozen-Turn name stack-up;
  minor half-station ticks DELETED; flipper box moved INSIDE the strip
  (hair → box → stations paint order) and runs seat-edge → name end so
  number + name sit on solid near-black over the axis line.
- **Round 7** — pointer 12×8; wheel SEAM rules at every loop boundary (own
  strip-level elements, warp-projected, under the box); ticker hard-clips
  flush (fade mask + trailing pad gone); **the hairline is a screen-space SVG
  path curved along the warp's implied cylinder** (static ellipse
  x = X0·√(1−(u/R·pitch)²); the whole `--scale-q` close-origin machinery
  deleted); desktop rest chip + hint at `--text-link` (the corner-pill line);
  `?scrimtune` bench (scrim opacity/blur/grain dials, `hold scrim` preview).
- **Round 7b** — the BLUR FIX (element opacity < 1 composites the
  backdrop-filter result over the SHARP original — depth moved into the
  background alpha, engaged opacity = 1; memory: backdrop-filter-opacity-trap);
  `--scrim-tint` (project color into the flood); grain anim fps (default 30);
  the house METRONOME (`housePulseLoop` seeks wallclock % period — every pulse
  loop phase-locks); the hint PULSES inverse-ink (white at the accent phase,
  dim at the grey-veil hit — verified locked to the CTA dim-fill peak).
- **Round 7c** — scrim recipe BAKED (Nathan's dial): `--pager-scrim` 0.4,
  tint 0.75, blur 7.5px, turbulence grain 0.84/1oct/0.82/rgb @184px, 30fps
  `fp-scrim-grain` steps() driver (RM-still; bench overrides via
  `--scrim-grain-anim`).
- **Round 8** — thaw-at-COMMIT (`setFrozen` in `land()`) fixing the
  incoming-card static flash; near trio: ±1 rows at FULL `--scale-sel`,
  centre ×`--scale-sel-bump` 1.15, box tracks sel×bump; desktop pitch 44→48;
  mobile dial sets (`?grainsize ?grainamt ?grainfreq ?grainfps ?scrimblur` +
  `?cardname ?tagtext ?tagpad ?tagpadx ?taggap`); service-tag ink on
  `--project-color-fg` (light accents flip black); (an exclusion-blend globe
  was added here and REMOVED in r9).
- **Round 9** — `[select_project]` = accent CHIP (project color + fg ink),
  ROTATED vertical on ≤768 (right edge, ~25vh — the name-creep fix; the
  vertical-rl + 180° idiom, never rotate(-90) on a horizontal box); tickers on
  near rows too; mobile grain BAKED (amt .56 / size 90 / freq .85 — ≤768 block
  + `SCRIM_GRAIN_MOBILE`); mobile fp-card BAKED (headline 6rem, tags 0.857rem,
  gap `--space-3`); **the MOBILE CORNER PRESET** (`?corners`, default ≤768):
  four fixed media seats — tier-1 deck-scale diagonal (walls own these first)
  + tier-2 diagonal, per-slug hashSeed flip, `placeDrumBlocks` gained an
  `anchors` path, ring/jitter/balance SKIPPED, exactly 4 media surfaces load.
- **Round 10** — corner plates smaller + tighter (`?t1deg` 1.35, `?t2deg` 0.8,
  `?cornerin` 0.8 baked; walls ride t1 under the preset); mobile sel-name cap
  WIDENED to `min(28vw, 8rem)` (mirror 28vw/112 — the chip left the band);
  **`PLATE_Z_EPS`** 0.003 per-plate radial shells fixing Munchietown's
  pre-settle Z-FIGHTING (all plates sat coplanar at SHELL_RADIUS−0.12;
  spawn-slerp crossings shimmered until settle).
- **Docs**: spec doc carries rounds 6–10 with per-round open calls;
  `docs/tunables-guide.md` at **253 params** — `--check` PASSES.
- **CHECKPOINT COMMIT `2d59980`** (19 files, +5715): engine + 3 arms + all
  ten rounds + benches + probe harness + docs. Tape/tuner ship in it
  untouched behind their params — the bake (delete them + legacy rail) waits
  for the scale arm's blessing.

## Current Branch & Git State

- Branch: `feature/v1-launch` @ `2d59980` — **3 commits ahead of origin, NOT
  pushed** (deploy law: branch pushes rebuild the stable preview; production
  = merge to `main`).
- Last 3 commits: `2d59980` feat(fp-pager) checkpoint · `f625090`
  docs(tunables) · `1e45cda` chore(skills).
- Uncommitted: `docs/.session-context.md` + `docs/v1-launch-plan.md` (this
  save — commit as `docs(session)` after). `.astro/*` = generated noise.
  Personal untracked strays (leave alone): `Client Logos/`, 2 root logo SVGs,
  2 reference PNGs, `.claude/skills/fable-mode-main/`, `scripts/shots/`
  (probe screenshots — never commit).

## Pending Tasks (Next Session)

1. **Nathan's device pass on rounds 6–10** — he explicitly said more
   refinement is coming. Live dials for the pass: `?t1deg ?t2deg ?cornerin`
   (corner preset), `?grainsize ?grainamt ?grainfreq ?grainfps ?scrimblur`
   (mobile scrim), `?cardname ?tagtext ?tagpad ?tagpadx ?taggap` (fp-card),
   `?magnet ?fliptau ?detent` (feel). Hard-load
   `http://192.168.1.9:4321/work?pager=scale` (dev server binding
   `0.0.0.0:4321`; restart: `npm run dev -- --host 0.0.0.0 --port 4321`).
2. **Open watch-items from the rounds**: neighbours at full size cross the
   curved hairline with NO black fill (only the selected row has the box —
   r8 flag); the parked-ticker ~4px box lead between wheel notches (r7 —
   fix = box scale riding the row's f² via CSS `round()`); shipped backdrop
   blur's mobile-GPU cost (`?scrimblur=0` compares); engage-DURING-external-
   Turn still freezes it mid-flight (the last pause-guard half).
3. **The BAKE, once the scale arm is blessed**: delete tape/tuner skins + the
   legacy `.fp-pager` rail + its marker-follower, re-run the tunables check,
   update the guide (arm-gated rows), spec doc close-out.
4. Then the standing queue: tunables-guide latent-bug tickets · at-machine
   items (memory: mobile-pass-followups) · scroll-trigger field pass · launch
   runway (F15 analytics, F9 form, merge to main).

## Key Files Reference

| File | Purpose |
|------|---------|
| `docs/fp-pager-rework-approaches.md` | THE spec: engine contract, rounds 1–10 + per-round open calls |
| `src/components/work/pager/usePagerGesture.js` | shared engine — FSM/damper/flick/keyboard, `wrap`/`magnetExp`, commit-at-disengage (`stallMs` non-finite) |
| `src/components/work/pager/GraticulePager.jsx` | THE arm: all round machinery (freeze/thaw, flipper, tickers, curved-hair build, grain dials) |
| `src/components/work/scrimNoise.js` | baked grain recipes (desktop + `SCRIM_GRAIN_MOBILE`) + `noiseUri` — shared by bench + URL dials |
| `src/components/work/ScrimTunePanel.jsx` | `?scrimtune` bench (defaults mirror the bake; stills the shipped grain driver) |
| `src/styles/featured-projects.css` | `.fp-scale` blocks + scrim recipe/driver + corner-preset-era mobile bakes |
| `src/styles/global.css` | `--scale-*` tokens both tiers (`--scale-sel-bump`, sel caps, pitch 48) + `--pager-scrim` 0.4 |
| `src/lib/motion.js` | `SCALE_*` dials + `housePulseLoop` METRONOME (wallclock phase-lock) |
| `src/components/work/world/fpDrum.js` | corner preset (`anchors` path, seats, walls-first) + `PLATE_Z_EPS` |
| `src/components/work/world/worldConfig.js` | `CORNER_PRESET / CORNER_T1_MUL / CORNER_T2_MUL / CORNER_INSET` |
| `scripts/pager-probe.mjs` | Playwright harness (SwiftShader doctrine in header); `--variant=scale [--mobile\|--rm]` |
| `docs/tunables-guide.md` | 253 params; `node scripts/tunables-keys.mjs --check` is the gate |

## Decisions & Context

- **The Turn evaluates at DISENGAGE, and the freeze lifts at COMMIT** — the
  two halves of the pause-screen contract (r6 + r8). A settle while engaged
  is a pure preview; `land()` thaws before `commit()` so entrance
  choreography is born against a running globalTimeline.
- **The hairline is screen-space** (r7): under warp the cylinder is a STATIC
  ellipse in screen coords — never re-derive per frame, and `--scale-q` is
  gone for good.
- **Backdrop-filter trap** (r7b, own memory file): element opacity < 1
  composites the filtered backdrop over the sharp original — scrim depth
  lives in background alpha, element opacity holds 1.
- **The corner preset rides the wall-anchor idiom** (r9/r10): fixed
  normalized seats through `placeDrumBlocks`' `anchors` path; walls first;
  per-slug diagonal flip; everything scales off `PLATE_DEG` so `?platedeg`
  still moves the whole system.
- **Never rotate(-90) a horizontal text box for a vertical seat** — the
  vertical-rl + 180° idiom keeps translate percentages honest (r9).
- **Per-plate radial shells** (`PLATE_Z_EPS`, r10): coplanar sector plates
  z-fight during spawn slerps; every media plate gets its own radius.
- **Guide doctrine unchanged**: add a param → add a row → `--check` PASS (253).
- **Deploy law unchanged**: production = merge to `main`; branch pushes
  rebuild the stable preview. NOT pushed this session — 3 commits local.

## Blockers

None in the code. Waiting on Nathan's rounds-6–10 device verdict (he has
explicitly queued further refinement) and the open watch-items above.

**Dev server**: running bound to `0.0.0.0:4321` →
**`http://192.168.1.9:4321/work?pager=scale`** (hard-load; soft nav drops the
query). Verified serving the current tree this session (HMR). Dies with this
session — restart: `npm run dev -- --host 0.0.0.0 --port 4321`.
