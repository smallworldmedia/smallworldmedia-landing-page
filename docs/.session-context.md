# Session Context — 2026-08-29 (evening) (revision round SHIPPED: privacy pill + footer choreography + nav underline + ink paint fix; /process UN-GATED)

> Auto-generated by `/save-session`. Read by `/load-session`.

## Last Session Summary

Nathan approved all shipped bakes, then filed a revision round from the
stable preview — all implemented, Playwright/pixel-verified, COMMITTED and
PUSHED (`d863919` + `967614e`), so the branch preview is rebuilding with
everything. Headline: /process un-gated (the planned 3-file commit); the
nav bracket/fxrule system retired for the footer's label-only underline;
a persistent lower-right PRIVACY pill mirroring the tagline pill (solves
the homeless homepage privacy link); the footer link-row stagger completing
the footer choreography; and a deep two-stage diagnosis of the /work nav
ink desync that ended at a real Chromium paint-invalidation trap.

## Completed Work (This Session)

- **/process un-gated** (`d863919`): redirect out of process.astro,
  forced 302 out of netlify.toml, `/process` out of SITEMAP_EXCLUDE. Spec
  §6 marked UN-GATED (anti-re-add guard); launch plan F2 row noted.
  `?debug` bench now publicly reachable (consistent w/ other benches —
  flagged to Nathan, no objection).
- **Nav underline swap**: brackets + fxrule fully retired (SiteNav.jsx
  effect/element + ~200 lines CSS). Hover/focus = footer idiom (underline
  on `.site-nav__label` only, 3px offset); glyph→text back to tight 2px;
  `data-current`/`aria-current` still maintained, NO current-page visual
  for now (mobile menu's current underline went too — one-rule restorable).
  Mobile menu press feedback = label underline on `:active`.
- **Privacy pill** (SiteTagline island owns BOTH corners now): fixed
  black capsule → /privacy at bottom-right, mirrored right→left clip wipe
  on the tagline intro beat; sets `data-privacy-landed` on <html> at land
  (+ re-asserts on every `astro:after-swap` — ClientRouter wipes <html>
  attributes on soft navs). Desktop hides the in-row footer privacy link;
  ≤768px shows it as the mobile fallback (fixed pill hidden there).
- **Footer link-row stagger** (SiteFooter): house stagger (0.45/0.07,
  y+10, power2.out, 0.25s beat) at reveal ≥0.85, reverse <0.5, gated on
  privacy-landed; ONE watcher (SiteTagline's attribute-gated rAF shape)
  covers scroll AND driven modes via the shared `--footer-reveal`
  broadcast; targets rebuild if the visible link set changes (768px
  crossing). CSS hidden ground on `.site-footer__link`.
- **Footer band fixes**: brand BLACK (was near-black — pills read darker
  than their own band); `min-height` computed from the lockup stack (old
  5rem under-reserved → lockup poked above the band; now ~18px air);
  label-only hover underline (decoration propagated into BOTH flex items →
  broken two-segment line); `--footer-bottom-inset` minted (16→12px),
  shared by tagline pill / privacy pill / footer padding.
- **/process caption keep-out**: clears the privacy pill (scrollbar-aware
  right reserve + pill width) AND the centered meter — font
  `clamp(0.5rem, calc(1.38vw - 0.5rem), --text-mono)` re-engages vw
  scaling on desktop (old 1.5vw never dropped below cap → constant-width
  line collided with the meter at ≤1384px after the first fix). Verified
  1440/1366/1280/1100.
- **NAV INK FIX (the big one, Nathan's rossi→kamino report)** — two-stage:
  (1) mid-weighted `--ease-ink` for stall resistance, then (2) Nathan's
  element-specific description cracked the REAL bug: Chromium does not
  paint-invalidate text/SVG consuming an ANIMATING inherited @property —
  computed style glides (getComputedStyle probes mask it!) but pixels
  freeze then CUT at settle. The pill survived only because its animating
  background forced repaints. Fix: `--nav-ink-l` SNAPS on <html>;
  links/labels/pill/lockup carry ELEMENT-LOCAL
  `transition: color/fill var(--ink-fade, var(--project-color-fade)) var(--ease-ink)`;
  lockup fill now `var(--nav-ink)` (was hsl(calc) twin); `--ink-fade`
  zeroed inside navAccent's commitInstant keeps arrivals/clears
  flash-free. Final call: `--ease-ink: var(--ease-panel)` — ink rides the
  wash in lockstep (pixel-verified both directions; the mid-weighted
  bezier is documented in the token comment as the dial-back).
- **Infra**: `http://localhost:4330` added to Sanity CORS (preview port —
  allowlist is PER-PORT); memories written:
  `clientrouter-html-attribute-wipe`, `custom-property-animation-paint-trap`,
  netlify-hosting updated. Stale memory corrected in passing: projectColor
  now exists on ALL 13 projects (11/13-missing note is obsolete).
- **Verification**: 4 workflows/agent passes this session (~1.0M subagent
  tokens): 3-page + review sweep, soft-nav latch re-verify, caption sweep,
  band+ink verify, and two PIXEL-based ink probes (screenshots only — the
  golden rule for paint bugs). Zero console errors everywhere.

## Current Branch & Git State

- Branch: `feature/v1-launch` @ `967614e`, PUSHED (origin in sync) —
  branch preview auto-rebuilding with all of it.
- Last 5: 967614e revision round · d863919 /process un-gate · 0bf05ed
  session save · d07206f branch-deploy trigger · 515fd89 domain fix.
- Uncommitted: none (`.astro` churn restored) + the 5 personal untracked
  strays (fable-mode skill, 2 root logo SVGs, 2 reference PNGs).

## Pending Tasks (Next Session)

1. **TOKEN SWEEP (Nathan's call — do BEFORE the mobile pass** so mobile
   work lands on tokens, not literals; model = the pill-line pass).
   Fodder: footer/nav 16/22 type literals; undefined `--color-charcoal`
   in work.css; NEW from this round — pill skin duplicated across
   `.site-tagline__pill`/`.site-privacy` (padding/radius/black), footer
   nav `margin-right: 7rem` + `margin-bottom: 0.571rem` clearance
   literals, the `4.96rem` band min-height calc, the caption's `6.5rem`
   reserve + `1.38vw` formula constants.
2. **Mobile device pass** (HR-6 gate): real-device; includes the
   IS_MOBILE load-time snapshot trap (worldConfig + processConfig don't
   re-branch on live cross-768px resize), the ≤768px privacy fallback
   (in-row footer link), and the mobile menu (no current-page visual now).
3. **Launch runway leftovers**: F15 analytics decision (cookieless,
   decide at deploy) · F9 real form submission test (preview URL exists).
   Then merge to `main` = production.
4. Standing deferrals: chrome keep-outs for [PREVIOUS]/[NEXT] bands (now
   ALSO the privacy pill corner on /work) · enter-dive re-bake
   (`?entertune`) · ATLAS idle beat · saga deck wall starts page 2 ·
   ROSSI next-project thumbnail renders near-black in headless (eyeball
   on real browser) · `goTo()` pager clicks don't check the turn lock
   (measured smooth, deliberate gap) · per-release metadata PARKED
   (socket stays, not needed with simplified deck viewer).

## Key Files Reference

| File | Purpose |
|------|---------|
| `src/components/SiteTagline.jsx` | BOTH persistent corners: tagline pill + privacy pill, intro wipes, `data-privacy-landed` latch + after-swap re-assert |
| `src/components/SiteFooter.jsx` | Link-row stagger watcher (mode-agnostic, hysteresis, rebuild-on-set-change) + reveal broadcast |
| `src/lib/navAccent.js` | Accent control point; `commitInstant` now zeroes `--ink-fade` (element-local ink gate) |
| `src/styles/global.css` | `--footer-bottom-inset` + `--ease-ink` tokens; `.site-privacy`; element-local ink transitions (~L520-560); footer band/stagger CSS |
| `src/styles/process.css` | Caption keep-out: pill reserve + the `1.38vw - 0.5rem` band-tracking font clamp |
| `src/pages/process.astro` | UN-GATED — real page, no redirect |
| `docs/process-page-spec.md` | §6 marked UN-GATED (don't re-add the gate from the checklist) |
| `docs/fpgrid-plan.md` | DRUM bake + knob reference (FOLDED) |

## Decisions & Context

- **Paint trap (memory: custom-property-animation-paint-trap)**: never
  animate an inherited @property that text/SVG consume — computed glides,
  pixels freeze; getComputedStyle polling MASKS it. Snap the var, put
  `transition: color/fill` on the consumers. Verify paint bugs with
  composited screenshots + pixel sampling ONLY.
- **Latch trap (memory: clientrouter-html-attribute-wipe)**: ClientRouter
  soft navs replace ALL <html> attributes; persisted-island latches must
  re-assert on `astro:after-swap`.
- **Ink timing (Nathan's call)**: `--ease-ink: var(--ease-panel)` — ink
  tracks the wash exactly; mid-weighted `cubic-bezier(0.65,0,0.35,1)` is
  the documented dial-back if turn jank ever reads snappy.
- **One black**: footer band + both corner pills = `--color-black`;
  everything on the bottom edge seats on `--footer-bottom-inset`.
- **One link language**: label-only hover underline everywhere (nav +
  footer); no current-page visual anywhere "for now".
- **Sanity CORS is PER-PORT** for localhost (4321 dev ≠ 4330 preview —
  4330 now allowlisted; wildcard covers the Netlify preview origins).
- **Playwright traps (additions)**: /work wheel listener is on the DECK
  element — `page.mouse.move(720,500)` before wheeling or nothing turns;
  one turn per 2-wheel round + ~2.1s lock wait; and the paint-bug golden
  rule above. (Standing: `viewport` key, page.$ for optionals, composited
  screenshots for WebGL, bundled playwright at
  `/opt/homebrew/lib/node_modules/@playwright/mcp/node_modules/playwright`, `channel:'chrome'`.)
- **Deploy law unchanged**: production = merge to `main`; branch pushes
  rebuild the stable preview automatically.

## Blockers

None. Everything pushed and verified; preview rebuilding. Next session
opens on the token sweep.
