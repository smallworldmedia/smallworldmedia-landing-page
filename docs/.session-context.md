# Session Context — 2026-07-13 (refinement round)

> Auto-generated by `/save-session`. Read by `/load-session`.

## Last Session Summary

The process-page REFINEMENT session (remote/cloud): Nathan reviewed the P0–P4 build on the phone-preview artifact while traveling and sent seven briefs plus his dialed `copy_url`. All seven shipped and were verified headless (29/29 assertions): his tunables baked as the new defaults, the **staged background** (blue field → contract-into-the-Core → black → home-hero gradient), **inked blue-on-blue Fragments** (shader edge stroke), the **Thread rebuilt as a real assembly mechanism** (straight segments, hop-order pull-in), the **core dissolve** at build-out (his occlusion flag), the **musical S5 rhythm engine** (hold + steep expo falloff, deeper dark, ripple/checker/random/cycle patterns), and the **one-section-per-swipe quantizer** (ADR-0004 amended). The phone-preview artifact was rebuilt and republished AT THE SAME URL — his existing link now serves the new build.

## Completed Work (This Session)

- **`9c2e803`** — Nathan's 2026-07-13 `copy_url` dial-in baked into `TUNING_DEFAULTS` (scatter 2.45, drift 0.23, fillfrac 0.89 both breakpoints, s3fill 0.5, s45fill 1.1, zoomout 0.45, threadhops 20, threadms 200, assemble 3, emanate 1.8, bpm 133, dropy 0.4). Two knobs re-baked, NOT copied (rationale in `processConfig.js` header): `?idlepower` 0.92 (his 0.05 was dialed for the retired near-black-shards look) and `?pulsemin` 0.12 (his 0.3 was the old slider FLOOR, not a choice). New knobs: `?stroke ?hold ?decay ?pattern ?swipe ?scroll ?swipems`; panel ranges widened wherever he was pinned at a rail; selects carry per-knob option lists.
- **`7afe60d`** — the visual system rework (one commit, five briefs):
  - *Staged background:* three DOM layers under the canvas, scene-owned (`pose.bg`, `data-bg` accent re-skins — Thread inks black on blue, mono tokens/captions go white). S2→S3 contracts the blue field into the Core's live screen-space disc (clip-path circle reprojected per frame against the dolly, seamless hand-off to the blue inner sphere); reversals expand it back; jumps/compressed catch-ups crossfade. S5 fades in the home-hero gradient.
  - *Inked Fragments:* `panelMaterial` gains a screen-constant black edge stroke (`uStrokeMix`/`uStrokeWidthPx`/`uStrokeColor`, fwidth-based) reading `aEdgeUv` — the natural spherical param recovered per panel BEFORE the local-origin re-bake (pole wedges' planar UVs can't hug the silhouette). Panel color is constant `LIT_COLOR` blue now; color never tweens — the page speaks through uPower + ink + background. Strokes burn off with the S3 cascade (the "goes solid" beat — retires the old solid-orb review flag AND the `uTint` fallback). Home globe untouched.
  - *Thread-as-assembly:* straight `L` segments from the Core's center-to-be; assembly stagger IS the hop order (chained beads seat first in connection sequence — string pulled taut; unchained swept up by center distance); claimed-blips dip *darker* (blue-on-blue idiom); the string rides its beads in and releases after seating.
  - *Core dissolve:* `pose.innerScale` — the filled core shrinks under the S4 emanation (was blocking the expanded lattice), returns on reverse.
  - *Musical rhythm:* per-hit envelope attack→HOLD on blue (`?hold` beats)→steep expo falloff to `?pulsemin`. Patterns rows/equator/ripple (radial from a central panel)/checker (per-beat parity)/random (seeded, re-dealt per pass)/**cycle** (default). Loop padded to the exact pass grid; rebuilds live.
- **`75383c5`** — one-section-per-swipe gesture quantizer in `useProcessScrollDriver`: house accumulator constants (600px fill, ×2 touch, 160ms rubber-band, one commit per gesture) → `lenis.scrollTo` glide to the next section's in-viewport rest on the Turn curve, locked till it lands; boundaries/copy/machine fire as from a free scroll. Copy column leans with the pull below threshold. Lenis free-wheel stopped while the island owns input; chrome/overlay/fields stay native; keyboard steps the grid; footer = one extra step; `?swipe=off` escape; RM untouched.
- **`1224d81`** — docs: spec §§1/3/4/5/9 + narrative rewritten to the new system, plan gains P4.5 row + build notes, **ADR-0004 amended** (no-snap superseded at Nathan's direction).
- **Artifact republished at the SAME URL** (`claude.ai/code/artifact/99ac39d9-2c5a-45a5-9d18-0b20006cea93`, title "SWM /process — build preview", label `refinement-round-07-13`): island re-bundled standalone (esbuild via repo node_modules, fonts inlined, Iosevka re-subset 1.5MB→20KB, Lenis bootstrapped). Smoke-tested headless (mobile viewport, swipe = one section, 0 errors). Rebuild recipe: session scratchpad `artifact/build.mjs` (self-contained — re-derivable from its description in this doc).

## Production Scorecard

> Live counts fetched at session start (2026-07-13) via the Sanity MCP connector — unchanged this session (no CMS work).

| Metric | Count |
|--------|-------|
| Clients | 59 |
| Service Tags | 15 |
| Media Assets | 776 |
| Featured Projects (`isFeatured`) | 16 |
| Manifests | 82 |
| Affiliations wired | 0 recorded |

## Current Branch & Git State

- Branch: **`claude/load-session-wippng`** — this session's designated branch, fast-forwarded from `claude/load-session-rkxhn0`'s tip (`c26bdd3`) at session start, fully pushed. **The authoritative tip is `origin/claude/load-session-wippng`** — fetch the branch HEAD, not a pinned hash (the final commit is always the `docs(session)` save itself). `rkxhn0` still points at `c26bdd3` and is now superseded by this branch.
- Session commits: `9c2e803` (defaults + knobs) → `7afe60d` (visual system rework) → `75383c5` (swipe quantizer) → `1224d81` (docs) → this save.
- Uncommitted changes: none at save.
- **PR #14** (docs) still open against `feature/v1-launch`; **PR #13** (brand-polish P0) still open; the token-fix duplication note from the last save still applies. This branch stacks the same way `rkxhn0` did.

## Pending Tasks (Next Session)

1. **Nathan's taste pass on the new system** — everything is live on his artifact link (same URL as before; it auto-loads the new baked defaults when opened WITHOUT query params — his old tuned URL still carries `idlepower=0.05&pulsemin=0.3`, which now read differently on the blue field; open clean with just `?debug`). New knobs to audition: `?stroke`, `?hold`/`?decay`/`?pattern` (try `pattern=ripple` and `checker`), `?scroll`/`?swipems` for the swipe weight.
2. **P5 — real-device pass** (unchanged blocker: needs local hardware; headless numbers — 19–42fps SwiftShader, draws 84–85 — only prove budget shape).
3. **P6 — launch:** nav re-add (`SiteNav.jsx:253` + mobile menu, `⊙ process`), un-gate = ONE 3-file commit, SEO verify, spec §11 acceptance run (checklist items on the Thread/assembly wording predate the rework — re-read against the amended spec).
4. Merge strategy per #14/#13 notes. Elsewhere: v1 launch gate (ws6 real-device pass) still ☐; CMS Phase 13 still not started.

## Active Phase

**Process page (v2): docs ✅ · build P0–P4 ✅ · P4.5 refinement round ✅ · P5 device pass ☐ · P6 launch ☐.**

## Key Files Reference

| File | Purpose |
|------|---------|
| `docs/process-page-spec.md` | THE build contract — amended 2026-07-13 (background §1, ink §3, Thread §4, quantizer §5, knob table §9) |
| `docs/process-page-plan.md` | Status board — P4.5 row + both build-notes blocks |
| `docs/adr/0004-process-scroll-driver.md` | Amended: no-snap superseded; quantizer rationale |
| `src/components/process/useProcessScene.js` | The stage machine + staged background + rhythm engine |
| `src/components/process/useProcessScrollDriver.js` | Boundaries + Lenis bridge + the swipe quantizer |
| `src/components/process/processConfig.js` | Baked dial-in + new knobs (re-bake rationale in header) |
| `src/components/process/ProcessDebugPanel.jsx` | `?debug` chrome — new sliders/selects |
| `src/components/globe/panelMaterial.js` | Edge-stroke uniforms (aEdgeUv contract in header) |
| `src/styles/process.css` | Background layers + `data-bg` accent skins |

## Environment

- Sanity Project: `b60h4u7o` / dataset: `production` — reachable via the MCP connector only (egress proxy blocks `*.sanity.io`; no `.env.local` in remote sessions).
- Remote-session notes (standing): full `astro build` can't prerender Sanity-backed routes here (`/process` itself builds); Google Fonts hangs ~13s — abort in Playwright runs; pip Playwright needs `executable_path="/opt/pw-browsers/chromium"`; astro dev toolbar intercepts clicks — dispatch via JS in tests; `pkill -f 'astro dev'` between runs.

## Decisions & Context

- **The page's visual language is now: constant blue + uPower + black ink + staged background.** Color tweens are gone. Anything that wants "structure" gets it from the stroke; anything that wants "drama" gets it from power depth or the background beat.
- **killTweensOf-eats-tl.set gotcha** (found by pixel assertion): a `tl.set(el, …)` is itself a tween of `el`; any `killTweensOf(el)` — even in a later-firing call of the SAME timeline — silently removes it. Background-morph endpoints are `tl.call()`s for this reason. Check this first if a DOM layer ever sticks mid-choreography.
- **Assembly order model:** chained panels get the leading `chainShare` of the stagger window in hop order; unchained follow by ascending center distance. `?threadhops` up to 84 = every bead on the string.
- **Rhythm grid:** one pattern per 8-beat pass (`PASS_BEATS`); `cycle` = rows→equator→ripple→checker→random (5 passes/loop); checker alternates parities per beat with the envelope clamped to its 2-beat cycle; random re-deals per pass (seeded, deterministic).
- **Quantizer honesty:** the document is real — scrollbar drag and RM native scroll still work; only wheel/touch/keys are quantized. `?swipe=off`/`=0` restores free scroll live from the panel.
- **`fillfrac` is now one value (0.89) for both breakpoints** — Nathan dialed it on the phone; the old 0.85/0.7 split is gone.
- ScrambleText trims leading whitespace — chip separators live in flex `gap` (standing).
- Boundary geometry `[top 60%, bottom 60%]` (standing); section rests = viewport-centered, computed fresh per gesture.

## Blockers

- **P5 needs real hardware** — cannot run from a remote container.
- None new. The swipe quantizer landed clean against Lenis (stop/start hand-off verified in both directions).
