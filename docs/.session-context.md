# Session Context — 2026-07-17 (v1-launch sync → FP-2 + A2b shipped → CREATIVE wave next)

> Auto-generated by `/save-session`. Read by `/load-session`.

## Last Session Summary

Synced the process branch with `feature/v1-launch` (PRs #30–#35), then executed
both remaining AGENT items as ultracode workflows: **FP-2 perceived-instant
video first frame (PR #36)** with full measured acceptance, and the **A2b Lenis
live dial (PR #37)** with a five-preset regression matrix. Nathan approved both
on preview; both are MERGED into `feature/v1-launch` and merged back here.
Issue #23 closed. Checkpoint taken before starting the CREATIVE items
(NAV-2 / FP-1 / DP-1).

## Completed Work (This Session)

- **Merged `origin/feature/v1-launch` into this branch** (`269048e`): netlify
  conflict resolved keeping BOTH the /process gate and the FP-4b redirects;
  probe-verified /process at the 4px chrome inset (prev x=4, next 4px off
  bottom), home HP-1 recomposition live, zero console errors.
- **PR #36 — FP-2 (merged 2026-07-17, closes #23):**
  `{startFragPrefetch, startLevel: 2, maxBufferLength: 12}` on MediaSlot +
  NextProjectBand, `eager` hero prop (seeds `useState(eager)` → manifest fetch
  at hydration; destructured so it never leaks onto the `<figure>`), IO band
  `'400px 0px'` (next-band's own 200px deliberately kept), posters `&time=0`.
  **Measured (system Chrome, rVFC, N=3 + warmups):** rossi hero 1688.5→1141.8ms
  (−32%); stream max 9→8; initial-8s weight +5.2MB (pre-load by design — the
  12s buffer cap returned the scroll-pass to baseline but can't reduce short
  loops that download in full); ⚠️ two near-black `time=0` posters flagged for
  Nathan (`y02EgA7x…` lum .073, `S029xcxq…` lum .080) — per-asset time or
  epsilon, his call.
- **PR #37 — A2b dial (merged 2026-07-17):** `?lerp` / `?wheelmult` /
  `?lenisdur` read once in `smoothScroll.js` start(), positive-finite gated,
  spread AFTER `LENIS_TUNING` (which stays `{}` — shipped feel provably
  unchanged; no-params profile measured identical to explicit Lenis defaults).
  Regression matrix green on an integration preview: Grid Socket drift clamped
  40.00px/0 flips on all 5 presets; np-band `NP_ARM_MS=250` phantom-fire
  swallow held (worst case lerp .075's 1.9s tail); /process quantizer
  byte-identical under `?lerp=.075`; ProcessMeter live. **Key discovery: the
  orbit scroll-kick is spec'd but DORMANT** — `momentum.js` `kick()` has zero
  importers; it reads `getLenis().velocity` (scales with lerp/wheelmult), so
  record baked values before `?orbitkick` is ever dialed.
- Final v1-launch merge-back (`3663a88`): board conflict + process-page-spec
  param-table conflict resolved (dial rows folded under the quantizer rows).
- Board updated: FP-2 ✅ PR #36 · A2b ◐ (agent side done, Nathan's bake
  remaining) · A2b gate ✅ (re-gate when momentum.js gains importers).

## Current Branch & Git State

- Branch: **`claude/load-session-wippng`** — pushed, tip `3663a88` + this save.
  Dirty: only `.astro/types.d.ts` (generated) + standing untracked files
  (`SWM-globe_white.svg`, `breadcrumb_move.png`, `.claude/skills/fable-mode-main/`).
- `feature/v1-launch` tip = PR #37 merge; PRs #15, #29–#37 all merged there.
  This branch contains all of it (two-way synced as of this save).
- Worktrees cleaned; branches `refine/video-first-frame` / `refine/lenis-dial`
  live on origin. Build verified: 23 pages.
- Dev server: `npm run dev` (:4321) running on this branch.

## Pending Tasks (Next Session)

1. **IN PROGRESS: CREATIVE ultracode wave — NAV-2 / FP-1 / DP-1** (Nathan's
   pick of the next items, 2026-07-17):
   - NAV-2: nav micro-interactions ×4 candidate options behind `?navfx`.
   - FP-1: enter_world dim pulse — consumes `housePulseLoop` (dim polarity per
     motion.js), RM-gated.
   - DP-1: next-project transition rework — fade confined to the media box,
     media grows to viewport.
2. **Nathan:** Studio drag-reorder on merged rossi (17 assets; hero =
   first-in-order) — he'll do it in the coming days.
3. **Nathan dial sessions:** A2b Lenis feel (`?lerp=0.075` / `?lerp=0.14` /
   `?wheelmult=1.2` on any detail page → bake into `LENIS_TUNING`); the two
   near-black FP-2 posters call.
4. **Process refine pass 2** → P5 device pass (iPhone ≥42–44fps) → P6 3-file
   un-gate.
5. v1 residuals: ws6 device gate → PR `feature/v1-launch` → `main`; F9 inquiry
   submission on a deploy preview; F15 analytics decision; ws3 content pass.

## Key Files Reference

| File | Purpose |
|------|---------|
| `docs/refinements-master-plan.md` | THE status board — all wave items + PR refs |
| `src/lib/motion.js` | House motion authority: pulse curve, 500/800 constants, `LENIS_TUNING` (empty, dial-ready) |
| `src/lib/smoothScroll.js` | Lenis wrapper + A2b `?lerp`/`?wheelmult`/`?lenisdur` dial |
| `src/components/work/detail/MediaSlot.jsx` | FP-2: `DETAIL_HLS_CONFIG`, `IO_MARGIN`, `eager` prop |
| `src/components/work/detail/NextProjectBand.jsx` | FP-2 band config + DP-1 rework target |
| `src/components/SiteNav.jsx` | NAV-2 target (no scroll listener; chrome via body classes) |
| `src/components/work/WorldCard.jsx` + `FeaturedProjects.jsx` | FP-1 enter_world target |
| `src/lib/momentum.js` | DORMANT — zero importers; future orbit scroll-kick |
| `docs/process-page-plan.md` | P0–P4.7 ✅, P5/P6 ☐ |

## Decisions & Context

- **A2b split:** agent side (dial + regression) DONE; the feel values are
  Nathan's CREATIVE call — `LENIS_TUNING` stays `{}` until he blesses a tune.
- **FP-2 weight:** initial-8s +5.2MB is the pre-load working as designed;
  mobile dial (narrower band / lower startLevel on small viewports) is a P5
  candidate, not a blocker.
- **startLevel is ladder-relative** (not "720p") — comments corrected.
- **Issue-close convention:** PRs target `feature/v1-launch`, so "Closes #N"
  does NOT auto-close — close issues manually with a shipped-evidence comment.
- **Local verify recipe:** scratch playwright-core at `/tmp/pw-verify` +
  chromium_headless_shell-1228 (`chrome-headless-shell-mac-arm64/…`), args
  `['--use-angle=metal']`, `waitUntil:'load'` never `networkidle`; system
  Chrome (`/Applications/Google Chrome.app/…`) for video/codec probes with
  `--autoplay-policy=no-user-gesture-required`. Worktree builds need `npm ci`.
  FP-2 probe suite (rVFC + playlist-chain playbackId resolution + weight
  split) preserved at scratchpad `fp2/` (probe.mjs — note nodeNet entries use
  `tPage` for the page clock).
- **Agent worktree convention:** built in `.claude/worktrees/`, committed (not
  pushed by agents), reviewed/shipped by the main session; ports 4343/4353/4363
  for probe previews, :4321 is Nathan's dev server — never touch it.
- **/process only exists on this branch** (v1-launch has no process.astro);
  anything needing /process regression runs on an integration merge.

## Blockers

- None hard. P5 needs Nathan's iPhone. Prod /process links 302 home until P6
  (deliberate). Production deploys must come from a ref including PR #34.
