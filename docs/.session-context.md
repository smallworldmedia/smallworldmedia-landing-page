# Session Context — 2026-08-31 (MOBILE PASS shipped across 7 commits, all PUSHED; next = FP pager rework, 3 approaches)

> Auto-generated by `/save-session`. Read by `/load-session`.

## Last Session Summary

The mobile refinement pass ran end-to-end over 08-30→31 (Nathan remote,
reviewing on the branch preview): the homepage went TAP-ONLY with a
centered globe on every breakpoint, the chrome consolidated onto one chip
family (breadcrumb / next-project ticker / stepper set / minimal status
bar / loader capsule), the tagline restructured (desktop capsule, mobile
three-line stack, menu-resident off-home), and the home→/work passage now
rides the SAME enter-tune choreography as FP→detail — letter exits
included. Every round was probe-verified (build + Playwright at 1440/390)
before pushing. Next session opens on the FP pager rework (3 creative
approaches — memory: fp-pager-rework).

## Completed Work (This Session)

- **Tap-only enter_world + centered comp** (`8d39f41`, `30588a0`): scroll
  accumulator/carets/hero lockup retired; NAV carries the brand on home
  (white fills; ≤768px bar-centered with `--bar-pill-reserve` guard +
  `--lockup-svg-h: 5rem`); globe dead-center both breakpoints (mobile
  −0.18 bias retired); desktop comp = device constants (null fill/fit,
  elev 8). Old comp via ?herofill=1.22&heroy=1&heroelev=56.5.
- **Home→/work passage = the FP→detail gesture** (`30588a0`, `705f4b1`):
  onEnterClick rides ENTER_TUNABLES (?enterms cover 1100ms, open-ended
  moveEnd-2 window, pow 7) — rig.zoom dives under the rising RouteFill;
  tagline LETTERS hard-cut in random order (swm:tagline-exit →
  SiteTagline, restores on astro:after-swap). Chunk-4 envelopment retired
  from the click path (?herotune dry-run still rehearses it).
- **Tagline architecture** (`a937a83`, `30588a0`): desktop = the black
  capsule pill site-wide (untouched, Frame 19); mobile = UNCAPSULED
  three-line stack (VISUAL WORLDS / for the music / industry.) at
  `--text-blurb` (shared with the detail blurb), home page + menu foot
  only (off-home page-level hidden — real estate); menu foot = privacy
  (item ink) above tagline at `--menu-pill-seat` (= `--footer-bottom-inset`).
- **Detail page chrome** (`d0935fa`, `cde8864`): next_project chip inline
  with the breadcrumb (right-anchored, NOT sticky, max-w = breadcrumb via
  `--detail-next-max: 13.08rem`, whole-label marquee ticker w/ edge-bled
  window); breadcrumb glyph = drawn SVG (↩ rendered emoji on iOS);
  DetailProgressBar replaced the ProcessMeter promotion.
- **Status bar family** (`cde8864`, `705f4b1`): `.detail-progress` minimal
  capsule (accent fill / darker tint, appears on scroll, wipes L→R at
  content end on --ease-draw) promoted to global.css; /process adopted it
  via `measure="document"` + it re-fires swm:process-index for the
  steppers (osboot ░▒▓█ ProcessMeter retired); blue-phase field override.
  RouteFill loader matched in WHITE (capsule, width-driven bar, mono pct
  left / chip-voice label right).
- **Stepper set = the primary prev/next** (`705f4b1`, `a3c32c7`,
  `c5915b8`): /work adopted the /process steppers (small sans, lowercase),
  VERTICAL stack (arrows above prev / below next), carets clip exactly at
  chip bounds, equal widths via `--stepnav-min-w: 5.25rem` (min — the
  back_to_top swap still fits), shared `--stepnav-edge-inset: 2.857rem`
  seat; /work chips STAY CENTERED (Nathan's call), charge/--tx-scale
  wiring intact.
- **Pill open/close states** (`705f4b1`, `c5915b8`): OPEN = white fill +
  accent ink (aria-expanded + the close pills incl. route-home variant);
  every pill :hover behind @media (hover: hover) — the stuck-tap hover
  that held pills white on touch is dead. Info drawer's redundant top-left
  close row REMOVED (`a3c32c7`) — the info pill is the one affordance.
- **Bug fixes**: textExit SplitText 'chars' → 'words,chars' (chars-only
  drops spaces → HHS collapsed to one line mid-exit; probe 241.9==241.9);
  SiteShell closed-drawer −(H+1) → −Math.ceil(H) (nav sat at −1px; menu
  lockup parity now sub-device-pixel); Astro CSS-order flake (fresh vs
  soft-nav /work loads order global/page css differently — overrides must
  win by SPECIFICITY, class-doubled selectors).
- **Tunables**: ?bluecascade=random (panelDelay random draw, zero
  per-frame cost); mobile pager active label hidden at rest (interim).
- **Info drawer accents**: description strong + clients scrollbar thumb on
  var(--project-color); loader label chip voice.

## Current Branch & Git State

- Branch: `feature/v1-launch` @ `c5915b8`, PUSHED (origin in sync) —
  preview auto-rebuilding.
- This session's commits: c5915b8 · a3c32c7 · 705f4b1 · 30588a0 · cde8864
  · d0935fa · a937a83 · 8d39f41 (on top of 7f59804).
- Uncommitted: none + 5 personal untracked strays (fable-mode skill, 2
  root logo SVGs, 2 reference PNGs).

## Pending Tasks (Next Session)

1. **FP PAGER REWORK** (memory: fp-pager-rework) — Nathan wants THREE
   distinct creative approaches: press-hold-slide-release wheel on touch
   (hover+scroll on desktop), minimal out-of-frame rest showing just the
   project number, ticker-chip idiom candidate. CONFIRMED: no client name
   at rest (redundant). Current interim: mobile hides the active label.
2. **At-machine queue** (memory: mobile-pass-followups): Chrome fp-card
   centering bug (UNREPRODUCED headless — needs live DevTools; do not
   blind-fix) · lens-distortion carry-over for the home commit (assessed:
   lazy composer = zero rest cost, ~1–2ms/frame mobile during the 1.1s
   passage; surgery in useGlobeScene) · loading-bar design iteration ·
   mobile lockup scale tunable · scroll-trigger sensitivity field pass
   (memory: mobile-scroll-trigger-field-pass; ?scroll per surface).
3. **Launch runway** (docs/v1-launch-plan.md): F15 analytics decision ·
   F9 real form submission test · then merge to `main` = production.
4. Real-device eyeballs still open from 08-29: /work album-card dark
   ground · ROSSI next-project thumbnail. Possible follow-ups Nathan may
   call: desktop tagline pill off-home (real estate), ✳ client-chip glyph
   emoji risk on iOS (same class as the fixed ↩), delete the retired
   chunk-4 envelopment machinery once the new passage is blessed.

## Key Files Reference

| File | Purpose |
|------|---------|
| `src/components/Hero.jsx` | Tap-only enter_world; onEnterClick = enter-tune cover + rig dive + swm:tagline-exit |
| `src/components/work/world/enterTune.js` | THE shared passage vocabulary (home + FP→detail) |
| `src/components/work/textExit.js` | Letter-exit ('words,chars' — the wrap-geometry fix) |
| `src/components/SiteTagline.jsx` | TAGLINE_LINES/EM_LINE export; letter-exit listener + after-swap restore |
| `src/components/work/detail/DetailProgressBar.jsx` | Shared status bar (flow/document measure, optional swm:process-index broadcast) |
| `src/components/RouteFill.jsx` | Loader capsule: width-driven bar + mono pct proxy |
| `src/styles/global.css` | Mobile :root tier (~L2470): --text-link 14/18, --lockup-svg-h 5rem, --bar-pill-reserve, --text-blurb; tokens --stepnav-edge-inset/--stepnav-min-w (~L232); pill open-state group + hover:hover (~L700); .detail-progress (~L1000); menu foot/tagline |
| `src/styles/featured-projects.css` | .fp-cta.fp-prev/.fp-next stepper adoption (class-doubled — CSS-order flake note); mobile pager rest override |
| `src/styles/process.css` | Stepper column skin; .detail-progress blue-field override |
| `src/styles/project-detail.css` | .detail-next ticker chip (+ --detail-next-max), SVG breadcrumb glyph |

## Decisions & Context

- **One passage vocabulary**: home→/work and /work→detail share
  ENTER_TUNABLES + powInOut/seg windows; letter exits share charCutMs.
  Never let them drift.
- **Chip family**: stepper (prev/next) + breadcrumb + next-ticker + status
  capsule + loader are ONE type voice (font-ui 1rem / mono for numbers).
  These are the components to refine going forward.
- **Astro CSS-order trap (NEW, bit us 08-31)**: stylesheet order between
  global.css and page css DIFFERS between a fresh load and a client-nav
  arrival — page overrides of global base rules must win by SPECIFICITY
  (class-doubling), never by order. Probe BOTH entry paths.
- **SplitText trap (NEW)**: type:'chars' alone drops inter-word spaces —
  wrapped text loses its break points and collapses. Always 'words,chars'.
- **Touch hover trap**: :hover sticks after taps — every stateful chrome
  hover lives behind @media (hover: hover); state is attribute-driven
  (aria-expanded).
- **Mobile-token doctrine**: breakpoint overrides live in the ≤768px :root
  block under the same token names; page-tuned constants become local
  custom props; measured constants get baked as documented tokens
  (--detail-next-max, --stepnav-min-w).
- **Verification pattern**: build + Playwright probe vs `astro preview
  --port 4330`; screenshots EYEBALLED (the probe passed while a fresh-load
  screenshot caught the CSS-order flake); overlay-scrollbar/headless
  can't see everything — Nathan's real-device reports are ground truth.
- **Deploy law unchanged**: production = merge to `main`; branch pushes
  rebuild the stable preview.

## Blockers

None. Everything pushed and probe-verified. The Chrome fp-card centering
report is parked (unreproducible headless) for the at-machine session.
