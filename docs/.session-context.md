# Session Context — 2026-07-20 (v1-launch refinements committed + PUSHED; parallel sessions closed, linear from here)

> Auto-generated by `/save-session`. Read by `/load-session`.

## Last Session Summary

Cleared the two queued ⚡ items (globe stroke-fit + scrollbar-stable nav), then
ran a second batch of Notion Revision-Notes-02 refinements across the home hero
and /process. Everything landed in ONE commit (`380a9a7`) which was
**fast-forward pushed to `origin/feature/v1-launch`** (now `380a9a7`, 66 commits
past the old remote tip `7153049`). Parallel-session work is DONE — refinements
are linear from here.

## Completed Work (This Session)

All in commit `380a9a7`; verified live via a scratch `astro dev` on :4408 + the
`/tmp/pw-verify` playwright harness.

**Homepage hero (Hero.jsx, HeroIntro.jsx, heroConfig.js, global.css):**
- **⚡#1 globe stroke-fit** — `GLOBE_STROKE_FRAC` moved to `heroConfig.js` as ONE
  shared source; Hero sizes the stroke disc `1+FRAC` proud, HeroIntro's
  `glyphPoseFor` frames the globe `1/(1+FRAC)` SMALLER so globe+stroke = the
  lockup "o" (verified ring/glyph ratio **0.9991**, was ~1.05).
- **Lockstep lockup exit** — `buildCharScrub` splits the wordmark at the globe
  into two RIGID groups (one shared x per side) + a **uniform group zoom** about
  the globe centre (own-centre scale + computed x/y so it's a coherent zoom, not
  per-letter drift). `.hero-intro__art { overflow: visible }` fixes letters being
  clipped at the narrow SVG box before the viewport edge. Verified: rigid groups
  reach the viewport edges, coherent zoom.
- **Tagline + scroll_to_enter column** — now **centre-justified in the gap LEFT
  of the globe** (Hero writes `--lead-gap` = globe left-edge px; the column spans
  `[0, gap]` and centres content). z-index 2→**3** (above globe z:2). Arrows
  centred below the button, bigger caret (30×16, home-only), shorter conveyor
  (52→40px), `gap: 1.714rem` between lead and CTA.
- **🐛 Fixed a pre-existing orphan `*/`** at `global.css:~1622` (after the F1
  nav-slide rule) that made the parser swallow the `.hero__lead-col` selector and
  **drop the whole rule** — the column had been falling back to hero
  flex-centering (this WAS the "drift to centre" Nathan reported). Still live on
  `main`; only fixed on this branch.

**Nav (SiteShell.jsx, global.css):**
- **⚡#2 scrollbar-stable nav** — `--scrollbar-w` is now a DEVICE-CONSTANT probe
  (off-screen `overflow:scroll` div, measured on mount + resize) instead of
  `innerWidth − clientWidth`. Route-independent → the nav/footer right inset (and
  the right-edge items) no longer jump between scrollable and locked routes.
  `scrollbar-gutter` stays OFF (it painted a blue band under the overflow:hidden
  lock). Verified identical geometry across routes.

**Process page (/process — ProcessPage.jsx, useProcessScene.js, useProcessCopy.js,
process.astro, process.css):**
- **Blue-field start** — reverted the white blank-canvas opening: `data-bg="blue"`,
  page bg electric-blue, S1/S2 poses `bg:'blue'`, white copy.
- **Phase 3 → brand BLACK** — S3/S4 poses `bg:'black'`; the redundant blue flood
  layer (`.process-bg__blue` / `blueEl`) was REPURPOSED to the black solidify
  field (grows from the Core at S3). Copy stays white on black. The blue core now
  pops on black (was blue-on-blue). Verified at phase 3.
- **PROCESS "O" = the finalized homepage globe** — `useProcessCopy` mounts a live
  `VideoGlobe` (same Sanity pool, fetched in `process.astro`) into the O, in its
  **opening / held line-art state** (`holdEntrance:true`, never released → the
  lockup-matching gap-lattice mark, NOT the video-panel globe). Height-fit to the
  cap height; mounted ONLY while the hero splash is on-screen (mount/unmount on
  the hero ScrollTrigger + splash-end) so it never leaves a 2nd WebGL globe
  rendering off-screen.
- Audited ALL 7 Rev-Notes-02 process items (14-agent workflow) — **all done**
  (blob-tracking port, white/blue skin, tagline caps/one-line, stage→phase,
  prev/next visibility, phase copy, back-to-top).

## Current Branch & Git State

- **Branch: `feature/v1-launch` @ `380a9a7`** — PUSHED. `origin/feature/v1-launch`
  = `380a9a7` (fast-forward, no force needed).
- Last commits: `380a9a7` (this batch) → `528542d` (prev session save) →
  `80f79b6` (the fold) → `c48c52e` …
- **Working tree clean** of tracked changes. Untracked strays only (not mine, not
  committed): `SWM-globe_white.svg`, `Small World Media Logo Inline Blue-01.svg`,
  `breadcrumb_move.png`, `homepage_refine_01.png`, `.claude/skills/fable-mode-main/`.

## Pending Tasks (Next Session) — priority order

1. **⚠️ Restart the `:4321` dev server** before reviewing /process — it holds a
   Vite 504 "Outdated Optimize Dep" (stale `react-dom/client` optimize) that
   leaves the client:only island blank/white. `rm -rf node_modules/.vite` +
   restart clears it. NOT a code bug. (Home changes HMR onto :4321 fine.)
2. **Nathan's aesthetic dials** (all live-tunable / one-value tweaks): arrow
   caret size + conveyor height, the lead↔CTA `gap`, the globe-O ink/contrast on
   the blue then black field, `?globestroke` %. Present for a look-pass.
3. **HR-6 — mobile/device gate**: the new centred CTA column has only a SAFE
   full-width mobile fallback (`.hero__lead-col` mobile override) — HR-6 refines
   the mobile composition (globe up-biased, small left gap). iPhone perf pass too
   (now TWO WebGL contexts on /process during the splash).
4. **The v1 PR** to `main` — branch is pushed now; open when the refinement pass
   settles. NB: cherry-pick / carry the orphan-`*/` fix (it's a real bug live on
   `main`).
5. **Rev Notes 03 [NEXT SESSION]** on Notion (`SWM Website` → Revision Notes 03) —
   the next refinement backlog.

## Key Files Reference

| File | Purpose |
|------|---------|
| `src/components/hero/heroConfig.js` | `GLOBE_STROKE_FRAC` (shared, `?globestroke`); the single source both Hero + HeroIntro read. |
| `src/components/hero/HeroIntro.jsx` | `glyphPoseFor` (stroke-compensated framing); `buildCharScrub` (rigid-group lockstep + uniform group zoom); `A_CHARS_SCALE`, `O_DESKTOP/O_MOBILE`. |
| `src/components/Hero.jsx` | Stroke disc effect; writes `--lead-gap` (globe left-edge px) for the centred CTA column. |
| `src/components/SiteShell.jsx` | `--scrollbar-w` DEVICE-CONSTANT probe (⚡#2). |
| `src/styles/global.css` | `.hero__lead-col` (centred-in-gap, z:3), `.hero__enter-wrap/.hero__enter` (centred, transform-origin centre), hero arrows size/height, `.hero-intro__art` overflow, the fixed orphan `*/` at ~1622, nav scrollbar-inset comments. |
| `src/components/process/useProcessScene.js` | Stage poses (S1/2 blue, **S3/4 black**, S5 gradient); `blueEl` repurposed to the black solidify field (bgInstant/bgMorph/bgCrossfade key on 'black'). |
| `src/components/process/useProcessCopy.js` | The B9 splash; mounts `VideoGlobe holdEntrance` into the PROCESS "O" (mount-when-visible lifecycle); `placeGlobe` height-fit. |
| `src/pages/process.astro` | Fetches `globeAssets` (after the DEV gate) → ProcessPage. Still dev-gated (`import.meta.env.DEV`). |
| `src/styles/process.css` | Blue base bg, `[data-bg='black']` white-copy rule, `.process-bg__blue` = black. |
| `src/assets/swm-lockup-inline.svg` | The lockup; `#swm-lockup-glyph` = the "o" reference. |

## Decisions & Context

- **`GLOBE_STROKE_FRAC` is ONE source** (heroConfig) so the stroke disc and the
  intro glyph framing can never drift; the base globe scale is derived (down) from
  the stroke fraction, not tuned separately.
- **Lockstep letters**: two RIGID side-groups + a uniform group zoom about the
  globe centre. Per-letter own-box scale was REMOVED then re-added AS the group
  zoom (own-centre scale + computed x) because own-box scale drifted the letters.
- **Process bg model now**: base BLUE (S1/S2), the `blueEl` flood layer repurposed
  to BLACK for the S3 solidify (grows from the Core), gradient at S5. White copy
  throughout (`[data-bg='blue']` + `[data-bg='black']` rules). Kept the `__blue`
  class/ref name for minimal churn — it renders black (commented).
- **Globe-O = opening state, not resting**: `holdEntrance` (never released) keeps
  the cascade/live-video deferred → the line-art lockup mark. Matches the homepage
  opening by construction (both holdEntrance + default ink).
- **Verify recipe:** scratch `npm run dev -- --port 4408` (dev mode → /process
  ungated + un-504'd); playwright-core + chromium-headless-shell in
  `/tmp/pw-verify` (ANGLE metal + autoplay flags). :4321 = Nathan's dev (home
  globe TEXTURED; /process 504'd until restart). Kill scratch servers after +
  `git checkout -- .astro` (the scratch server dirties `.astro/` cache).

## Blockers

- None code-side. The only friction is the `:4321` Vite 504 (restart clears it —
  an env/cache issue, not the process code). Two WebGL contexts on /process during
  the splash is a mobile-perf item for HR-6, not a blocker.
