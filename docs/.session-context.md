# Session Context — 2026-07-15 (back-at-desk integration + refinement round 2)

> Auto-generated by `/save-session`. Read by `/load-session`.

## Last Session Summary

Nathan's first at-desk session after traveling. All four traveling sessions were
identified and woven together: **PR #13** (brand-polish audit P0 tranche) and
**PR #14** (process docs) were merged into `feature/v1-launch`, which was then
merged into this branch — so the process build now sits on top of the audit
fixes (woff2 fonts, token fixes, video cap, dead-code removal). Localhost tuning
resumed: Nathan's second `copy_url` dial-in was baked as `TUNING_DEFAULTS`, and
his first refinement-notes round shipped — nav re-add, the Y-jitter diagnosis +
fix, poster-scale typography, [previous]/[next] steppers, and the walkthrough
block-meter. Everything browser-verified locally; all pushed.

## Completed Work (This Session)

- **Branch integration (the "weave"):** merged PR #13 (`751698c`) and PR #14
  (`11c46ab`) into `feature/v1-launch` via a temp worktree (both PRs now show
  MERGED; the sandbox PR-watch trigger's until-merge condition is satisfied),
  then merged `feature/v1-launch` into this branch (`cf00884`). Conflicts were
  the predicted ones: rolling session-save (kept newest), two comment-only CSS
  hunks, and the deliberate P0-token duplication — kept PR #13's canonical
  `--tracking-tight`/`--lh-body`, dropped the process-branch copy.
- **`698f5e9`** — baked Nathan's 2026-07-15 dial-in as defaults (all 18 values
  verbatim: scatter 1.8, drift 0.4, idlepower 0.74, stroke 2, fillfrac 0.99,
  s3fill 0.35, stagems 650, zoomout 0.35, threadhops 84 = every bead,
  threadms 100, assemble 1.9, emanate 1.3 on poles, bpm 141, pulsemin 0.08,
  hold 0.1, decay 2, scroll 700). `?drift` slider widened to 0.8 (was pinned).
  Verified: clean `?debug` serializes to a bare `/process?debug`.
- **`0084013`** — refinement round (Nathan's five notes):
  1. *Nav:* `⊙ process` re-added (desktop + mobile menu). Gate still stands.
  2. *Y-jitter fixed* (two stacked causes): the copy-column peek `quickTo` and
     the release/commit `gsap.to`s could overlap on `y` (alternating writes at
     commit), and wheel momentum tails re-leaned the column after the glide
     landed. Now: ONE owning tween (`leanTo` kills the incumbent), `force3D`
     through the interaction, and a 100 ms arming gap (np-band idiom) that
     swallows inertia tails until a quiet beat or direction flip. Probe:
     translateY dead-still (span 0 px, 0 flips) through burst + tail.
  3. *Type scale:* display register to poster scale — hero `clamp(4rem,10.5vw,12rem)`,
     stage headlines `clamp(3rem,6.2vw,6.75rem)` escaping the prose column
     (max-width 58vw), CTA `clamp(3.6rem,8.5vw,10rem)`, leading 0.85. Prose
     register = `.project-blurb__text` verbatim (detail-page blurb voice),
     incl. its ≤768 cut; measure `min(26ch, 44vw)`.
  4. *`ProcessStepCtas.jsx`:* [previous]/[next] — the /work CTA chip family
     (`CtaArrows` carets re-cut 34×20) small/subtle/left-aligned, fixed
     bottom-left; dispatches `swm:process-step` → driver's quantizer commit
     (glide lock respected), RM = instant centered `scrollIntoView`; hover
     fills electric blue (black on the blue field); slides right of the
     `?debug` panel (`:has()` — the panel covered that corner); hidden ≤768.
  5. *`ProcessMeter.jsx`:* the carousel osboot boot-log block meter promoted
     to site chrome — ten ░▒▓█ cells (fractional cell steps ░→▒→▓), dimmed
     `NN / 07` counter, `the_process` label; Lenis + native scroll driven
     (rAF-throttled); data-bg-aware (white cells on blue field, electric blue
     on black); fixed bottom-right (bottom-left on phones).
- **Plan updated:** `docs/process-page-plan.md` gains the P4.6 row; P6 slims to
  the pure un-gate (nav re-add pulled forward — the live link 302s home in
  prod until P6 ships).

## Production Scorecard

> Carried from the 2026-07-13 save, NOT re-fetched (no CMS work this session):
> Clients 59 · Service Tags 15 · Media Assets 776 · Featured 16 · Manifests 82.

## Current Branch & Git State

- Branch: **`claude/load-session-wippng`** — fully pushed. **The authoritative
  tip is `origin/claude/load-session-wippng`** (the final commit is always the
  `docs(session)` save itself).
- Session commits: `751698c`/`11c46ab` (PR merges, on `feature/v1-launch`) →
  `cf00884` (merge into this branch) → `698f5e9` (bake) → `0084013`
  (refinement round) → this save.
- `feature/v1-launch` now at `11c46ab` (= `0cb7823` + audit P0 + process docs),
  pushed. PRs #13 and #14 are **MERGED**; issues #1–#12 remain open (the
  post-launch roadmap).
- Uncommitted: none. Untracked (standing, Nathan's): `SWM-globe_white.svg`
  (loose root copy), `breadcrumb_move.png`, `.claude/skills/fable-mode-main/`.
- `feature/v1-launch` stash: one entry ("feature/v1-launch WIP at branch
  switch") holding the superseded 07-05 session save + generated astro
  settings — safe to drop when noticed.

## Pending Tasks (Next Session)

1. **Refinement round 3 — Nathan's declared focus:** animation timing, section
   & user flow, further interaction/timing improvements — make the page feel
   "more dynamic and effortless, and natural to experience." Likely levers:
   stage-transition choreography overlap (entrances vs. scene beats), swipe
   glide feel (`?swipems`/curve), copy entrance pacing vs. the 650 ms stage
   base, boundary geometry, steppers/meter voice + placement, hero arrival.
2. Meter + steppers polish (position/composition were round-one defaults —
   expect notes), then consider the meter for wider site reuse.
3. **P5 device pass** — now unblocked (Nathan is home): live scene on iPhone ≥
   the home globe's 42–44 fps baseline, RM on-device, mobile belt fit.
4. **P6 un-gate** — one 3-file commit + SEO verify + spec §11 acceptance run
   (remember: nav already live → prod link 302s home until this ships).
5. v1 launch residuals (unchanged): ws6 device gate → PR `feature/v1-launch`
   → `main`; F9 inquiry submission on a deploy preview; F15 analytics decision
   at deploy; ws3 content pass.
6. Parked: carousel branch `claude/max-effort-nbx20u` (Nathan: separate,
   non-launch workstream). ⚠️ Its session's `/save-session` never got pushed —
   `social/world-build/README.md` + commit `88bc2d8` are the record.

## Key Files Reference

| File | Purpose |
|------|---------|
| `src/components/process/useProcessScrollDriver.js` | Quantizer + boundaries + the jitter fix (leanTo single-owner, arming) + `swm:process-step` |
| `src/components/process/processConfig.js` | Baked 07-15 defaults — the knob table |
| `src/components/process/ProcessStepCtas.jsx` | [previous]/[next] steppers (new) |
| `src/components/process/ProcessMeter.jsx` | ░▒▓█ walkthrough status meter (new) |
| `src/styles/process.css` | Poster-scale type + stepnav/meter chrome + data-bg skins |
| `src/components/process/useProcessScene.js` | Stage machine + staged background + rhythm engine |
| `src/components/process/useProcessCopy.js` | OS-boot copy entrances (round-3 timing target) |
| `docs/process-page-plan.md` | Status board — P0–P4.6 ✅, P5/P6 ☐ |
| `docs/process-page-spec.md` | The build contract (amended 07-13) |

## Decisions & Context

- **Copy-column y has ONE owner** (`leanTo` in the driver): any future tween on
  `.process-page__copy` transform must go through it or kill it first — the
  07-15 jitter was exactly two tweens sharing the property. Momentum tails are
  swallowed by a 100 ms arming gap after every commit (re-arm on quiet gap or
  direction flip).
- **Type registers:** display = poster scale/0.85 leading and may escape the
  prose column; prose = the detail-page blurb voice verbatim (`.project-blurb__text`).
  If detail-page type changes later, /process inherits the intent, not the file
  — the values are copied, not var-shared.
- **ProcessMeter is deliberately self-contained** (own scroll subscription, no
  scene coupling) so it can be lifted site-wide later — Nathan flagged exactly
  that.
- **`?debug` panel owns the lower-left**: any new fixed chrome must either
  avoid that corner or take the `:has(.process-debug)` shove like the steppers.
- **Local verification recipe (this machine):** no Playwright in the repo —
  scratch `playwright-core` at `/tmp/pw-verify` + the ms-playwright cached
  `chromium_headless_shell-1228` binary via `executablePath`. `networkidle`
  NEVER fires on `/` (HLS streams) — use `waitUntil: 'load'`. Detail slugs come
  from `curl /work | grep href="/work/…"`. Worth capturing as a project run
  skill if it keeps proving useful.
- Dev server: `npm run dev` (background task this session) on :4321 — the
  previous instance had been running 10 days; restart after big branch moves.
- Merge convention established: PR-merge into `feature/v1-launch` locally via
  worktree (`--no-ff`), push → GitHub marks the PR merged; rolling session-save
  conflicts resolve to the newest side.

## Blockers

- None hard. P5 needs Nathan's iPhone (available now). The prod nav link 302s
  home until P6 un-gates — deliberate, but don't merge this branch to a
  deploying branch without either P6 or reverting the nav commit's link hunk.
