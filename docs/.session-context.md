# Session Context — 2026-09-07 (evening): title token + fill ticker shipped, services readout dropped, SSR dial bug + two ticker phase bugs fixed — all PUSHED @ 11be6d4

> Auto-generated by `/save-session`. Read by `/load-session`.

## Last Session Summary

Nathan's desktop round on the SSR'd graticule (`/work`, the scale arm).
Shipped the Sanity `title` as a secondary token after the client name and
a fixed-width desktop flipper box whose selected name always rolls
(the "fill ticker"). A services readout (two-line box) was built on
request, then dropped the same day. Along the way three real bugs fell
out: every `?` dial had been dead since the 09-06 SSR (hydration kept the
server's fallback style/class), the fill roll re-phased on every
setStation (the "COCO re-evaluation"), and the wheel's re-base swapped in
an unfilled clone row (COCO scrolled up → single name in the box).
Nathan confirmed the last fix: "that fixed it!". Nine commits
`b57c853` → `11be6d4`, all pushed; branch level with origin.

## Completed Work (This Session)

- **Title token** `.fp-scale__sub` (`b57c853`): Sanity `title`
  ("(Pre-2026)" on COCO #08 / Heavy House Society #09, null elsewhere)
  rides after the client name in every row — body family, sentence case,
  weight 400, `--scale-sub` .62em, `--scale-sub-ink` **1** (undimmed per
  Nathan). Inside the name copy so cap/ticker/box include it; suppressed
  when title == client name; aria-label + live region carry it.
  `?sub ?subink`.
- **SSR dial fix** (`d9bcd6a`): the server renders each inline-token
  dial at its fallback (PARAM has no window) and React hydration KEEPS
  the server's style/class — so `?roster ?selscale ?falloff* ?ink*
  ?slice* ?namemax ?sub …` were all dead on a hard load since `a6553b9`.
  `rootStyle` / `rootClass` are now re-applied imperatively in the FIRST
  effect (gear() reads tokens off computed style). With a dial in the
  URL React logs one "attributes didn't match … won't be patched up"
  notice — harmless; a plain `/work` logs nothing.
- **Fixed-width desktop box + FILL ticker** (`d6e2d44`, `afa3813`,
  `0e32bd8`): box width = selected `offsetLeft` + widest name (+ sub),
  capped so `--scale-box-gap` stays before the `[select_project]` chip;
  the selected name ALWAYS rolls, copies cloned into the track to fill
  (hidden off `data-fill`), keyframe `fp-scale-fill` shifts one copy +
  gap (`--fill-shift` px) per `--fill-s` = shift ÷ `--scale-fill-pxs`
  (constant px/s, wall-clock phase-locked). No trailing pad — clip edge
  = box edge (measured: box / name clip end on the same pixel).
  Roster and ≤768 rows keep the two-copy `data-marquee`.
- **Nathan's bake** (his dial string): `--scale-fill-pxs` **30**,
  `--scale-box-gap` **12px**, desktop `--scale-name-max` **2400px**
  (uncapped — the box is the selected clip), `?unfill` default **0**.
- **Ticker phase bug 1** (`0e32bd8`): `fill()` rewrote `animation-delay`
  on every setStation (Turn end / follow re-run it on the same row); a
  new delay on a node already rolling JUMPS its phase (currentTime =
  elapsed − delay). Now written only when the roll ARMS. Verified: phase
  tracks the wall clock within ms across re-select / leave / re-engage.
- **Ticker phase bug 2** (`11be6d4`): scrolling UP from #01 crosses the
  wrap seam — the engine re-bases the strip the instant q < 0, swapping
  the node under the lens for COCO's CLONE (no data-fill/data-sel) →
  a single COCO in the box until the next detent. `onRenorm` now re-runs
  `setMarquee(cRef.current)`; the wall-clock phase makes the hand-off
  seamless. Probe: tick 1 re-bases k 0 → 13 with fill + sel on the
  clone, exactly one row rolling through 13 → 12. **Nathan: fixed.**
- **`?unfill`** (outgoing-row roll hold, ms): a wrong-cause fix for the
  COCO snap — 3τ and 1τ both "hung in the roster". Default 0, dial kept.
- **Services READOUT — built then DROPPED** (`d6e2d44` → removed in
  `0e32bd8`): option 3 of three proposed (in-box trailing line ·
  vertical tag column beside the rotated mobile chip · two-line box). A
  mono line of the project's `services` under the name, box grown by
  `--scale-tags-h`, rows below the detent shifted, slice extended,
  hitTest compensated, `?tags ?tagsh ?tagsize`. Nathan: "not moving
  forward with the service tags on the scale" — fully removed; the
  card's tag row stays the tag surface.
- **Chip wipe** desktop 520 → **300ms** (≤768 holds 520).
- Docs: spec doc § "09-07 — the title token, the fixed-width fill
  ticker, the readout that was dropped"; `docs/tunables-guide.md` at
  **275 params**, `--check` PASS; `docs/v1-launch-plan.md` ws 6 row
  refreshed; memory `fp-pager-rework.md` updated.
- **Prettier trap (learned)**: `npx prettier` in this repo runs with NO
  config (double quotes, trailing-comma all) and the files are not
  prettier-clean at HEAD — never `--write`; one commit had to be rebuilt
  from exact string edits.

## Current Branch & Git State

- Branch: `feature/v1-launch` @ `11be6d4` — **level with origin (pushed)**.
- Last 5 commits: `11be6d4` wrap re-base fill · `0e32bd8` drop readout +
  bake + re-phase fix · `0bda577` ?unfill 1τ · `afa3813` flush clip +
  outgoing hold · `5809e0c` ?tagsize (since removed). Earlier today:
  `d9bcd6a` SSR dial fix · `d6e2d44` readout + fill ticker · `b57c853`
  title token.
- Uncommitted: only `.astro/*` (generated) + this file + the plan row
  (commit as `docs(session)` after). Personal untracked strays (leave
  alone): `Client Logos/`, 2 root logo SVGs, 2 reference PNGs,
  `.claude/skills/fable-mode-main/`, `scripts/shots/` (never commit).
- Netlify branch preview rebuilds from `11be6d4`; `/work` needs NO query.

## Pending Tasks (Next Session)

1. **Nathan's next read of the preview / device pass.** Live desktop
   dials now WORK on a hard load: `?fillpxs ?boxgap ?unfill ?namemax
   ?sub ?subink ?chipwipe ?roster ?selscale ?selbump ?falloff*
   ?ink* ?slice* ?magnet ?fliptau`. Dev: `npm run dev -- --host 0.0.0.0
   --port 4321`, hard-load `http://192.168.1.9:4321/work`.
2. **Mobile has no fill ticker** (two-copy marquee + evaluated cap,
   unchanged) — decide whether the fixed-box/fill idiom goes to ≤768.
3. **Open watch-items**: HHS (Pre-2026) selected box ≈ 600px overlaps the
   centred card at 1440 under the scrim (accepted); neighbours boxless
   over the sliced axis (r8 flag); `?wallrows` adds a cell on both wall
   kinds; engage-during-external-Turn freeze.
4. **Rail arm (parked)** — `?pager=rail` front end over the scale
   interaction; spec'd in the spec doc § 09-05. Revisit only if asked.
   **Legacy rail retirement** follows if the rail arm is dropped.
5. Standing queue: tunables-guide latent-bug tickets · at-machine items
   (memory: mobile-pass-followups) · scroll-trigger field pass · launch
   runway (F15 analytics, F9 form, merge to main).

## Key Files Reference

| File | Purpose |
|------|---------|
| `docs/fp-pager-rework-approaches.md` | THE spec: engine contract, rounds 1–11h, deletion, rail-arm proposal, § 09-07 (title token, fill ticker, SSR dial fix, dropped readout) |
| `src/components/work/pager/GraticulePager.jsx` | THE arm (SSR'd): `rootStyle`/`rootClass` + the mount effect that re-applies dials; `subOf`/`nameOf`; `setMarquee` (desktop fixed box + `fill`/`unfill`/`unfillLater`); `onRenorm` re-fill; every `?` dial |
| `src/components/work/pager/usePagerGesture.js` | shared engine — FSM/damper/flick/keyboard, magnet, wrap + renorm, commit-at-disengage |
| `src/styles/featured-projects.css` | `.fp-scale` rules: station curve, sliced axis, `.fp-scale__sub`, `fp-scale-fill` keyframe + `[data-fill]`, chip wipe |
| `src/styles/global.css` | `--scale-*` tokens both tiers — `--scale-sub/-sub-ink`, `--scale-fill-pxs` 30, `--scale-box-gap` 12px, `--scale-name-max` 2400px desktop, `--scale-chip-wipe-ms` 300 / 520 ≤768 |
| `src/lib/queries.js` · `src/pages/work/index.astro` | worlds carry `title` + `services` (project doc, else asset union) |
| `scripts/pager-probe.mjs` | Playwright harness (`--scenario=hover,wheel,touch`, `--extra="&dial=…"`); Playwright at `~/.npm/_npx/e41f203b7505f1fb/node_modules/playwright` |
| `docs/tunables-guide.md` | 275 params; `node scripts/tunables-keys.mjs --check` is the gate |

## Decisions & Context

- **Services tags do NOT live on the scale** (Nathan, 09-07). The card's
  tag row is the tag surface. Three readout options were proposed and
  the two-line box was tried; if the idea returns, the spec § 09-07 has
  the mechanics.
- **Desktop selected row = a fixed box + an always-rolling fill ticker**
  (Nathan's ask: box width constant client to client; short names repeat
  to fill; established ticker choreography). Roster + ≤768 unchanged.
- **Wall-clock phase doctrine, sharpened**: write a looping animation's
  phase ONLY when it arms — rewriting `animation-delay` on a running
  node jumps it. And any re-base that swaps the DOM node under a
  persistent visual (the wheel's renorm) must re-arm on the new node.
- **SSR + `?` dials**: an inline prop computed from `location` is never
  written by hydration — every such dial must be applied imperatively
  after mount. (Structural dials like `?pause=0` cause a client re-render
  and still work.)
- **Prettier**: no repo config; never `--write` here.
- **Deploy law**: production = merge to `main`; branch pushes rebuild the
  stable preview. Everything is pushed.

## Blockers

None. Waiting on Nathan's next read of the pushed preview.
