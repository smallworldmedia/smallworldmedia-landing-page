# Session Context — 2026-06-12

> Auto-generated by `/save-session`. Read by `/load-session`.

## Last Session Summary

Two major arcs. **(1) The CMS video globe shipped end-to-end**: prototype at `/lab/globe` → design rounds → iPhone perf gate passed (42–44fps) → **graduated to the home page hero** (Unicorn Studio removed) → wired to the Featured Project hierarchy (4-tier pool, editorial project ranking) → wordmark/CTAs removed from hero (SiteNav owns them). Two commits pushed (`f416d2c`, `d3339c9`). **(2) Featured Projects audit + tagging unification**: confirmed the 20 Mux-pending motion assets, unified three drifted deck mediaTypes into canonical `brand-deck` (80-doc migration), defined + applied the carousel convention (`carousel-slide`), re-homed HUS/Imperfect orphaned deck subfolders into their parent collections (41 docs + folder/file renames), and codified all conventions in CONTEXT.md + the manifest template.

## Completed Work (This Session)

### Video Globe (committed: f416d2c, d3339c9)
- Full prototype → production arc: tiered Mux video sphere (live HLS on prominent panels, thumbnails elsewhere, hidden-hemisphere cycling), pole wedges with planar UVs, power-on cascade (sweep default), drag+inertia, mobile cover-overscan + viewport-aware scheduler + single-rendition 540p manifests, hero graduation with `VideoGlobe.jsx` production component (`/lab/globe` kept as debug wrapper)
- 4-tier asset sourcing: `globeOrder` picks → hero reels by project `sortOrder` (uncapped) → featured-collection showcase → general backstop (`GLOBE_ASSETS_QUERY` + `buildAssetPool.js`)
- Fixed: hls.js `autoLevelEnabled` read-only throw; three.js VideoTexture double-gamma (manual `srgbToLinear`/`uVideoB` in panelMaterial.js)
- Hero cleanup: UnicornBg + unicornstudio-react deleted; wordmark/CTAs removed (sr-only h1 kept); blue→black gradient hero bg

### Featured Projects Audit + Unification (uncommitted, in working tree)
- **Mux gap confirmed**: exactly 20 motion assets without video — 15 Andhera Branding + 5 DEVELOPED (still pending as of save). Disk↔manifest↔Sanity reconcile perfectly; "extra" disk files are duplicate .mov exports + deck source PDFs
- **Deck unification**: `deck-page` (12) + `brand-deck-page` (59) → canonical **`brand-deck`** (now 112 total, zero invalid mediaTypes). DEVELOPED pages got `displayGroup: developed-pitch-deck` + `brandDeckOrder` 1–12
- **Carousel convention defined + applied**: new `carousel-slide` mediaType (schema enum), `buildContentFlow` holds carousels out of masonry (new `carousels` partition), Women's Day Carousel (9 slides) tagged `womens-day-carousel`
- **Orphaned decks re-homed**: HUS `Label Artwork 061126` → `Label Artwork Deck/` (14 pages, `label-artwork-deck`), Imperfect `Branding Phase 1 052026 Opt` → `Branding Phase 1 Deck/` (27 pages, `branding-phase-1-deck`); folders/files renamed to DEVELOPED-pattern, rows merged into parent manifests (titles preserved → ingest IDs stable, re-ingest upserts not duplicates), 41 Sanity docs patched to parent sourceFolder/sourceManifest
- **Docs codified**: CONTEXT.md gained Brand Deck Dual-Feed Rule, Carousel Convention, kebab-case `displayGroup` correction, carousel-slide hierarchy row; manifest template gained `carousel-slide` + optional `displayGroup`/`brandDeckOrder` columns with examples
- Verified: functional `buildContentFlow` partition tests pass for all touched collections, production build passes (22 pages)

## Production Scorecard

| Metric | Count |
|--------|-------|
| Clients | 59 |
| Service Tags | 15 |
| Media Assets | 784 |
| Mux Video Assets | 215 |
| Project Docs | 17 |
| Brand Deck Pages (unified) | 112 |
| Carousel Slides | 9 |
| Manifests | 82 |
| Motion assets pending Mux | 20 (15 Andhera Branding, 5 DEVELOPED) |
| Affiliations wired | 0 |

## Current Branch & Git State

- Branch: `Featured-Project-Detail-Page` (globe commits pushed)
- Last 5 commits:
  - `d3339c9` feat(globe): graduate video globe to home hero + Featured Project sourcing
  - `f416d2c` feat(lab): CMS video globe prototype at /lab/globe
  - `4ced6b7` chore(cms): re-ingest 10 collision-fixed manifests, delete 14 orphans
  - `a530980` fix(manifests): resolve 18 duplicate ID collisions across 10 manifests
  - `784dcd5` docs: prioritize ClientPanel refinement → directory filter variant
- Uncommitted changes: **Yes (~55 files)** — Nathan's featured-projects refactor (work/detail components, buildContentFlow, schemas, scripts, routes, styles) PLUS this session's audit fixes (CONTEXT.md, manifest template, mediaAsset carousel-slide enum, buildContentFlow carousels partition, 4 manifest edits, roadmap/session docs). These interleave — commit as the featured-projects refactor lands

## Pending Tasks (Next Session)

1. **Run the Mux pipeline for the 20 pending assets** (files + manifests ready):
   `node scripts/ingest.mjs "media/Andhera/Andhera Branding/_manifest.md" --dry-run` then live; same for `DEVELOPED Artist Workshop`. They auto-join detail pages + globe pool once playable
2. **Finish + commit the featured-projects refactor** (Nathan, in-flight) — now includes the audit unification edits
3. **Build the held-out components**: BrandDeckViewer (112 pages across 6 decks ready), Carousel (`womens-day-carousel` ×9 ready), AlbumArtOrbit — all data is partitioned and waiting in `buildContentFlow` returns (`brandDecks`, `carousels`, `albumArt`)
4. **Directory grid dual-feed**: deck pages + carousel slides currently surface as individual cards on /work (grid query only excludes album-art) — needs grouped directory components or query exclusion
5. **Re-sync stale Mux statuses** — many docs say "preparing" though streams serve 200; globe query accepts both as workaround, tighten after re-sync
6. **Masonry flush-tiling** (deferred by design): full-viewport flush bottom edge needs a JS row-packing pass with gentle scaling — CSS columns can't do it natively
7. **Mobile hero text legibility** (design) — taglines over bright video panels
8. **Globe curation**: set `globeOrder` picks; complete project `sortOrder` ranks past #8
9. **Phase 13: Affiliation Wiring** (CMS, not started)

## Active Phase

Frontend: **Featured Projects refactor** (in-flight) + held-out components next. Globe: DONE, live on home hero.
CMS Backend: Phase 13 (Affiliation Wiring) — not started

## Key Files Reference

| File | Purpose |
|------|---------|
| `src/components/globe/` | Production video globe (VideoGlobe.jsx, globeConfig.js tunables, buildAssetPool.js 4-tier pool) |
| `src/components/work/detail/buildContentFlow.js` | Partitions: showcase / albumArt / brandDecks / carousels / bts |
| `src/components/work/detail/MediaSlot.jsx` | Masonry slot — 3:4 portrait clamp via data-portrait |
| `src/lib/queries.js` | All GROQ incl. GLOBE_ASSETS_QUERY (4-key) |
| `src/lib/projectSlug.js` | toProjectSlug — asset↔project join everywhere |
| `src/schemas/mediaAsset.ts` | mediaType enum (brand-deck, carousel-slide), globeOrder, displayGroup |
| `src/schemas/project.ts` | Project docs (isFeatured, sortOrder = editorial rank) |
| `scripts/ingest.mjs` | Manifest → Sanity; docId = mediaAsset-{clientSlug}-{titleSlug} (title-keyed!) |
| `CONTEXT.md` | Content Population Hierarchy + ALL tagging conventions (deck, carousel, dual-feed, crop) |
| `docs/_manifest-template.md` | Manifest authoring (incl. optional displayGroup/brandDeckOrder columns) |
| `docs/cms-backend-roadmap.md` | Master roadmap (Phases 13–15 pending) |

## Environment

- Sanity Project: `b60h4u7o` / dataset: `production`
- Token: `.env.local` (`SANITY_WRITE_TOKEN`); Mux: `MUX_TOKEN_ID`/`MUX_TOKEN_SECRET`
- Project Directory: `media/` (gitignored; manifests tracked)
- Deps: `three`, `styled-components` (consumers land with refactor); `unicornstudio-react` removed
- Globe debug: dev always on /lab/globe, `?debug` anywhere in prod builds

## Decisions & Context

### Tagging conventions (canonical, enforced this session)
- **Brand decks**: per-page JPEGs in a subfolder, `mediaType: brand-deck`, `displayGroup` = kebab deck slug, `brandDeckOrder` 1-based; source PDF stays unmanifested. Dual-feed: BrandDeckViewer (detail) + grouped directory presence
- **Carousels**: one image per slide in a subfolder, `mediaType: carousel-slide`, `displayGroup` = kebab carousel slug, `sortOrder` sequences slides; held out of masonry for the Carousel component
- **`displayGroup` is always a kebab-case slug** (production: developed-pitch-deck, label-artwork-deck, branding-phase-1-deck, womens-day-carousel, bedouin-brand, saga, hbd, coco-brand-deck-2026)
- **Masonry crop**: portrait assets clamp to `max(ratio, 3/4)` + cover (9:16 renders as 3:4) — implemented in MediaSlot, documented in CONTEXT.md
- **Ingest IDs are title-keyed** (`mediaAsset-{clientSlug}-{titleSlug}`) — preserve titles when moving rows between manifests to keep upsert identity

### Globe (final, Nathan-approved — do not relearn)
- Tiered illusion (decode limits), 4-tier pool = first-impression curation, mobile cover-overscan + 540p single-rendition manifests, sweep cascade
- three.js custom shaders must manually decode VideoTexture sRGB; hls.js autoLevelEnabled is read-only; never gsap.ticker.fps() globally; forceContextLoss() on unmount; hidden videos never display:none

## Blockers

- **20 Mux-pending motion assets** (Andhera Branding 15, DEVELOPED 5) — everything staged, just needs the ingest run
- **Stale Mux "preparing" statuses** — re-sync needed, then tighten globe query to ready-only
- **Directory grid noise** — 112 deck pages + 9 slides as individual /work cards until grouped components/query exclusion land
- **28 missing video sources** (carried) — mediaAsset docs referencing absent files
