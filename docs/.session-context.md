# Session Context — 2026-06-11

> Auto-generated by `/save-session`. Read by `/load-session`.

## Last Session Summary

Stabilized the persistent navigation shell architecture. Reverted CSS regressions from a gap-fix attempt that broke the info drawer's initial state, fixed the nav bar fade during page transitions caused by the browser scrollbar gutter shifting fixed-position elements, and ensured the SiteShell persists cleanly across all page navigations. Resolved 18 manifest title collisions across 10 manifests (still/motion disambiguation, generic title prefixing, cross-reference deduplication). Deleted 5 orphan PDF-sourced assets and 1 stale video reference from Sanity.

## Completed Work (This Session)

- **Persistent SiteShell architecture** — Implemented `SiteShell.jsx` as the unified, layout-level React island containing `InfoPanel` (drawer + nav) and `ProjectOverlay`, persisted via `transition:persist="site-shell"` in BaseLayout
- **Cross-island communication** — Hero CTA dispatches `swm:open-overlay` CustomEvent to trigger the overlay from within the landing page's separate React tree
- **Info drawer close button** — Moved close button inside the overlay form and gave it a stroke style matching the info pill
- **Project overlay z-index fix** — Resolved close button being hidden behind the nav bar
- **Scroll-to-toggle** — Implemented then removed per user preference
- **CSS reversion** — Reverted `--nav-height` CSS variable, `overflow: visible`, visibility toggle, and `translateY` removal that caused drawer to load in broken open state
- **View Transition fix** — Added `transition:animate="none"` to SiteShell and `::view-transition-old/new(site-shell)` CSS to prevent Astro's default fade during page swaps
- **Scrollbar gutter fix** — Added `scrollbar-gutter: stable` to `html` to prevent nav items from shifting when scrollbar appears/disappears between pages
- **Removed unused import** — Cleaned up `useLayoutEffect` from SiteShell

## Production Scorecard

| Metric | Count |
|--------|-------|
| Clients | 59 |
| Service Tags | 15 |
| Media Assets (Sanity) | 675 |
| Manifest Rows (total) | 690 |
| Mux Video Assets | 201 |
| Manifests | 83 |
| Manifest ID Collisions | 0 |
| Affiliations wired | 0 |

## Current Branch & Git State

- Branch: `Featured-Project-Detail-Page`
- Last 5 commits:
  - `e7024d8` chore: add save-session / load-session slash commands
  - `79caf33` feat(work): Featured Project Detail page — dynamic /work/[slug] route
  - `1d56efc` refactor(work): rename header to project_directory, switch to mono typeface
  - `2a7bff1` feat(work): complete /work route — editorial media grid, filter system, HLS video
  - `8e5939e` feat(cms): Phase 12 complete — 681 assets, 201 Mux videos, full media pipeline
- Uncommitted changes: Yes — persistent navigation shell, SiteShell architecture, CSS stabilization, scrollbar gutter fix

## Pending Tasks (Next Session)

1. **Refine ClientPanel** — polish the featured project detail page header component (`ClientPanel.jsx`) that displays the client name, info, service tags, and social links. This is the foundation component.
2. **Client filter header for Project Directory** — create a variant of the refined ClientPanel that appears when a user selects a client as their filter option on `/work`. This connects the drawer's client list → filtered directory view with a contextual client header.
3. **Sub-pixel gap** between nav bar and content below — cosmetic, deferred; needs a surgical CSS approach that doesn't interfere with GSAP animation layer
4. **Mobile menu** — implement the distinct mobile-specific menu component (currently a placeholder)
5. **NextProjectCard** — scroll-to-next-project component (3 scroll states mocked in Figma)
6. **Re-ingest collision-fixed manifests** — 10 manifests had title edits to resolve 18 ID collisions; these need `ingest.mjs` re-run to create the now-unique Sanity docs (net +15 new assets expected after old orphan IDs are cleaned)
7. **PDF → JPEG Pipeline** — convert 5 known brand deck / pitch deck PDFs (Andhera: DEVELOPED Pitch Deck, Step & Repeat; Bedouin: Brand Guidelines, Human By Default, Saga) into per-page JPEGs for ingestion. Required for BrandDeckViewer component.
8. **BrandDeckViewer component** — accordion-style expanding component for brand deck pages inside FeaturedProjectDetail and FeaturedProjects
9. **Phase 13: Affiliation Wiring** — WIKKA → D'Witches, Pulse Artists → managed artists, labels → signed artists
10. **Phase 14: Record Crate Component Integration** — album art → frontend
11. **Phase 15: Portfolio Filtering Integration** — serviceType × Work page filters

## Active Phase

Frontend: Client Identity Components
- What's done: SiteShell persists across all pages, drawer toggle works site-wide, scrollbar gutter prevents layout shift, view transition fade suppressed
- What's next:
  - Refine ClientPanel on detail pages (typography, layout, social links, service tags)
  - Create ClientPanel variant for Project Directory filter state (user selects client in drawer → directory shows filtered view with client header)
  - Mobile menu implementation
  - NextProjectCard (3-state scroll transition)

CMS Backend: Phase 13 (Affiliation Wiring) — not started

## Key Files Reference

| File | Purpose |
|------|---------|
| `src/components/SiteShell.jsx` | Persistent nav/drawer/overlay shell (React island) |
| `src/components/SiteNav.jsx` | Blue nav bar component (globe, info pill, links) |
| `src/components/InfoPanel.jsx` | Drawer content (description + client list) |
| `src/components/ProjectOverlay.jsx` | Project inquiry form overlay |
| `src/layouts/BaseLayout.astro` | Layout shell — `transition:persist` + `ClientRouter` |
| `src/styles/global.css` | Global styles, shell positioning, view transition overrides |
| `src/components/work/detail/FeaturedProjectDetail.jsx` | `/work/[slug]` orchestrator |
| `src/components/work/ProjectDirectory.jsx` | `/work` orchestrator |
| `src/styles/project-detail.css` | Detail page stylesheet |
| `src/styles/work.css` | Work/directory page stylesheet |
| `src/lib/queries.js` | All GROQ queries |
| `docs/cms-backend-roadmap.md` | Master roadmap (Phases 13–15 pending) |
| `CONTEXT.md` | Domain glossary + Content Population Hierarchy |

## Environment

- Sanity Project: `b60h4u7o` / dataset: `production`
- Token: loaded from `.env.local` (`SANITY_WRITE_TOKEN`)
- Mux: `.env.local` (`MUX_TOKEN_ID`, `MUX_TOKEN_SECRET`)
- Project Directory: `media/` (relative to workspace root, gitignored)
- Dev Server: `npm run dev` (Astro, port 4321+)

## Decisions & Context

### Persistent Shell Pattern
- Navigation is layout-level via `SiteShell.jsx` in `BaseLayout.astro` with `transition:persist="site-shell"`
- State management uses React local state + custom DOM events (no global store)
- GSAP handles all drawer/overlay animations via `useGSAP`
- `transition:animate="none"` + `::view-transition-old/new(site-shell)` CSS suppresses Astro's default page-swap fade
- `scrollbar-gutter: stable` on `html` prevents layout shift from scrollbar presence

### CSS Architecture for Nav
- Nav bar height is `2.929rem` (41px at 14px base) — hardcoded in rem, NOT a CSS variable (the `--nav-height` variable approach caused regressions)
- `translateY(-100%)` is the CSS fallback for closed state; GSAP measures actual panel height and sets precise `y` offset on mount
- The shell uses `position: fixed; top: 0` with `pointer-events: none` (re-enabled on interactive children)

### Known Fragility
- The info-wrapper initial position depends on GSAP measuring `.info-panel` height after mount — any CSS that changes visibility/display before GSAP runs can break the initial state
- Sub-pixel gap between nav and content below is a known cosmetic issue; DO NOT attempt to fix with overflow/visibility changes on `.site-shell` or `.info-wrapper`

## Blockers

- **No `project` documents exist in Sanity** — overview blurb band renders without editorial text until they're created
- **28 missing video sources** (carried over) — 28 mediaAsset docs reference video files absent on disk
- **5 brand deck PDFs** need per-page JPEG conversion before they can be ingested (see CONTEXT.md § PDF → JPEG Pipeline)
- **Browser subagent** — CDP context management errors prevent automated browser testing; all verification is manual via user
