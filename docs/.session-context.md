# Session Context — 2026-07-20 (v1 FOLD complete; two globe/nav refinements queued first)

> Auto-generated by `/save-session`. Read by `/load-session`.

## ⚡ Start here — the FIRST TWO tasks next session

Both are refinements to work that just folded into `feature/v1-launch`. Do these
before anything else.

1. **Globe stroke + starting scale → match the SWM lockup's globe, factoring in
   stroke width.** The homepage globe now has an outer stroke (a blue disc behind
   the canvas, `.hero__globe-stroke`, driven by `Hero.jsx` from the overlay disc;
   thickness = `?globestroke` %, default 5) AND the intro starts with the live
   globe framed in the "o" of the wordmark. Refine BOTH together so the
   ringed globe matches the **size of the globe glyph in the SWM lockup** — the
   added stroke makes the globe read larger, so the base globe's **starting slot
   scale** must come DOWN to compensate (globe-diameter + stroke = lockup glyph
   diameter). Knobs: `?globestroke` (stroke %) in `Hero.jsx` (`GLOBE_STROKE_FRAC`);
   the intro slot targets `O_DESKTOP`/`O_MOBILE` in `HeroIntro.jsx` (the `vw/min/max`
   glyph-diameter, currently sized to the RAW globe, not globe+stroke). Compare
   against `src/assets/swm-lockup-inline.svg` (the `#swm-lockup-glyph` "o").

2. **Scrollbar presence must NOT shift the nav items — keep their position
   PERSISTENT across pages.** The current fix (`--scrollbar-w`, measured in
   `SiteShell.jsx`, added to the nav/footer right inset via
   `calc(--nav-inset + max(--space-4, --scrollbar-w))`) clears the scrollbar but
   makes the nav items sit at DIFFERENT x positions on a scrollable page vs a
   non-scrollable one → they "jump" between pages. Desired: the nav's right-edge
   items land in the SAME place on every route, scrollbar or not, while still
   clearing the bar. Right approach is to **reserve the gutter consistently on
   every page** (e.g. `scrollbar-gutter: stable` on the scroll root, or always
   account for the scrollbar width even where the page doesn't scroll) so the
   layout is identical regardless of scrollbar presence. Touch points:
   `SiteShell.jsx` (`--scrollbar-w` effect) + `global.css` (the `.site-nav` /
   `.site-footer` `padding-right`, and possibly a `scrollbar-gutter` on `html`).

## Last Session Summary

Completed the pass-2 nav/colour refinements + the two Globe/Homepage items, then
executed the **final two-stream fold to `feature/v1-launch`** (ONE squash commit,
`80f79b6`, build-verified). Nathan approved the parallel globe session, which
unblocked the cross-stream footer nav-slide and cleared the way to do the
Globe/Homepage items on the cascade branch and to fold.

## Completed Work (This Session)

**Pass-2 nav/colour refinements (on `refine/site-pass-2`, `29a4627`→`10789bc`):**
- **Single nav-accent control point** — `src/lib/navAccent.js` (applyNavAccent /
  clearNavAccent / reapplyNavAccent), applied BEFORE paint by the RouteFill
  route controller (`astro:after-swap`). Fixes: no brand-blue flash on
  enter_world / next-project / breadcrumb (0 blue frames sampled), colour
  persists through the breadcrumb back to /work. Fixed the `if(v)` 0-falsy bug
  so `--nav-ink-l = 0` (dark ink) actually applies → light accents (HHS) darken.
- **Detail-page nav tint** (was stuck blue); **breadcrumb hover** accent +
  readable ink; **client socials icons** light/dark via `--project-globe-filter`;
  **prev/next CTA** flash → accent; **left pager chip** colour lag removed;
  **enter_world** colour sweep 0.04s→0.5s.
- **oklab nav ink** — perceptually-even light↔dark both ways (`--nav-ink-l`
  scalar → `color-mix(in oklab …)`), replacing the asymmetric sRGB fade.
- **Info-pill fill** synced to the bar (cleared its micro-transition low-pass).
- **Footer**: SWM lockup SVG (`?raw`, mono-white) replaces text+globe; **footer
  nav-slide** wired (F1) — `.site-shell` slides up `--nav-height` on
  `[data-footer-revealed]`, gated `:not([data-chrome-open])` so an open
  drawer/overlay isn't shifted.
- **Scrollbar-aware nav inset** (`--scrollbar-w`) + **globe icon** now rides the
  ink cross-fade (`brightness(var(--nav-ink-l))`, was snapping). ← both get
  REFINED next session (see ⚡ #2 and the globe part of #1).

**Globe/Homepage items (on `refine/home-hero-rework`, `d431631`):**
- **Outer stroke** — `.hero__globe-stroke`, a blue disc behind the canvas tracked
  to the live globe disc each frame (transform-only), showing a thin ring at the
  silhouette; scales through the intro/commit. `?globestroke` dials it.
- **Globe obscures the wordmark on scale-up** — `.hero__globe` z:2 (above the
  intro letters, now z:1); variant A's chars scale up + slide off-frame (no
  fade) so the growing globe covers/carries them out instead of dissolving.

**THE FOLD (`feature/v1-launch`, `80f79b6`):** squash-folded BOTH streams into
ONE commit over the shared base `c48c52e`. `global.css` auto-merged as the union
(hero + nav/colour/footer sections don't overlap); only conflict was the session
doc (moot — this file). Build clean (22 pages). Non-destructive.

## Current Branch & Git State

- **Branch: `feature/v1-launch` @ `80f79b6`** (main checkout). One fold commit on
  base `c48c52e`. Tree clean (only untracked strays: reference PNGs/SVGs, the
  `fable-mode-main` skill — none tracked).
- Last commits: `80f79b6` (fold) → `c48c52e` (shared base) → `04a2888`…
- **Stream branches preserved, untouched:** `refine/home-hero-rework` @ `d431631`
  (cascade), `refine/site-pass-2` @ `10789bc` (checked out in the `swm-pass-2`
  worktree). Old v1 tip `11c46ab` preserved as an ancestor of `c48c52e` (nothing
  lost).
- **NOT pushed.** `origin/feature/v1-launch` is at `7153049` (behind); the v1 PR
  is the next git milestone (see Pending).

## Pending Tasks (Next Session) — priority order

1. **⚡ Globe stroke + starting scale to match the lockup glyph** (see top).
2. **⚡ Scrollbar-persistent nav position across pages** (see top).
3. **HR-6 — mobile / device gate**: iPhone perf + comp pass on the folded v1
   (globe scroll video-decode churn, mobile layouts). Was deferred behind the
   pass-2 crossover approval, now clear. Run it against `feature/v1-launch`.
4. **The v1 PR**: push `feature/v1-launch` and open the PR to `main` (squashes to
   one commit on merge). Only after the two ⚡ refinements + HR-6 sign-off.
5. Later / brand-cohesion: bring the meridian-scroll choreography to `/process`
   (built as reusable opt-in primitives).

## Key Files Reference

| File | Purpose |
|------|---------|
| `src/components/hero/HeroIntro.jsx` | Intro machine (wordmark → globe). `O_DESKTOP/O_MOBILE` = the intro's starting globe slot scale (⚡#1). Variant A char exit = scale+slide-off (obscure). |
| `src/components/Hero.jsx` | `GLOBE_STROKE_FRAC` (`?globestroke`) + the stroke-tracking effect; commit/scroll choreography; overlay disc bridge. |
| `src/styles/global.css` | `.hero__globe-stroke`, hero z-orders (globe z:2 / intro z:1), the nav-tint/@property oklab block, `.site-nav`/`.site-footer` scrollbar inset (⚡#2), footer nav-slide. |
| `src/components/SiteShell.jsx` | `--scrollbar-w` measurement (⚡#2), `data-chrome-open` gate, footer-slide host. |
| `src/lib/navAccent.js` | The single nav-accent control point (before-paint, oklab, 0-safe). |
| `src/components/RouteFill.jsx` | Route controller — applies the nav accent on `astro:after-swap` (no blue flash) + the envelop/fill bridge. |
| `src/lib/projectColor.js` | Per-project colour core (YIQ threshold, `--nav-ink-l`, `--project-globe-filter`). |
| `src/assets/swm-lockup-inline.svg` | The brand lockup; `#swm-lockup-glyph` is the "o" globe — the reference for ⚡#1's target size. |
| `docs/refinements-master-plan.md` | Status board (Wave 5 HR-0…HR-6 + HR-R). |

## Decisions & Context

- **v1 fold shape:** `feature/v1-launch` = base `c48c52e` + ONE squash commit
  (`80f79b6`) = union of both streams. The base already carried the home-hero
  foundation (99 commits above the old v1 tip `11c46ab`, which stays in history).
  The eventual PR to `main` squashes to one. Stream branches are kept as the
  authoritative per-stream history until the PR merges.
- **Nav colour is ONE control point** (`navAccent.js`), applied before paint on
  every route swap — the fix for cross-page blue flashes AND breadcrumb
  persistence. `--nav-ink-l = 0` is a VALID value (dark ink); never guard with
  `if(v)`.
- **Globe outer stroke** = a flat blue disc BEHIND the (alpha) canvas, only the
  silhouette ring shows; transform-driven so it's compositor-cheap per frame.
- **Globe obscures, not fades**: globe canvas z-above the intro letters; the
  transparent canvas shows them through, the opaque disc covers them.
- **Scrollbar current state is a HALF-fix**: it clears the bar but shifts nav
  position between pages — ⚡#2 makes the reservation consistent instead.
- **Verify recipe:** prod preview on a scratch port (cascade used **:4402**,
  pass-2 **:4400**; NEVER :4321 = Nathan's dev). Headless: playwright-core in
  `/tmp/pw-verify` (reinstall if the scratch node_modules got GC'd — it did once
  this session), chromium headless shell in `~/Library/Caches/ms-playwright/…`,
  ANGLE/metal + autoplay flags. Home globe renders untextured on non-allowlisted
  ports (Sanity CORS only has `127.0.0.1:4399`) — shape/choreography still read.

## Blockers

- None hard. The two ⚡ refinements are tractable dials/CSS. The v1 PR waits on
  those + HR-6. Nothing is pushed; `origin/feature/v1-launch` (`7153049`) will be
  fast-forwarded/replaced by the local `80f79b6` line at PR time.
