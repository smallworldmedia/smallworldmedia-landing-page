# Session Context — 2026-08-29 (late) (TOKEN SWEEP shipped + the soft-nav right-edge shift fixed; next = mobile pass)

> Auto-generated by `/save-session`. Read by `/load-session`.

## Last Session Summary

Executed Nathan's pre-mobile token sweep in full (3 commits, all PUSHED —
preview rebuilding): every literal on the fodder list converted to tokens,
value-identical and probe-verified; the scrollbar keep-out formula
consolidated to ONE `--chrome-right-inset` token; and Nathan's live report
(nav items shift right after home→featured_projects) diagnosed to a REAL
bug — the ClientRouter `<html>` wipe eating SiteShell's `--scrollbar-w`
inline property — and fixed with the standing after-swap re-assert idiom.
Branch is clean; the runway to `main` is the mobile pass + F15/F9.

## Completed Work (This Session)

- **Token sweep** (`7f29ff3`): all value-identical, computed-style verified
  against the built site at 1440/1280/1100, zero console errors.
  - `--text-link`/`--lh-link` (16/22): nav links, footer links + copyright,
    BOTH corner pills' text — one LINK type tier (pills previously mixed
    `--text-ui` with `--lh-pill`).
  - Capsule pill skin: `--pill-pad-y`/`--pill-pad-x` (aliases of
    --space-4/--space-6) + `--radius-capsule: 999px` shared by
    `.site-tagline__pill`/`.site-privacy`; every other 999px capsule
    (cta-primary, project tags, detail chip) joined the radius token.
  - `--privacy-clearance: 7rem` (footer nav keep-out); the row's
    margin-bottom is now literally `var(--pill-pad-y)`.
  - `--lockup-svg-h: 7rem` + `--lockup-art-h` (×0.28): nav/tagline lockup
    svg heights, `--lockup-bleed`, AND the footer band min-height all
    derive from one canvas token — the flat `4.96rem` replaced by the real
    stack calc (Δ 0.01px rounding); `--footer-top-pad` local on
    `.site-footer__inner`; tagline lockup's 0.857rem seat is now
    `var(--pill-pad-x)` (the alignment it was encoding).
  - /process caption: `--caption-pill-reserve`/`--caption-slope`/
    `--caption-shrink` local knobs replace 6.5rem + 1.38vw − 0.5rem
    (deliberately NOT coupled to --privacy-clearance — 6.5 vs 7rem is
    intentional, no row gap to mirror).
  - work.css: undefined `--color-charcoal` → `--color-dark-gray`. The ONE
    visible change of the sweep: album-card loading ground was silently
    TRANSPARENT, now the intended dark fill. Probe couldn't reach an
    .album-card on /work at 1440 — **needs a quick eyeball**.
- **`--chrome-right-inset`** (`c5af0c2`, Nathan approved the proposal):
  the calc(--nav-inset + max(--space-4, --scrollbar-w)) formula was
  hand-copied at SIX sites (nav bar, mobile menu bar, footer bar + inner,
  privacy pill, /process caption) — now one token in global.css ~L171
  (carries the full device-constant story; the --footer-bottom-inset move
  mirrored for the right edge). Process caption composes its reserve on
  top. GOTCHA hit + fixed mid-swap: the perl regex rewrote the token's own
  definition into a self-reference — restored by hand.
- **NAV SHIFT FIX** (`0a123cf`, Nathan's report: home hard-reload → nav
  items sit left; click featured_projects → they shift right ~7px):
  ClientRouter soft navs wipe ALL `<html>` attributes INCLUDING INLINE
  STYLE — SiteShell's one-shot `--scrollbar-w` write vanished on the first
  soft nav; on classic-scrollbar devices (Nathan's mouse) the right inset
  collapsed 19px→12px, shifting every `--chrome-right-inset` consumer.
  Overlay-scrollbar headless probes masked it (measured 0px = wiped
  fallback 0px). Fix: setSbw re-runs on `astro:after-swap`
  (SiteShell.jsx ~L149). Probe-verified: property survives
  home→/work→/process with byte-identical link rects.
- **Island `<html>`-write audit** (post-fix sweep): data-privacy-landed
  (re-asserted), --footer-reveal (per-frame), navAccent ink vars
  (route-controller re-driven on after-swap) all covered. One theoretical
  edge left alone: `data-chrome-open` if a soft nav could land while the
  info panel is open — not a real user path today.
- **Memory updated**: clientrouter-html-attribute-wipe now records the
  second victim + the non-obvious "inline style counts as an attribute"
  detail.

## Current Branch & Git State

- Branch: `feature/v1-launch` @ `0a123cf`, PUSHED (origin in sync) —
  branch preview auto-rebuilding.
- Last 5: 0a123cf scrollbar-w latch fix · c5af0c2 --chrome-right-inset ·
  7f29ff3 token sweep · 418c14c session save · 967614e revision round.
- Uncommitted: none + the 5 personal untracked strays (fable-mode skill,
  2 root logo SVGs, 2 reference PNGs).

## Pending Tasks (Next Session)

1. **Mobile device pass** (HR-6 gate) — now unblocked, lands on tokens as
   intended. Real-device; includes the IS_MOBILE load-time snapshot trap
   (worldConfig + processConfig don't re-branch on live cross-768px
   resize), the ≤768px privacy fallback (in-row footer link), and the
   mobile menu (no current-page visual currently).
2. **Nathan eyeballs** (quick, real browser): the album-card dark ground
   on /work (the sweep's one visible change) · the nav-shift fix with his
   mouse plugged in (classic scrollbars) · ROSSI next-project thumbnail
   (standing headless-render doubt).
3. **Launch runway leftovers**: F15 analytics decision (cookieless, decide
   at deploy) · F9 real form submission test (preview URL exists). Then
   merge to `main` = production.
4. Standing deferrals: chrome keep-outs for [PREVIOUS]/[NEXT] bands (+ the
   privacy pill corner on /work) · enter-dive re-bake (`?entertune`) ·
   ATLAS idle beat · saga deck wall starts page 2 · `goTo()` pager clicks
   don't check the turn lock (deliberate) · per-release metadata PARKED.

## Key Files Reference

| File | Purpose |
|------|---------|
| `src/styles/global.css` | Token block ~L100-235: NEW --text-link/--lh-link, --pill-pad-y/x, --radius-capsule, --privacy-clearance, --lockup-svg-h/--lockup-art-h, --chrome-right-inset (~L171, six consumers) |
| `src/components/SiteShell.jsx` | ~L132-155: --scrollbar-w probe now re-asserts on astro:after-swap (the latch fix) |
| `src/styles/process.css` | Caption knobs: --caption-pill-reserve/--caption-slope/--caption-shrink on .process-tagline |
| `src/styles/work.css` | album-card ground: --color-dark-gray (was undefined --color-charcoal) |
| `docs/v1-launch-plan.md` | Launch runway (F15/F9 rows) |

## Decisions & Context

- **Latch trap EXTENDED (memory: clientrouter-html-attribute-wipe)**: the
  ClientRouter wipe eats INLINE STYLE on `<html>` too, not just data-*
  attributes. Every one-shot html-level write from a persisted island
  needs an astro:after-swap re-assert. Audit run this session — all
  writers covered.
- **Scrollbar-masking probe trap**: overlay-scrollbar environments
  (headless, trackpad-only Macs) CANNOT catch --scrollbar-w regressions —
  measured 0px is indistinguishable from the wiped 0px fallback. Verify
  that class of bug by asserting the inline property EXISTS
  (`html.style.getPropertyValue`), not by comparing rects.
- **Token philosophy of the sweep**: value-identical substitutions only;
  page-local tuned constants become LOCAL custom props (process caption),
  not global tokens; deliberately-different values (6.5 vs 7rem reserves)
  stay separate tokens rather than forced derivations.
- **--lh-pill vs --lh-link**: both 1.571rem by design (18px pill tier and
  16px link tier share one rhythm) — separate tokens so either can move;
  the footer band min-height calc rides --lh-link (what the corner pills
  consume).
- **Verification pattern for CSS refactors**: build + Playwright
  computed-style probe (getComputedStyle px values) against `astro
  preview --port 4330` (CORS-allowlisted), compared to pre-change probe
  values — byte-identical or bust.
- **Playwright traps** (standing): /work wheel needs mouse.move to the
  deck first · composited screenshots for WebGL/paint bugs · bundled
  playwright at
  `/opt/homebrew/lib/node_modules/@playwright/mcp/node_modules/playwright`,
  `channel:'chrome'` · viewport key · page.$ for optionals.
- **Deploy law unchanged**: production = merge to `main`; branch pushes
  rebuild the stable preview automatically.

## Blockers

None. Everything pushed and probe-verified; preview rebuilding. Next
session opens on the mobile device pass (plus Nathan's three quick
eyeballs).
