# Session Context — 2026-07-16 (revision-notes triage → round 3 → wave execution)

> Auto-generated by `/save-session`. Read by `/load-session`.

## Last Session Summary

The biggest shipping day of the project. Nathan's Notion **"SWM Website Revision
Notes"** was triaged into `docs/refinements-master-plan.md` (plan mode: 5-agent
recon + 3-critic adversarial review; Nathan confirmed 4 interpretation calls),
then largely EXECUTED: Phase A motion foundations, the full process-page round 3
(B1–B9), Nathan's two dial-ins baked, and seven wave PRs (#29–#35) built,
probe-verified, approved on a live preview, and merged into `feature/v1-launch`
— including the **rossi+homegrwxn CMS merge (published)** and the HP-1 home
hero recomposition. Eight GitHub issues closed.

## Completed Work (This Session)

- **Master plan authored** (`docs/refinements-master-plan.md`, the status board)
  + issues #16–#28 filed. Confirmed calls: hero h1 → "PROCESS"; only [previous]
  moves top-left; meter = 20 cells; caps words = stage headlines.
- **PR #15 Phase A** (merged): `src/lib/motion.js` (house pulse curve
  `HOUSE_PULSE_PATH`/`housePulseLoop`, per-surface scroll constants,
  `LENIS_TUNING` passthrough), `--ease-pulse`/`--duration-pulse`/`--nav-inset`
  tokens + `@keyframes house-pulse` in global.css.
- **Process round 3** (this branch, commits `90e8658`…`bdd019b`): B1 copy deck
  v2 (PROCESS hero, caps headlines, Notion blurbs, bottom-right tagline) · B2
  chrome ([previous] top-left below nav, [next] bottom-left, meter
  bottom-center ×20 cells) · **B3 Thread → in-scene Line2** (depth-occluded,
  inside-normal attachment; spec §4 amended) · B4/B5 S1 suspended flood (36
  decoys as ONE InstancedMesh, zero-collision relaxed seeding, 4 blob-tracking
  labels, flicker-in; S2 zoom + decoy cull) · B7 S4 emanation lag/jitter · B8 S5
  `?s5zoom/?s5tilt/?s5stroke` + brown-blue inner stroke (S5_STROKE_A/B) +
  `?decaycurve` A/B · B9 hero splash (globe alone → PROCESS random-char → 1.5s
  hold → out → auto-glide) + globe-O placeholder. **20/20 headless suite**
  (jitter probe span 0.000px). Nathan's dial baked (`eea45dc`): scatter 2.05,
  drift 0.09, idlepower 0.54, bpm 123, s5tilt 33, s5stroke 1, swipems 800…
- **PR #29** (merged): triggers unified **500px** + **`GLIDE_MS` 800** site-wide
  (hero envelopment + next-project band + process swipe; `TURN_DURATION` 1700
  deliberately kept). Spec/param rows updated.
- **PRs #30–#32** (agent-built in worktrees, probe-verified, approved by Nathan
  on a merged preview @4343, merged): #30 FP-4a detail routing by
  `project._ref` (0-diff fingerprint) · #31 FP-3 scroll-up-home reverse
  envelopment (+ fixed: home never dispatched `swm:fill-release`) · #32 inquiry
  batch (top-down wipe-in / bottom-up wipe-out, next-field
  `house-pulse-brighten`, icon parity Δ0.02px).
- **PR #33** (merged): NAV-1/FT-1 — `--nav-inset` 16px → **4px**
  (`var(--space-2)`, Nathan's pick); footer bar full-bleed, content on the nav
  edges.
- **FP-4b executed** (PR #34 merged + CMS **published**, snapshot on issue #26):
  pre-check caught that the derived slug would be `rossi-homegrwxn` → routing
  switched to **authored `slug.current`** on both World cards and detail paths
  (toProjectSlug = fallback); 4 stale authored slugs aligned to live URLs
  (coco ×2, HHS ×2); homegrwxn's 6 assets → `project-rossi` (17 total),
  homegrwxn + bellaire unfeatured, rossi `title:"Rossi"`; redirects
  `/work/homegrwxn`→`/work/rossi` 301, `/work/bellaire`→`/work` 302. Build
  verified: 14 pages, no orphans.
- **PR #35** (merged): HP-1 home hero recomposition — ⊙ process top-right slot,
  start_project → right edge vertical-center (shell-portaled), "Visual worlds
  for the music industry." → left-center statement lead in the /process prose
  register (replaces the orphaned drifting taglines + hero footer tagline slot;
  `SiteFooter` tagline renders only when truthy). Envelop choreography updated
  (start exits right). Probe 10/10.
- Issues #19–#22, #24–#28 closed. Memory updated (`refinements-master-plan`).

## Current Branch & Git State

- Branch: **`claude/load-session-wippng`** (the process build) — fully pushed,
  tip `446c914` + this save. Dirty: only `.astro/types.d.ts` (generated) +
  the standing untracked files (`SWM-globe_white.svg`, `breadcrumb_move.png`,
  `.claude/skills/fable-mode-main/`).
- `feature/v1-launch` tip = PR #35 merge. **PRs #15, #29–#35 all merged there.**
- ⚠️ **Divergence:** this branch last merged v1-launch at PR #29. PRs #30–#35
  are NOT here yet — next session should `git merge origin/feature/v1-launch`.
  Note: that merge brings `--nav-inset` 4px, which process.css chrome corners
  consume (steppers/meter/tagline will tighten 16→4px — likely desired, but
  Nathan should eyeball).
- ⚠️ **Deploy note:** the CMS is already merged/published — any production
  build MUST come from a ref that includes PR #34 (older refs would derive the
  wrong rossi slug).
- Dev server: `npm run dev` (:4321) on this branch, running at save time.

## Pending Tasks (Next Session)

1. **Nathan:** Studio drag-reorder on the merged rossi project (17 assets;
   hero = first-in-order; homegrwxn ranks interleave until reordered).
2. **Merge `feature/v1-launch` into this branch** (see divergence note) and
   eyeball the 4px chrome corners on /process.
3. **Remaining AGENT items:** FP-2 video first-frame (issue #23 — measured
   before/after), A2b remainder = `LENIS_TUNING` fill + parallax/scroll-kick
   regression pass.
4. **CREATIVE items (ultracode):** NAV-2 nav micro-interactions ×4 options
   (`?navfx` demo branch), FP-1 enter_world dim pulse (consumes
   `housePulseLoop`), DP-1 next-project transition rework (fade confined to
   media box, media → viewport).
5. **Process refine pass 2** (Nathan's stated intent) — then P5 device pass
   (iPhone ≥42–44fps) → P6 3-file un-gate (`/process` launches; the home +
   nav process links stop 302ing).
6. v1 residuals: ws6 device gate → PR `feature/v1-launch` → `main`; F9 inquiry
   submission on a deploy preview; F15 analytics decision; ws3 content pass.

## Key Files Reference

| File | Purpose |
|------|---------|
| `docs/refinements-master-plan.md` | THE status board — all wave items + PR refs |
| `src/lib/motion.js` | House motion authority: pulse curve, 500px triggers, GLIDE_MS 800, LENIS_TUNING |
| `src/components/process/useProcessScene.js` | Scene: 3D thread, decoy pool, labels, tilt/inner-stroke rhythm |
| `src/components/process/processConfig.js` | Baked 07-16 dial (scatter 2.05, drift 0.09, bpm 123, s5*) |
| `src/components/process/useProcessCopy.js` | B9 splash + globe-O overlay |
| `src/lib/queries.js` + `src/pages/work/[slug].astro` | Authored-slug routing (slug.current, toProjectSlug fallback) |
| `src/components/HeroText.jsx` / `Hero.jsx` / `SiteNav.jsx` | Home recomposition (lead, pill placements, envelop exits) |
| `docs/process-page-plan.md` | P0–P4.7 ✅, P5/P6 ☐ |

## Decisions & Context

- **Authored slugs are THE route** (`slug.current`; derived = fallback). The 4
  stale doc slugs were aligned to live URLs first — zero URL changes.
- **500px / 800ms are the house gesture constants** (motion.js); /work's
  TURN_DURATION 1700 is a scene transition and keeps its own dial.
- **`--nav-inset` = 4px** (`var(--space-2)`) — one token moves nav, mobile
  menu, footer, follow_us pill; right edges add `--space-4` clearance.
- **Copy-column y still has ONE owner** (`leanTo`); the `?debug` panel owns the
  lower-left ([next] takes the shove).
- **S5 swatches** `S5_STROKE_A/B` (0x3e3a52 / 0x53402f) are first drafts;
  `?decaycurve` defaults expo until Nathan blesses linear.
- **Globe-O is an SVG placeholder** — the looping longitudinal-translation
  Lottie is an open asset task.
- **Local verify recipe:** scratch playwright-core at `/tmp/pw-verify` +
  chromium_headless_shell-1228 (`chrome-headless-shell-mac-arm64/
  chrome-headless-shell` — path changed from the old note), args
  `['--use-angle=metal']`, `waitUntil:'load'` never `networkidle`.
  Worktree builds need `npm ci` (symlinked node_modules breaks Vite).
- **Agent worktree convention:** wave items built by agents in
  `.claude/worktrees/`, committed (not pushed), reviewed + shipped by the main
  session; one agent died at its session limit mid-verify — its complete work
  was verified and shipped by the main session (PR #32).

## Blockers

- None hard. P5 needs Nathan's iPhone. The prod /process links 302 home until
  P6 (deliberate). Production deploys must include PR #34 (CMS already merged).
