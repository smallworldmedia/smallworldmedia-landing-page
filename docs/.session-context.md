# Session Context — 2026-07-20 (meridian-scroll set COMMITTED + APPROVED — pole height-eat + mirror + pole cap + live bench + baked comp)

> Auto-generated by `/save-session`. Read by `/load-session`.

## ⚡ Start here

The **entire meridian-scroll globe set is committed AND APPROVED by Nathan** (2026-07-20).
It shipped as one change:
**`bdea06e feat(hero): meridian-scroll globe — pole height-eat, live tuning bench, baked comp`**
on `refine/home-hero-rework`, with Nathan's dialed values BAKED as the shipped
defaults (near equator-on, pole-cap globe, labels off, slow reverse yaw drift).

**Homepage globe work is DONE for now — do NOT resume it.** Nathan relayed the
approval to the parallel **revision-notes-02 (pass-2)** session agent. The remaining
sequencing is now GATED on that stream:

1. **Nathan finishes/approves the crossover items on the parallel pass-2 work** (the
   revision-notes-02 stream, `swm-pass-2` worktree). ← the gate, owned by that session.
2. **THEN Nathan picks up the HR-6 mobile device gate** on this globe (iPhone perf
   pass — yaw + scroll + video-decode churn; mobile comp NOT re-dialed). He said he'll
   do this AFTER the pass-2 crossover items are approved — so it is DEFERRED, not next.
3. **THEN fold + squash both streams to `feature/v1-launch`.**

If you're a fresh agent on THIS (globe) branch: there is no active task — the globe is
approved and parked pending the parallel stream. Don't touch it unless Nathan asks for
the HR-6 gate or a specific tweak.

## Last Session Summary

Executed Nathan's pole-refinement pass on the meridian-scroll globe and shipped it.
Added the **near-pole height-eat** (tiles lift into rounded caps terminating short
of the pole), **mirrored it to BOTH poles**, added a **persistent spherical pole
cap** to mask the residual convergence, built a **live `?herotune` tuning bench**
for the whole pole/orientation/motion set (+ URL params + copy_url), then **baked
Nathan's dialed values** as defaults and **committed the full set as one change**.
Three adversarial-review Workflows ran across the session; every confirmed finding
(all minor/nit) was fixed, incl. a reduced-motion regression the parity suite caught.

## Completed Work (This Session) — COMMITTED in `bdea06e`

**Pole height-eat** (`panelMaterial.js`) — the near-pole refinement Nathan asked for:
- The pole-facing bottom of each scroll tile LIFTS into a rounded elliptical cap
  that terminates SHORT of the pole (clean blue pole region). Single branchless
  field `cornerD = max(boxD, capD)` — C0 across the midline by construction, and
  the cap's vertical radius exceeds 0.5 natively (normalized ellipse, no
  `0.5 − rV` box inset — that was the math trap). Chosen via a design-panel
  Workflow (halfcap beat ellipse/vertex) + adversarial harden.
- **Mirrored to BOTH poles**: a per-row flat varying `vFlipPole` (vertex sets it
  = `thetaC < π/2`) flips the cap's vertical coord `w = mix(vUv.y, 1−vUv.y,
  vFlipPole)` so the SAME dialed values eat the correct (pole-facing) edge at each
  pole. The flip is at the equator where poleness=0 → seamless. Guarded by
  `clamp(uPoleCornerStart, 1e-4, 1.0)` (sin(θ) threshold; >1 is meaningless and
  would pop the flip).

**Persistent pole caps** (`useGlobeScene.js`) — a small `SphereGeometry` cap in the
inner-sphere blue at EACH pole (radius `1.003·R`, shares `innerMaterial`), masking
the residual convergence point. Rebuilt-not-scaled on size change; gated on
`conveyorMode` (home only). Tunable `SCROLL_POLE_CAP_DEG` (default 6°).

**Live tuning bench** (`?herotune=1`, `HeroTunePanel.jsx` + `heroConfig.js` +
`Hero.jsx` + `useGlobeScene.js`):
- New live-tunable knobs, all pushed slider→`heroConfig` TUNING pub/sub →
  `Hero.stampGlobeTuning` → new scene-api setters (`setPoleTuning` /
  `setGlobeOrientation` / `setCascadeSpeed` / `setPoleCap`), persisted across
  scene rebuilds in a hook-level `tuneRef`.
- Knobs + URL params: pole lift `?polelift`, nose `?poletip`, wide `?polewide`,
  ramp `?polestart`, base corner `?corner`, cap `?polecap`, tilt `?herotilt`,
  yaw offset `?heroyaw`, **yaw speed `?yawspeed`** (steady auto-rotation, °/s),
  cascade pace `?cascadespeed`. All live, all serialized by `copy_url`
  off-default-only. Cascade + tilt + yaw are now LIVE (were reload-only).

**Baked defaults** (Nathan's final dial):
- Pole (`globeConfig.js`): `SCROLL_POLE_TIP_LIFT` 0.7, `SCROLL_POLE_CORNER_START`
  0.4, `SCROLL_POLE_CORNER_WIDE` 0.3, `PANEL_CORNER_RADIUS` 0.07. (TIP 0.5, CAP 6°
  unchanged.)
- Comp (`heroConfig.js` desktop): fill 1.42, offsetX 0.59, offsetY 0.5,
  elevDeg 2.5 (near equator-on — flipped from the old elev-70 underside view),
  textGap 40.
- Motion: `cascadeSpeed` 2.5, `tiltDeg` 45.5 (literal, decoupled from
  `INITIAL_PITCH_DEG`=40 so /lab stays 40), `yawSpeed` −1.5°/s.
- `HERO_LABELS` → **false** (blob-tracking chips off by default).

**Guards / fixes** (from the 3 review Workflows): RM suppresses the yaw spin
(`if (!PREFERS_REDUCED_MOTION)`); `clamp(uPoleCornerStart,1e-4,1)` mirror seam;
per-tick `step = min(accumulated, 0.1)` prevents a tab-resume yaw/scroll jump
(gsap `lagSmoothing(0)` is global). Remaining nits accepted: `spinYaw` resets on
scene rebuild (home never rebuilds); no eslint gate so no lint impact.

Verified throughout: build clean (22 pages), **reduced-motion static**,
**/process + /lab byte-identical**, both poles show the mirrored lift face-on,
yaw rotates, copy_url off-default-only, baked default matches the dial, zero
console errors.

## Current Branch & Git State

- Branch: **`refine/home-hero-rework`** — tip **`bdea06e`** (the meridian-scroll set),
  now 18 commits ahead of the base. Prior tip was `c48c52e`.
- **Committed:** the 10-file feature set (`Hero.jsx`, globe `InteractionController`
  / `LivePanelScheduler` / `buildGlobeGeometry` / `globeConfig` / `panelMaterial` /
  `useGlobeScene` / **`MeridianScroll.js`** (new), hero `HeroTunePanel.jsx` /
  `heroConfig.js`).
- **Uncommitted (intentionally left out):** `.astro/data-store.json` +
  `.astro/types.d.ts` (generated CMS/type artifacts), `docs/.session-context.md`
  (this file — commit separately as `docs(session)`), and untracked strays
  (`SWM-globe_white.svg`, `Small World Media Logo Inline Blue-01.svg`,
  `breadcrumb_move.png`, `homepage_refine_01.png`, `.claude/skills/fable-mode-main/`).
- **NOT PR'd.** Two-stream fold-in still pending.

## Pending Tasks — priority order (globe stream is GATED on the parallel stream)

0. **[GATE — owned by the parallel revision-notes-02 session, NOT this branch]**
   Nathan finishes/approves the crossover items on the pass-2 site-wide work
   (`swm-pass-2` worktree, on `:4399`). Everything below waits on this.
1. **HR-6 mobile device gate — DEFERRED until (0) clears.** Nathan does the iPhone
   perf pass himself AFTER the pass-2 crossover items are approved. Profile the yaw
   auto-rotate + meridian scroll + video-decode churn together on mobile, and check
   the mobile comp (`heroConfig` mobile COMP_DEFAULTS were NOT re-dialed — still
   fill 0.95 / elev 8 / offsetY −0.18; confirm the pole-cap globe reads on mobile).
2. **Fold + squash to `feature/v1-launch`** — combine this cascade/meridian stream
   with the parallel pass-2 stream after BOTH approved. ONE commit on
   `feature/v1-launch`. Neither pushes to v1 alone.
3. **Bring the meridian-scroll choreography to `/process`** (Nathan wants it; built
   as reusable opt-in primitives — `useProcessScene` currently keeps its own fixed
   globe). Later / brand-cohesion.
4. **Optional bench polish:** the mobile bench values, and the fact that `poletip`/
   `polecap` weren't in Nathan's last dial (left at 0.5 / 6°) — reconfirm on device.

## Key Files Reference

| File | Purpose |
|------|---------|
| `src/components/globe/panelMaterial.js` | Vertex polar-scroll + `vFlipPole` mirror; fragment media-crop (`mUv`/`vK`) + height-eat halfcap (`max(boxD,capD)`) + pole-corner uniforms. THE pole shader. |
| `src/components/globe/MeridianScroll.js` | The scroll driver (scroll/wrap/recycle, `centerDir`, `heldThumbId` refcounting, `setSpeed`). Reusable brand choreography. |
| `src/components/globe/useGlobeScene.js` | Scene hook. `tuneRef` (live tuning, survives rebuilds), pole-cap meshes, api setters, tick rotation (tilt offset + `spinYaw`), `step` clamp. |
| `src/components/globe/globeConfig.js` | Baked pole defaults (`SCROLL_POLE_*`, `PANEL_CORNER_RADIUS`, `SCROLL_POLE_CAP_DEG`). |
| `src/components/hero/heroConfig.js` | TUNING state + defaults (baked comp/motion/labels), PARAM_KEYS, copy_url. |
| `src/components/hero/HeroTunePanel.jsx` | The `?herotune=1` bench — globe·flow+orient + globe·pole cap sections. |
| `src/components/Hero.jsx` | `stampGlobeTuning` (pushes pole/orient/cascade/cap to the api), `PANEL_CORNER_RADIUS` from globeConfig. |
| `docs/refinements-master-plan.md` | Status board — Wave 5 (HR-0…HR-6) + HR-R. |

## Decisions & Context

- **The cascade IS a meridian-scroll** (tiles physically travel; persistent assets;
  poles fixed) — the BRAND globe choreography. Opt-in via `uUsePolarScroll` so
  /process + /lab are byte-identical.
- **Pole height-eat is a single `max(boxD, capD)` field, mirrored via `vFlipPole`.**
  Same dialed values → both poles. The **pole cap** (spherical, blue) masks what the
  eat leaves.
- **`tiltDeg` is decoupled from `INITIAL_PITCH_DEG`** — the tick applies
  `(tiltDeg − INITIAL_PITCH_DEG)` as an offset, so baking home tilt to 45.5 keeps
  /lab at 40 (parity). Same idea for the yaw spin (home-only via the Hero push).
- **Reduced motion suppresses the yaw spin** (motion), but the static tilt/yaw
  offsets still apply (pose, not motion).
- **Nathan's URL param gotcha:** panel labels ≠ URL param names — wide radius is
  `?polewide` (not `wideradius`). copy_url emits the canonical names.
- **Verify recipe:** headless shell at
  `~/Library/Caches/ms-playwright/chromium_headless_shell-1228/.../chrome-headless-shell`,
  args `--use-angle=metal --autoplay-policy=no-user-gesture-required`. **Prod preview
  on scratch port 4388** (`npm run build` then `npm run preview -- --port 4388`),
  scripts in `/tmp/pw-verify/` (`pole_check.mjs`, `scroll_check.mjs` = parity suite,
  `pole_zoom.mjs`, `cap_capture.mjs`, `mirror_yaw.mjs`, `pole_faceon.mjs`).
  **⚠️ PORT CHANGE: `:4399` belongs to the PARALLEL session (`swm-pass-2` worktree)
  — DO NOT touch it. Use 4388 for scratch previews. `:4321` is Nathan's dev server —
  never kill it.**
- **Nathan reviews on `:4321`** (HMR of the working tree). He dials via `?herotune=1`
  + URL params, hands back a copy_url; I bake + review + commit.

## Two-session workflow + git strategy (unchanged)

Two streams fold into a SINGLE commit for the v1 PR: **this cascade/meridian-scroll
stream** (deep, home hero only — now committed as `bdea06e`) + **pass-2 site-wide
refinements** (`swm-pass-2` worktree, on `:4399`). Fold to ONE commit after BOTH
approved → squash-merge to `feature/v1-launch`. Neither pushes to v1 alone.

## Blockers

- None on this branch. The globe is shipped + committed + baked + **APPROVED**. The
  only thing gating the fold/PR is an EXTERNAL dependency: the parallel
  revision-notes-02 (pass-2) crossover items being approved — owned by that session,
  not this one. HR-6 (mobile device gate) is intentionally DEFERRED behind that gate;
  Nathan runs it himself when the time comes. Mobile comp values were NOT re-dialed —
  verify the pole-cap globe reads on iPhone during HR-6, before the PR.
