# Session Context — 2026-08-26 (enter-world choreography round; ALL CODE STILL UNCOMMITTED — now THREE stacked rounds)

> Auto-generated by `/save-session`. Read by `/load-session`.

## Last Session Summary

Third live round on top of the still-uncommitted 08-24/25 rounds (their full
detail: `git log -p docs/.session-context.md`). Fixed the /work → detail
"enter world" transition — the grid was BOWING OUTWARD + shrinking because the
old `ENTER_LENS_SWELL` (+0.32) added to the negative base distortion crossed
zero into barrel warp. Rebuilt the ramp on the commit-choreography model (one
linear timeline, windowed power-inOut channels): lens now DEEPENS (negative)
while a new projection zoom scales grid+tiles UP — a real dive INTO the world.
Two new typed benches shipped (?entertune TR, ?texttune TL — CommitTunePanel
model: number inputs, ▶ dry-run, copy_url, reset). Nathan's motion bake is IN
as defaults. The text-exit choreography (tags/letters hard-cut random, tab
clip, nav wipes, DOM scale on the same curve) rides the same event as the
scene. Nav-centric chips restyled site-wide into one family (accent rest →
white/black hover, --font-ui). Build green (22 pages), all Playwright-verified.

## Completed Work (This Session)

- **Enter-world scene ramp rework** (`world/enterTune.js` NEW +
  `useWorldScene.js`): live `ENTER_TUNABLES` store; channels LENS
  [lensStart,lensEnd] (additive `lens`, NEGATIVE = deeper inside-sphere) and
  MOVE [moveStart,moveEnd] (camera dolly + `camera.zoom` 1→`scale` — the zoom
  is what scales the radius-16 shell grid; dolly alone couldn't). Master
  linear, channels powInOut(`pow`) — `powInOut`/`seg` exported from
  enterTune.js (shared with textExit). detail.dryRun → hold `holdMs`, unwind
  0.6s expo.out. WorldCard reads `enterMs` LIVE at click (legacy ?entercover
  still seeds).
- **Nathan's 08-26 motion bake = the defaults**: enterMs 1100, lens −0.07,
  scale 3.2, dolly 1, moveEnd 2 (open-ended window — steep mid-rise at
  handoff, the recenterEnd-3 idiom), pow 7, holdMs 10.
- **?entertune=1 bench** (`EnterTunePanel.jsx` NEW, top-right on /work):
  11 typed fields + dry-run (cover honors `cover`/?enterfill) + copy_url +
  reset. Params: enterms enterlens enterlensa/b enterscale enterzoom(=dolly)
  entermovea/b enterpow enterhold enterfill.
- **Text-exit choreography** (`work/textExit.js` NEW): fires on the SAME
  'swm:enter-world' (listener armed in FeaturedProjects; DOM queried at FIRE
  time — client:only stale-DOM trap). Service tags + SplitText'd client-name
  chars HARD CUT random order (plain visibility writes + delayedCall clocks,
  Hero lockup-beat convention); PROJECT_## clips top→bottom; [PREVIOUS] wipes
  bottom→up, [NEXT] top→down (clip-path, enter curve); card block +
  nav chips scale on the SAME master/move-window/pow as the scene
  (`--tx-scale` multiplied into .fp-cta transform, transitions suspended
  during the run to dodge the 0.12s low-pass). dryRun restores everything.
  enter_world pill itself is NOT cut (pressed control; cover swallows it) —
  Nathan hasn't asked.
- **?texttune=1 bench** (`TextTunePanel.jsx` NEW, top-LEFT): tagCutMs
  charCutMs tabDelay/tabMs navDelay/navMs cardScale navScale (params tx*).
  Scale TIMING deliberately lives on the enter bench (shared windows).
- **Nav-chip family restyle** (Nathan 08-26): rest = project accent +
  readable ink, hover/scroll = white bg + black text/arrows, font --font-ui
  (Inter, = enter_world), sizes unchanged. Applied: /work [NEXT]/[PREVIOUS]
  (global.css .fp-cta + FeaturedProjects ctaColor lerp accent→white, hover
  charges full, commit-pin now WHITE/black then eases to destination accent);
  detail breadcrumb (accent rest, was near-black); client-panel__next (KEEPS
  near-black rest — sits ON the accent band — family via font+hover);
  process stepnav + back_to_top + phone .process-cta__top (blue fallback
  rest; blue phase → near-black rest; white phase hover → black/white
  inversion — both documented in process.css).
- **Verified** (Playwright vs :4321): wipe timelines sampled per-frame,
  cuts 6→0 tags / 4→4 chars, restore clean, real commit navigates, computed
  styles on /work + detail (COCO accent) + process. `npm run build` GREEN.

## Current Branch & Git State

- Branch: `feature/v1-launch` @ `016b343`, origin in sync.
- **Uncommitted (DELIBERATE — three stacked rounds; commit when Nathan calls
  it):** rounds 1–2 files + this round: useWorldScene.js, worldConfig.js,
  WorldCard.jsx, FeaturedProjects.jsx, global.css, project-detail.css,
  process.css, commit-tune.css + NEW enterTune.js, EnterTunePanel.jsx,
  textExit.js, TextTunePanel.jsx. Untracked strays unchanged (2 root logo
  SVGs, 2 stale PNGs, fable-mode skill).
- Suggested commit shape when blessed: one feat per round, or fold round 3
  into "enter-world choreography + nav-chip family". `git checkout -- .astro`
  first, as always.

## Pending Tasks (Next Session)

1. **Nathan feel-checks round 3**: text-exit rates/wipes on real runs
   (?texttune=1; set ?enterfill=0 to watch uncovered), nav-chip family on all
   three pages (esp. process blue/white phase choices + [PREVIOUS] wipe —
   only [NEXT] was live-verified; prev needs project ≥2), commit-pin
   white/black call. Decide: cut the enter_world pill too during text-exit?
2. Rounds 1–2 gates still open: `?lockupbeat` pacing, deck-wall feel,
   hero-deck 70dvh, SA bottom wall, hurry-up-slowly incidental hero deck.
3. **Commit the three rounds** once blessed (re-run build + mobile pass;
   lockuph 400 + HR-6 still deferred).
4. **Deploy note**: bedouin CMS changes are live in the dataset; deployed
   SSG site needs the new code — ship both together.
5. Standing backlog: projectColor hexes 11/13, 2 rev-notes-03 polish calls,
   hosted-Studio sync-or-retire, VideoSlotPool perf, Notion notes-03 label,
   mobile brand-arrival feel-check.

## Key Files Reference

| File | Purpose |
|------|---------|
| `src/components/work/world/enterTune.js` | ENTER_TUNABLES live store + powInOut/seg (shared channel math), URL seed/copy. Defaults = Nathan's 08-26 bake. |
| `src/components/work/world/useWorldScene.js` | Enter ramp (lens deepen + camera.zoom/dolly, dryRun unwind) ~L255-322; applyLens uses ENTER_TUNABLES.lens. |
| `src/components/work/textExit.js` | TEXT_TUNABLES store + runTextExit/cancelTextExit (cuts, clips, wipes, --tx-scale). |
| `src/components/work/EnterTunePanel.jsx` | ?entertune=1 bench (TR). |
| `src/components/work/TextTunePanel.jsx` | ?texttune=1 bench (TL). |
| `src/components/work/FeaturedProjects.jsx` | Panel gates + text-exit listener + reworked ctaColor (accent→white lerp). |
| `src/components/work/WorldCard.jsx` | enterWorld reads ENTER_TUNABLES.enterMs live. |
| `src/styles/global.css` | .fp-cta family skin (accent rest, --font-ui, --tx-scale in transform). |
| `src/styles/project-detail.css` | breadcrumb accent rest + white/black hover; client-panel__next exception. |
| `src/styles/process.css` | stepnav/back-to-top family skin + blue/white phase overrides. |
| `src/styles/commit-tune.css` | Shared bench chrome + --tr/--tl corner modifiers. |

## Decisions & Context

- **Lens sign doctrine**: base distortion is NEGATIVE (inside-a-sphere);
  enter must push MORE negative. Positive additive = outward barrel bow +
  shrink (the fixed bug). ?enterlens flipped sign semantics with the fix.
- **?enterzoom still means the dolly** (historic); the projection zoom is
  ?enterscale. Legacy ?entercover seeds enterMs.
- **One master timeline, windowed pow channels** is the house commit model
  (Hero.beginEnvelopment); enter scene ramp AND text-exit share ENTER_TUNABLES
  timing (enterMs/moveStart/moveEnd/pow) so DOM + WebGL can't drift.
- **Nav-chip family**: accent rest / white-black charged / --font-ui, with
  two sanctioned exceptions where the field matches a state (chip-on-accent
  → near-black rest; process white phase hover → black/white).
- **Bench corners on /work**: entertune TR, texttune TL, fp1tune+deckdebug BR.
- Playwright gotcha (cost 30s of confusion): locator.evaluate AUTO-WAITS on
  missing elements (.fp-prev at first project) — use page.$ for optionals.
- Benches by corner elsewhere: herotune TR, committune BR (home), lenistune
  BL, footertune TL. Playwright via
  `require('/opt/homebrew/lib/node_modules/@playwright/mcp/node_modules/playwright')`
  + `chromium.launch({channel:'chrome'})`.

## Blockers

None. Everything verified live; build green. Gate = Nathan's eye on the three
stacked rounds.
