# Session Context — 2026-09-08 (late): logo ticker + footer choreography + overlays round — all PUSHED @ 5eea54a

> Auto-generated by `/save-session`. Read by `/load-session`.

## Last Session Summary

A two-day desktop + mobile round on the site chrome. Shipped the client-logo
ticker riding the top of the links footer (static assets via a prep script,
optically balanced, tight-cropped, on the globe's drag + momentum engine),
rebuilt the footer choreography (home reveals it on scroll, masked exits,
the tagline pill raises it IN PLACE as an overlay with click-outside close,
a house-glide wipe), unified every overlay wipe (inquiry / mobile menu /
privacy — up from the bottom, inverse out, never a fade), turned the
privacy page into an overlay whose pill is the close, restructured the
mobile footer corner (lockup + © group, privacy pill twins), and started the
process-page refinement (keyword highlight wipe, footer baked into the last
slide, full-bleed scrim off, bottom-anchored phone cards, no text shadows).
Eight commits `d86df6f` → `5eea54a`, all pushed; branch level with origin.
**Next session = refining the process page** (Nathan).

## Completed Work (This Session)

- **Client-logo ticker** (`d86df6f`, `fa0936d`): `ClientLogoTicker.jsx`
  rendered by `SiteFooter` as the first child of the links panel on all
  three footer routes (Nathan). Copy "Utilized by a full spectrum of
  industry" + CSS-keyframe odometer (artists/promoters/labels/agencies/
  venues), ONE row of 40 logos. Assets: intake `Client Logos/` (untracked,
  gitignored) → `scripts/prep-client-logos.mjs` (sharp: alpha-bbox crop for
  rasters AND SVGs (viewBox rewritten), ≤240px, every file white-on-
  transparent, kebab-case, deny-list, `manifest.json` with intrinsic dims)
  → tracked `src/assets/client-logos/`. Optical balance: height ×
  (3 ÷ aspect)^0.5 (`?logobal ?logoref`). Homegrwxn re-sourced from Dropbox
  (the intake file had an arrow). Copy line = body face at `--weight-medium`
  (the enter_world token — .cta-primary is 500, not 700).
- **Ticker on THE drag + momentum engine** (`b7b51fa`):
  `src/lib/dragMomentum.js` extracted from the globe's InteractionController
  (EMA 0.7 flick sample, per-axis cap, 0.35s power2.out settle to ambient);
  globeConfig re-exports from `DRAG_CHOREO`. The ticker rolls at ambient
  −`--logo-pxs`, drag 1:1, flick on the globe's cap in px, wraps modulo one
  track width (JS strip, CSS keyframe roll gone).
- **Footer choreography** (`b7b51fa`, `2783c8b`, `11e077a`, `af69ba3`):
  · HOME reveals the footer on scroll/swipe (driven mode, Lenis off on `/`,
    `SCROLL_TRIGGER_HOME_PX`; enter_world retracts first).
  · MASKED EXITS: `--footer-peak` broadcast (progress's peak since park);
    the ticker fade reads it; link stagger + tagline stack drop their
    retreat reverse and snap on park; the tagline stack (fixed island) is
    clip-path'd per frame at the panel's live top edge.
  · TAGLINE PILL = a button dispatching `swm:footer-reveal`; the footer
    rises IN PLACE as an overlay (no scroll) on the house glide
    (`wipeReveal`, World Turn curve); `data-footer-invoked`; a pointerdown
    outside closes it (scroll mode wipes back to the scroll-implied
    progress; driven hosts get `swm:footer-close`).
  · MOBILE: `.site-privacy` shows lower-right one size up (`--text-pill`)
    and `.mobile-menu__privacy` is its pixel twin; the footer's lower-left
    is the LOCKUP + © group (absolute, snug, bottom ON the privacy text
    bottom; `--menu-pill-seat` = inset + pill-pad-y); the rising footer
    inverse-masks the tagline pill (`maskPill`).
- **One overlay wipe** (`af69ba3`): `src/lib/overlayWipe.js` — up from the
  bottom edge on the Turn curve, inverse wipe out. Consumers: the project
  inquiry overlay (FLIPPED — it grew top-down before), the mobile menu (was
  a 0.3s fade), the privacy overlay.
- **Privacy overlay** (`af69ba3`): `PrivacyOverlay.jsx` in the SiteTagline
  island UNDER its pill (z 44 < 46) — the pill IS the close (`close ×`,
  info-pill skin, aria-expanded); Escape + route swap close; SiteShell folds
  `swm:privacy-state` into `data-chrome-open`; `/privacy` renders the same
  `PrivacyContent.jsx`; styles in global.css `.privacy*`. The mobile menu's
  privacy opens it via `swm:open-privacy`.
- **Inquiry overlay lockup** (`5eea54a`): the SWM lockup replaces the
  top-left globe at the NAV BAR's seat (desktop left seat / ≤768 centred).
- **Process page, round 1** (`fa0936d`, `11e077a`): keyword highlight wipe
  (`src/lib/keywords.jsx` — `[[word]]` markers, absorbs glued punctuation,
  box + ink mask, `--kw-bg/--kw-ink` per field; process blurbs marked,
  detail blurb wired via `useKeywordWipe` but Sanity descriptions NOT yet
  marked); the footer + ticker baked into the last slide (driver's final
  rest = document end, `travelK={1}`, `--footer-panel-h`); phone scrim
  full-bleed + OFF (`?scrim`); phone cards bottom-anchored
  (`--ps-mobile-bottom`); every text-shadow removed.
- **Trap learned**: a `button.class` reset with `font: inherit` outranked
  the `.class` size rules and shrank the phone tagline to 14px — resets
  live at the class's own specificity now.
- Docs: tunables guide 286 params, `--check` PASS; launch plan row; memory
  notes `client-logo-ticker`, `keyword-highlight`, `drag-momentum-one-place`,
  `overlay-wipe-one-place`.

## Current Branch & Git State

- Branch: `feature/v1-launch` @ `5eea54a` — **level with origin (pushed)**.
- Last 5 commits: `5eea54a` inquiry lockup at the nav seat · `af69ba3`
  in-place footer + privacy overlay + one overlay wipe + mobile group ·
  `11e077a` tagline clip + process shadows · `2783c8b` pill wipe ·
  `b7b51fa` home footer + masked exits + drag engine + privacy twins.
- Uncommitted: only `.astro/*` (generated) + this file + the plan row
  (commit as `docs(session)` after). Personal untracked strays (leave
  alone): root logo SVGs, reference PNGs, `.claude/skills/fable-mode-main/`,
  `scripts/shots/`. `Client Logos/` is gitignored intake (44 files).
- Netlify branch preview rebuilds from `5eea54a`. Dev: `npm run dev --
  --host 0.0.0.0 --port 4321` → `http://192.168.1.9:4321/`.

## Pending Tasks (Next Session)

1. **Refine the process page** (Nathan's stated next). Round 1 landed the
   keyword wipe, last-slide footer, scrim off, bottom-anchored phone cards,
   no shadows — Nathan's read of the preview drives round 2. Knobs:
   `?scrim`, `--ps-mobile-bottom`, keyword markers in
   `processContent.js`, `--kw-bg/--kw-ink` per `data-bg` in process.css.
2. **Mark keywords in the Sanity `description` fields** (`[[word]]`) so the
   detail-page highlight has something to wipe — nothing marked yet.
3. **Logo intake loose ends**: `wikka_logo.jpg` needs a transparent export
   (denied); `sound-logo-web-sm.png` = which client?; Imperfect Records /
   Munchietown / Sunday Brunch / Off Menu still expected — drop in
   `Client Logos/`, run `node scripts/prep-client-logos.mjs`, commit the
   output. Sanity migration of logos "later once finalizing".
4. **Watch-items from this round**: home's nav bar slides up with the
   footer reveal (takes the brand lockup off screen while the footer is up)
   — eyeball; the inquiry overlay now wipes bottom-up (flipped); the info
   drawer still slides (y) — not on the overlay wipe (Nathan didn't ask).
5. Standing queue: `/work` mobile fill-ticker idiom decision; tunables
   latent-bug tickets; mobile at-machine items; scroll-trigger field pass;
   launch runway (F15 analytics, F9 form, merge to main).

## Key Files Reference

| File | Purpose |
|------|---------|
| `src/components/ClientLogoTicker.jsx` | the logo band: glob + manifest loader, odometer, JS strip on `DragMomentum`, dial effect |
| `scripts/prep-client-logos.mjs` | intake → `src/assets/client-logos/` (crop, whiten, manifest); `--check` |
| `src/lib/dragMomentum.js` | THE drag + flick physics (`DRAG_CHOREO`); globe `InteractionController` is a skin |
| `src/lib/overlayWipe.js` | THE overlay wipe (`wipeIn`/`wipeOut`); inquiry, mobile menu, privacy |
| `src/lib/keywords.jsx` | `renderKeywords` / `kwWipe` / `kwSet`; `[[markers]]` |
| `src/components/SiteFooter.jsx` | modes, `--footer-reveal/-peak/-panel-h`, `FOOTER_REVEAL/CLOSE_EVENT`, `wipeReveal`, invoked overlay, `travelK`, ticker mount |
| `src/components/SiteTagline.jsx` | pill button, masked exits + `maskToPanel`/`maskPill`, privacy pill + `PrivacyOverlay` mount |
| `src/components/PrivacyOverlay.jsx` · `PrivacyContent.jsx` · `src/pages/privacy.astro` | the notice, one source |
| `src/components/Hero.jsx` | home driven footer (wheel/touch accumulator, pill, close) |
| `src/components/process/{ProcessPage.jsx,useProcessCopy.js,useProcessScrollDriver.js,processContent.js}` · `src/styles/process.css` | the process page — next session's surface |
| `src/styles/global.css` | `--logo-*` tokens, `.logo-ticker`, `.kw`, `.privacy*`, `.site-privacy` + menu twin, mobile tagline group, `.project-overlay__lockup` |
| `docs/tunables-guide.md` | 286 params; `node scripts/tunables-keys.mjs --check` is the gate |

## Decisions & Context

- **One place per choreography** (Nathan): drag/flick = `dragMomentum.js`;
  overlay wipes = `overlayWipe.js`; footer wipe = `wipeReveal` on the World
  Turn curve. Never re-inline the numbers.
- **Footer elements never fade out** — exits are masked by the panel's top
  edge (`--footer-peak`, per-frame clip for the fixed tagline stack).
- **Overlays never fade** — wipe up from the bottom, inverse wipe out.
- **Logos static now, Sanity later**; every logo FILE is white-on-
  transparent and cropped to its ink (the prep script does it; never
  hand-edit the output folder).
- **Privacy pill is the close** for the privacy overlay (info-pill
  convention); the overlay sits under it in the SiteTagline island.
- **Process page**: services tags stay off the scale (09-07); keyword
  markers are the authoring surface for highlights; scrim OFF by default.
- **Prettier**: no repo config; never `--write`.
- **Deploy law**: production = merge to `main`; branch pushes rebuild the
  preview. Everything is pushed.

## Blockers

None. Next session opens on the process page with Nathan's read of the
preview.
