# Session Context — 2026-07-12

> Auto-generated by `/save-session`. Read by `/load-session`.

## Last Session Summary

All-documentation session (remote/cloud, no local machine): produced the complete, grilled design documentation for the **process page** — the v2 workstream deferred at launch (`docs/v1-launch-plan.md` F2). Ran the `grill-with-docs` workflow with Nathan across four rounds (16 decisions), including a mid-grill **redesign of Stages 1–2** (the three-globe trio was cut in favor of Fragment-belt → Thread → Core assembly). Everything is committed, solidified, pushed, and open as **PR #14**. **No code was written — next session is the build.**

## Completed Work (This Session)

- **`docs/process-page-spec.md`** — build spec, *Grilled & solidified 2026-07-12*: user flow, page composition (fixed full-viewport canvas + ~40ch overlay copy column on a Lenis document route, `body.route-process`, brand-black field), complete copy deck (hero `THE_PROCESS` / `FROM CORE TO WORLD`, five ≤45-word Stage blurbs, snake_case chrome, `⊙ process` nav link, `↳ start_project` + `⁕ featured_projects` close), the five-Stage **ProcessScene** machine, **the Thread**, scroll choreography, 3-part route gating, RM/mobile paths, perf budget, 13 `?knob` tunables, build order, 12-item acceptance checklist.
- **`docs/process-page-plan.md`** — phased **P0–P6** build plan (status table, all ☐), deliberate cuts, launch gate.
- **`docs/adr/0004-process-scroll-driver.md`** — the process narrative is **ScrollTrigger-driven and time-based** (not accumulator-paged, not scrubbed); first ScrollTrigger in the codebase, island-scoped; accumulator stays the idiom for single-commitment gestures.
- **`CONTEXT.md`** — new "Process Page — concepts" (Fragment, the Thread, the Core, Stage + bare-**World** disambiguation), `ProcessPage`/`ProcessScene` component-vocabulary rows, `/process` route-map row.
- **Verification passed:** docs-only diff (+271/−1 across 4 files); all 25 cited paths exist at HEAD; load-bearing line refs match verbatim; format lints against house exemplars; no `_Avoid_` aliases used; conventional `docs(scope):` commits.
- **PR #14 opened:** `claude/process-page-design-plan-rx7y2k` → `feature/v1-launch` — https://github.com/smallworldmedia/smallworldmedia-landing-page/pull/14

## Production Scorecard

> ⚠️ Carried forward from 2026-06-24 (this remote session has no `.env.local`; Sanity unreachable). Manifests re-counted fresh. Re-run `/save-session` locally to refresh GROQ counts — note ws3 content work since (HHS `releaseInfo` ×15 published).

| Metric | Count |
|--------|-------|
| Clients | 59 *(2026-06-24)* |
| Service Tags | 15 *(2026-06-24)* |
| Media Assets | 784 *(2026-06-24)* |
| Featured Projects (`isFeatured`) | 16 *(2026-06-24)* |
| Manifests | 82 *(fresh)* |
| Affiliations wired | 0 |

## Current Branch & Git State

- Branch: **`claude/process-page-design-plan-rx7y2k`** — based on `feature/v1-launch` @ `0cb7823`, **pushed to origin**, PR #14 open against `feature/v1-launch`.
- Session commits (tip `9ec94b1`):
  - `9ec94b1` docs(process): solidify spec + plan — grilled 2026-07-12
  - `88a1894` docs(context): process-page vocabulary — Fragment, Thread, Core, Stage
  - `bbfc0cf` docs(process): grill rounds 2+4 — Fragment assembly replaces the trio; vocabulary locked
  - `0044eda` docs(adr): 0004 — process narrative is ScrollTrigger-driven, time-based
  - `476a28e` docs(process): grill round 1+3 — driver, composition, copy registers locked
  - `49375f6` docs(process): draft process-page spec + build plan (pre-grill strawman)
- Uncommitted changes: none (this file is committed with the session-save).

## Pending Tasks (Next Session — the process-page BUILD)

Work the phases in `docs/process-page-plan.md` order; the spec (`docs/process-page-spec.md`) is the contract — build to its acceptance checklist.

1. **Branch/base decision first:** if v1 has launched (PR `feature/v1-launch` → `main` merged), rebase/restart the build branch from `main`; otherwise continue on this branch or branch off it. Merge PR #14 (or fold its docs) before or with the build.
2. **P0 — route scaffold + gate:** `src/pages/process.astro` (redirect stub + `v2:` re-enable comment) + `netlify.toml` forced 302 + `SITEMAP_EXCLUDE` entry + `ProcessPage.jsx` island + `route-process` body class + `processContent.js` stub + define missing tokens (`--tracking-tight: -0.02em`, `--lh-body`, promote `--color-near-black` to `:root`) + fix the stale `GAP_COLOR` comment (`globeConfig.js:88`).
3. **P1 — scroll-driver skeleton:** ScrollTrigger (island-only) + Lenis bridge (`getLenis()?.on('scroll', ScrollTrigger.update)`; refresh on `astro:page-load`/fonts) + 5 stub sections + stage machine logging `goTo` states.
4. **P2 — scene core (S3–S5):** `useProcessScene` on the 84-mesh globe — `rows` light-up (`uFallbackColor` + `uPower` cascade), emanation (`?emanate`, `?emanateorder` sweep), `?bpm` 122 rhythm loops.
5. **P3 — Fragments + Thread + assembly (S1–S2):** seeded belt (`seededLayout.js` PRNG), Thread chain + captions, drift damping, pull-in assembly + inner-sphere reveal, S2→S3 zoom-out.
6. Then P4 (copy + chrome + arrival) → P5 (RM/mobile/perf device pass) → P6 (nav re-add + 3-file un-gate) per the plan.

## Active Phase

**Process page (v2): documentation ✅ complete — build ☐ not started.** All P0–P6 unchecked in `docs/process-page-plan.md`.
Elsewhere: v1 launch gate (ws6 real-device pass) was still ☐ in `docs/v1-launch-plan.md` as of `0cb7823`; CMS Phase 13 (Affiliation Wiring) still not started.

## Key Files Reference

| File | Purpose |
|------|---------|
| `docs/process-page-spec.md` | **THE build contract** — stages, copy deck, tunables, acceptance checklist |
| `docs/process-page-plan.md` | P0–P6 phase order + cuts + launch gate |
| `docs/adr/0004-process-scroll-driver.md` | Scroll-driver decision (ScrollTrigger, time-based) |
| `CONTEXT.md` § "Process Page — concepts" | Canonical vocabulary: Fragment / the Thread / the Core / Stage |
| `src/components/globe/buildGlobeGeometry.js` | Panel geometry brain (84 records w/ `row`/`lonIndex`/`centerDir`) — reuse, don't fork |
| `src/components/globe/panelMaterial.js` | Per-panel shader (`uFallbackColor * uPower` untextured) — no changes needed |
| `src/components/globe/cascade.js` | Stagger engine (`rows`/`poles`/`sweep` + flicker keyframes) — S3/S5 driver |
| `src/components/globe/globeConfig.js` | Constants + `PARAM()` knob reader + RM/mobile gates (`GAP_COLOR` comment is stale — value is blue) |
| `src/components/work/world/worldConfig.js:78` | `TURN_EASE_PATH` — the house CustomEase for every transition |
| `src/components/work/world/seededLayout.js` | `mulberry32`/`hashSeed` — Fragment-belt scatter PRNG |
| `src/lib/smoothScroll.js` | Lenis singleton (`getLenis()`; `/process` inherits smooth scroll automatically) |
| `src/components/RouteFill.jsx` | `swm:fill-release` on mount (arrival choreography hook) |
| `src/pages/specimen.astro` + `netlify.toml` + `astro.config.mjs:8` | The 3-part disabled-route pattern to copy for `/process` |

## Environment

- This session ran in a **remote container** (fresh clone; only `main` cloned initially — `git fetch origin '+refs/heads/*:refs/remotes/origin/*'` to see all branches). No `.env.local` → no Sanity/Mux access (not needed: docs-only).
- The process page needs **no CMS/media access** to build — untextured scene, static copy in `processContent.js`.
- Local dev unchanged: `npm run dev` (Astro :4321), Studio at `/studio`, Sanity `b60h4u7o`/`production` via `.env.local`.
- Live tuning on the gated route: `/process?debug` + the spec §9 knob set (`?scatter ?drift ?threadhops ?threadms ?assemble ?zoomout ?emanate ?emanateorder ?bpm ?cascade ?fillfrac ?stagems`).

## Decisions & Context

- **Stage 1–2 redesign (Nathan, grill round 4):** the three-globe trio was **cut** — presenting three candidate worlds implies SWM always pitches three concepts. Replaced by: a belt of ~84 drifting **Fragments** (the raw references) → **the Thread** shoots from the would-be center and connects the dots → connected Fragments **pull inward and construct the Core** — one unified concept built from what the client gives us. Belt and globe are the **same 84 meshes** (draw calls flat ~85, zero textures).
- **Vocabulary:** experience is plainly **"Process"** (worlds motif lives in visuals/copy only); **the Core** is Nathan's term for the assembled globe; Stages tokened `STAGE_01…05`; never bare "World" (reserved for `/work`).
- **Motion:** discrete time-domain transitions on the house `TURN_EASE_PATH` curve fired at ScrollTrigger boundaries (no scrub/pin/snap — ADR-0004); exits ≈0.7×; S3 cascade default **`rows`** (home hero keeps `sweep`); S5 rhythm on `?bpm` 122.
- **Copy registers:** chrome = snake_case mono, display = squeezed caps, prose = sentence case w/ em-dashes + triads; blurbs ≤45 words / ≤3 sentences (acceptance item). H1 `FROM CORE TO WORLD` derives from Nathan's "from core concept to full world, building from the inside out."
- **Architecture lineage:** route-scoped canvas (ADR-0002 — third scene in the site), pure-brains/thin-consumer reuse of globe primitives (ADR-0003 grammar) — `useProcessScene` composes `buildGlobeGeometry`/`createPanelMaterial`/`buildCascadeTimeline`, never forks `useGlobeScene`.
- **Corrections ledger (don't re-propagate):** `momentum.js` is dormant (nothing imports it); `globeConfig.js:88` comment says black but the value is electric blue; `.claude/skills/gsap-swm` is stale (Next.js/ScrollTrigger-era) — trust shipped code; `CtaButton.jsx`/`InfoPill.jsx` are dead code.

## Blockers

- **None for the build itself.** Watch-outs: (1) rebase target depends on whether v1 has merged to `main` by build time; (2) un-gating at P6 is **3 files in one commit** (redirect + netlify 302 + sitemap exclude); (3) real-device pass (P5) needs local hardware — can't be done from a remote container.
