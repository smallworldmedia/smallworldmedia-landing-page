# Session Context — 2026-08-27 (fp-grid WORKTREE: all three media-in-grid iterations SHIPPED)

> Auto-generated by `/save-session`. Read by `/load-session`.
> This is the `smallworldmedia_fp-grid` WORKTREE's context (branch
> `refactor/fp-grid-media`, based on the DO-NOT-MERGE snapshot de49839).
> The main checkout (`smallworldmedia_landing-page`, :4321) keeps its own
> session context — do not confuse the two.

## Last Session Summary

The full "media lives IN the grid" creative refactor, plan → approval → all
three iterations, in one session. Proposed three directions (workflow: 3
creative-lens generators + 3 adversarial code-verifiers, corrections folded
in), Nathan approved with: default mode 1, DRUM idle-creep as a live toggle,
house tokens/curves/timings everywhere. Built ATLAS (`?fpgrid=1`, on-sphere
plates behind the graticule), FORME (`?fpgrid=2`, letterpress pane + re-deal
wave Turn), DRUM (`?fpgrid=3`, one revolving rigid body, 60° arcs). All
Playwright-verified on coco + bedouin, live video intact in every mode,
`?fpgrid=0` legacy regression-free, build green (22 pages).

## Completed Work (This Session)

- **Plan** (`docs/fpgrid-plan.md`, c40c89a → status header 982b5aa): three
  directions with mechanisms, distinctness matrix, verify protocol, live-knob
  reference; adversarial feasibility verdicts folded in.
- **Shared foundation** (`fpGridCells.js`): fine-cell math (250×230 shell →
  1.44°×0.78° cells), aspect-aware lens-cropped angular window (`?fpwin` 0.78),
  ratio→whole-cell span, seeded annulus placement with card-rect keep-out
  (CARD_HALF = CENTER_CLEAR·0.6) + ring-walk resolution (3 outward layers,
  DROPS a block over corner pileups — empty cells are composition), sector/
  border geometry helpers (1 segment per cell — edges land on lattice lines).
  `FPGRID` mode switch (`?fpgrid`, default 1); SHELL_SPIN → 0 in grid modes.
- **ATLAS** (`fpAtlas.js`, f11b6a8): sphere-sector plates at R+0.25 (behind
  the lines — "pressed under glass"), accent border rings at R−0.1 winning the
  shared edge, explicit renderOrder contract (transparent painter-sort is
  distance-~0 for everything sphere-centered), BackSide U-flip cover-fit,
  on-sphere slerp appear (planar lerp would slide plates off the sphere),
  register plate for deck/album (two stacked sectors, meridian clip-wipe on
  BAND_CYCLE_S + turnRollEase). Turn = existing pivot roll UNCHANGED (plates
  roll over the fixed graticule, snap-register on landing). Parallax = camera
  head-turn (`?camlook`).
- **FORME** (`fpForme.js`, d7d0cf6): scene-level planar lattice at z −3.8,
  pitch = 3× shell fine pitch (`?panepitch` — pitch-copying moirés against the
  shell), shell dimmed ×0.45 behind. Blocks + rule frames w/ quoin ticks on
  CTA ink states (white → accent by alloc), furniture (9 registration crosses,
  5 cell-coordinate captions, 3 accent floods, idle blink) — accent-inked,
  per-slot, re-deals with the edition. Turn = dealloc/alloc wavefront
  (windowed per-block gates over ONE eased master inside goToWorld —
  finishTurnInstant progress(1) contract kept), cell-row-quantized clip-plane
  wipes (always-attached planes parked at PASS_ALL — no shader recompiles;
  video overlays keep DECODE_VIDEO_TEXTURE). Signature deck block + mono folio
  (canvas ink textures). Parallax: pane one-body drift + lensPass
  principalPoint lean + texture-offset ink breathing.
- **DRUM** (`fpDrum.js`, 6fde3fb): hierarchy scene → parallax → roll → body
  (Rz π/2, poles → ±X offscreen) → shell + both slot pivots. 60° arcs
  (`?arcdeg`; 120 left a bare-grid beat — 60 keeps the conveyor continuous),
  Turn tweens roll to ABSOLUTE index×ARC targets (×1.15 duration `?drumturn`),
  `?creep` deg/s idle toggle (default 0) absorbed by next Turn's absolute
  target. Plates INSIDE the lines at R−0.12 (third stroke treatment: media
  replaces its cells), UV-remapped sectors ((su,sv)→(1−sv,1−su)) so plateCover
  works unchanged. Drum-frame placement (axis roles swap: local lon =
  screen-vertical). Register plate reused via new hooks (geometryFor,
  orientPlane→world clip, orders, borderRadius, spawnTarget).
- **worldLive seam**: records may carry `makeOverlay(video)` → curved/ordered
  overlays; legacy flat-plane path untouched. Live verified: 4/4/3/4 streams
  live on modes 0/1/2/3 (FORME dropped 1 video block to ring saturation —
  acceptable), overlay animation confirmed frame-diffed.
- **Infra**: Sanity CORS + `http://localhost:4322` (image CDN 403s unlisted
  origins, `vary: Origin`; :4321 was already allowed). Memory saved
  (`fpgrid-media-in-grid`).

## Current Branch & Git State

- Branch: `refactor/fp-grid-media` (worktree; never touch the sibling main
  checkout; no remote push expected until Nathan blesses a direction).
- Last 5: 982b5aa docs status · 6fde3fb DRUM · d7d0cf6 FORME · f11b6a8
  ATLAS+foundation · c40c89a plan.
- Uncommitted: only `.astro/data-store.json` churn (regenerated;
  `git checkout -- .astro` before commits, as always).
- Build: GREEN (22 pages). Console clean in all four modes.

## Pending Tasks (Next Session)

1. **Nathan's feel pass** on all three (`/work` on :4322, `?fpgrid=1|2|3`,
   legacy `?fpgrid=0`): composition balance per project (seeded —
   `?platedeg` `?fpwin` `?max` reshape live), ATLAS plate scale, FORME frame
   weight + furniture density + flood alpha, DRUM creep on/off + rate
   (`?creep=0.5` to taste) + arc size. Then likely: pick a winner or hybridize.
2. **Enter-dive re-bake for on-sphere modes** (?entertune): dolly is ~4×
   weaker against radius-16 geometry — the dive is zoom-dominant; needs
   Nathan's motion bake per mode (or accept current).
3. Polish backlog (deliberately deferred): chrome keep-outs for [PREVIOUS]/
   [NEXT] bands (blocks may sit under top/bottom-center chrome), ATLAS
   survey-flicker idle beat, FORME accent-flood visibility check on bright
   accents, DRUM N+2 arc pre-dressing, mobile pass (HR-6-style gate).
4. If a direction is blessed → tune bench (?fpgridtune panel, house
   CommitTunePanel model) instead of raw URL params.

## Key Files Reference

| File | Purpose |
|------|---------|
| `docs/fpgrid-plan.md` | The three directions + STATUS header + live-knob reference |
| `src/components/work/world/fpGridCells.js` | Shared cell math: window, spans, seeded ring placement, sector/border geometry |
| `src/components/work/world/fpAtlas.js` | Mode 1 + shared plateCover/spawnQuaternion/createRegisterPlate (hook-generalized) |
| `src/components/work/world/fpForme.js` | Mode 2: lattice, blocks/frames/furniture, wave apply, signature block |
| `src/components/work/world/fpDrum.js` | Mode 3: drum hierarchy, drum-frame placement, UV remap, arc dressing |
| `src/components/work/world/useWorldScene.js` | Mode branches: buildSlot, applyParallax, tick parallax, goToWorld Turn variants, S2 tint |
| `src/components/work/world/worldLive.js` | `tile.makeOverlay` seam (curved video overlays) |
| `src/components/work/world/worldConfig.js` | FPGRID + all fp-grid knobs (top of file) |

## Decisions & Context

- **Nathan's calls (08-27)**: default `?fpgrid=1`; DRUM creep = live toggle
  default still; reuse house tokens/curves/timings everywhere (done:
  turnRollEase/TURN_DURATION for all transits, TILE_APPEAR/FANOUT for
  arrivals, BAND_CYCLE_S for decks, S2 accent ingestion, CTA ink states).
- **renderOrder doctrine per mode** (everything is transparent AND
  sphere-centered → painter sort useless): ATLAS plates(10) < overlays(20) <
  shell(30) < borders(40); FORME floods(32) < lattice(34) < captions(35) <
  media(40) < overlays(42) < frames(44), shell 30 behind all; DRUM shell(30)
  < plates(32) < strips(33/34) < overlays(36) < borders(40).
- **Placement doctrine**: seeded phyllotaxis annulus survives verbatim; block
  bodies clear a CARD rect (not an infinite ellipse); conflicts walk the ring
  (radius-preserving, deterministic, zero rand draws), retry 3 outward
  layers, then DROP. Strips/signatures claim cells first at the live
  ?bandx/?bandy anchor.
- **Traps hit** (cost real time): drum roll SIGN must be derived against the
  Rz(π/2) body mapping with a concrete point (first cut rolled both arcs out
  the bottom = "empty world"); Sanity image CDN enforces per-origin CORS
  (403 + vary: Origin) — new dev ports need `add_cors_origin`; verify
  scripts must live in the scratchpad (repo is type:module → require fails);
  `page.mouse.wheel` misses the fp wheel handler — page via the pager
  buttons (`button:has-text("03")`).
- Playwright: bundled at
  `/opt/homebrew/lib/node_modules/@playwright/mcp/node_modules/playwright`,
  `chromium.launch({channel:'chrome'})`; scripts + screenshots in the session
  scratchpad. Debug dumps: `?debug=1` → `window.__fpAtlas` / `__fpDrum` /
  `__worldLiveStats()`.
- Dev server: `npm run dev -- --port 4322` (nohup'd; :4321 belongs to the
  main checkout). It died once mid-session — restart + re-verify before
  trusting a blank screenshot.

## Blockers

None. Gate = Nathan's eye on the three modes.
