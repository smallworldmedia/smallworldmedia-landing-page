# Session Context — 2026-08-29 (fp-grid WORKTREE: FOLD-READY — resize doctrine + live globe-O shipped)

> Auto-generated by `/save-session`. Read by `/load-session`.
> This branch (`refactor/fp-grid-media`) is FOLD-READY: history was rewritten
> (content-lossless, tree-hash-verified) so the old DO-NOT-MERGE base is now a
> proper landing commit. THE NEXT SESSION runs in the MAIN checkout
> (`smallworldmedia_landing-page`, :4321) and executes the FOLD-IN RUNBOOK
> below. This doc rides the merge and becomes that checkout's session context.

## Last Session Summary

Environment fixed for good (node_modules excluded from Dropbox sync on all
five Website checkouts; real dev server back after fileproviderd's ~2.6h
de-sync). Shipped: bedouin's second deck wall (hero-led wall-deck order),
`?vtiles` 6 + `?wallgear` 12 bakes, the two-tier RESIZE DOCTRINE on all three
3D pages (buffer instant, camera chases on `?camlag`, heavy work waits for
settle — `src/lib/settleResize.js`), the /work black backdrop + black root,
and the /process globe-O taken LIVE through three rounds to lockup fidelity
(home scroll-globe at rest, pole treatment intact, spin −20, ring 3, pad
0.175). An 11-finding adversarial review of the process resize work was
fixed. Nathan's own `featured-projects.css` tag pass committed separately.
History rewritten for the fold; runbook below.

## Completed Work (This Session)

- **Dropbox wedge ROOT-FIXED**: `com.dropbox.ignored=1` on all five
  `Website/*/node_modules`; dev server verified on :4322. TRAP for next time:
  right after heavy fileproviderd churn, metadata re-stamping can read as an
  astro "config changed" self-restart and the dying listener holds the port.
- **Second deck wall** (9b43054): deck-heavy Worlds surface `brandDecks2` in
  the album-art anchor when free (bedouin: saga + bedouin-brand). Wall decks
  re-lead with the HERO's own deck group (buildContentFlow skips the hero, so
  a deck-page hero swallowed its group's first appearance — hbd had taken the
  wall). `?vtiles` desktop bake 6 (all-video showcases fill via pool
  rotation; bedouin 4/8 → 6/6, dropped 0). `?wallgear` bake 12.
- **Resize doctrine** (67abc59 + 0a30cce + 140900d + 6e76424):
  - `src/lib/settleResize.js` — shared trailing debounce (`settleMs` after
    the LAST event, `maxWaitMs` caps a storm).
  - /work: buffer instant; camera aspect chases via retargeted power3.out
    (`?camlag`, bake 0.7); live video pauses through the gesture, resumes on
    settle; the placed solve is NEVER touched by resize (the repopulation
    experiment was built, then REVERTED at Nathan's call — width density
    `maxTilesFor()` still applies at build/Turn time, `?max` pins it).
  - home globe: first resize fades the canvas (the stroke disc is the rest
    state), settle fades back; `?globestroke` bake 8.2.
  - /process: same doctrine + the mid-transition skip catches up on settle.
    REVIEW FIXES (6e76424): retargetCam re-syncs camLag from the LIVE rig
    (transitions write the camera directly — tweening from stale shadow
    state snapped the camera back); aspect applies even mid-transition
    (position defers, aspect has no other owner); RM stamps one frame;
    goTo/setStageInstant kill in-flight camLag tweens; `?ostroke` clamps ≥0.
- **/work black field**: `?fpfade` bake 0 (accent gradient off) AND
  `html/body:has(.fp)` paint black — macOS fills scale-UP exposure with the
  ROOT background, which flashed the site-wide electric blue.
- **Live globe-O** (140900d → 09e8886 → fd20f92): `liveLockupGlobe.js` = the
  HOME hero globe AT REST — `buildScrollingGlobeGeometry` pinned at scroll-0,
  panel shader's polar path ON (pole pinch/rounded nose/lattice stroke width
  = the lockup SVG construction), `createPanelMaterial` with
  PANEL_CORNER_RADIUS, corner cut painted WHITE (this page runs the mark's
  inverted idiom — white ring, not blue: the field IS electric blue).
  Cascade was built (home cascade family) then RETIRED — rotation carries
  the motion. Bakes from Nathan's dials: `?ospin` −20 · `?ostroke` 3 ·
  `?opad` 0.175 (O clear-air, was the 0.26em CSS literal). Fixed 768px
  backing CSS-scaled into the glyph — resizes never touch GL; creates on
  hero mount, disposes on leave.
- **Nathan's css pass committed** (efedd11): .fp-tag white-on-accent, card
  400px cap, pager chip type.
- **History rewrite for the fold**: base snapshot reworded via
  `git commit-tree` + `rebase --onto` (30 commits replayed clean); final
  tree hash byte-identical (feca9882…). Old history preserved on
  `backup/fp-grid-pre-fold`.

## Current Branch & Git State

- Branch: `refactor/fp-grid-media`, tip **fd20f92**, tree clean.
- New landing base: **16581ad** "feat(site): land the 08-24..26 revision
  rounds" (same TREE as the old de49839 snapshot; de49839 survives only via
  the backup branch).
- feature/v1-launch (main checkout) sits at **016b343** = the landing base's
  parent → the fold is a pure FAST-FORWARD.
- Last 8: fd20f92 opad bake · 09e8886 O pole fidelity · 6e76424 review
  fixes · 140900d live O + doctrine · efedd11 Nathan css · 0a30cce resize
  round 2 · 67abc59 settle-rebuild (later reverted) · 9b43054 deck wall.

## FOLD-IN RUNBOOK (run in `smallworldmedia_landing-page`, branch feature/v1-launch)

The main checkout's dirty working tree is BYTE-IDENTICAL to commit 16581ad's
tree (verified 08-29): its ~27 tracked modifications + 8 untracked source
files ARE the snapshot content, now landed properly on this branch. So the
fold is: prove that's still true, drop the duplicate dirt, fast-forward.

1. **Prove losslessness** (must pass before touching anything):
   `git diff 16581ad --stat -- . ':(exclude)docs/.session-context.md' ':(exclude).astro'`
   → EXPECT exactly 8 files, ALL pure deletions (they're untracked here,
   committed there): src/components/hero/CommitTunePanel.jsx,
   src/components/process/lockupGlobeSnapshot.js,
   src/components/work/EnterTunePanel.jsx,
   src/components/work/TextTunePanel.jsx,
   src/components/work/detail/DeckScroller.jsx,
   src/components/work/textExit.js, src/components/work/world/enterTune.js,
   src/styles/commit-tune.css.
   ANY other entry = un-snapshotted work exists → STOP and reconcile with
   Nathan before discarding anything.
2. **Drop the duplicate dirt** (content-identical to what the merge brings):
   `git restore .` then `rm` the 8 files listed above (they arrive tracked
   via the merge; leaving them blocks checkout). LEAVE the personal scratch
   untracked files alone (`.claude/skills/fable-mode-main/`,
   SWM-globe_white.svg, "Small World Media Logo Inline Blue-01.svg",
   breadcrumb_move.png, homepage_refine_01.png — not in the merge's path).
3. **Fold**: `git merge --ff-only refactor/fp-grid-media` →
   feature/v1-launch = fd20f92. (ff-only is the safety: if it refuses, the
   branch state changed — investigate, don't force.)
4. **Verify**: no `npm install` needed (zero dep changes on the branch);
   `npm run dev -- --port 4321`; check /work (DRUM bake, walls, black
   field), /process (live O), home (stroke ring + settle fade). Build green
   expected (22 pages).
5. **Cleanup (Nathan's call, after he's satisfied)**:
   `git worktree remove "../smallworldmedia_fp-grid"` ·
   `git branch -d refactor/fp-grid-media` ·
   `git branch -D backup/fp-grid-pre-fold` (drops the pre-rewrite history).
6. NOTE: this doc rides the merge and replaces the main checkout's old
   session context (its pre-fold version stays in git history). The shared
   project memory (`fpgrid-media-in-grid`) is already fold-aware.

## Pending Tasks (post-fold)

1. Nathan's continuing taste pass on the shipped bakes (all knobs live).
2. POST-MERGE token sweep (Nathan's standing call): site-wide
   literal→variable sweep, minting tokens where none match (model = the
   pill-line pass). Known fodder: footer/nav 16/22 literals, undefined
   `--color-charcoal` in work.css.
3. Homepage privacy link — still homeless since the hero footer bar retired.
4. Per-release metadata (catalog #, dates, stream links) — tabled with the
   BandPager; needs a home if wanted.
5. Standing deferrals: chrome keep-outs for [PREVIOUS]/[NEXT] bands,
   enter-dive re-bake (`?entertune`), ATLAS idle beat, mobile pass (HR-6
   gate; note IS_MOBILE is a load-time snapshot in worldConfig AND
   processConfig — a live cross-768px resize doesn't re-branch), saga deck
   wall starts at page 2 (its cover IS the bedouin hero — flagged, no call).

## Key Files Reference

| File | Purpose |
|------|---------|
| `docs/fpgrid-plan.md` | STATUS header = the canonical DRUM bake + knob reference |
| `src/lib/settleResize.js` | The site's settle-debounce (two-tier resize doctrine) |
| `src/components/work/world/worldConfig.js` | fp-grid knobs: wallgear 12, vtiles 6, camlag 0.7, fpfade 0, maxTilesFor() |
| `src/components/work/world/useWorldScene.js` | /work resize: camera lag + video pause/settle-resume |
| `src/components/process/liveLockupGlobe.js` | The live O — home scroll-globe at rest, mark colorway |
| `src/components/process/processConfig.js` | O knobs (ospin −20, ostroke 3, opad 0.175) + camlag |
| `src/components/process/useProcessCopy.js` | O mount/fit/resize-fade + splash choreography |
| `src/components/process/useProcessScene.js` | Process resize doctrine (camLag re-sync invariants) |
| `src/components/globe/useGlobeScene.js` | Home globe settle fade; the O borrows this globe's build |
| `src/pages/work/index.astro` | brandDecks/brandDecks2 serialization (hero-led wall order) |

## Decisions & Context

- **Resize doctrine (site law now)**: cheap per-event (buffer, canvas size);
  everything expensive waits for settle; camera re-evals CHASE on a
  retargeted ease (`?camlag` — Nathan's "TouchDesigner lag"); designed
  drop-then-return where blanking is physics (home/process globes).
  NEVER re-populate media on resize (Nathan's explicit revert call).
- **camLag invariant** (process): the shadow state must re-sync from the
  live rig at every retarget, transitions own the camera (kill camLag
  tweens on takeover), aspect defers to no one.
- **The O = the home globe** — one source of truth for the mark's globe
  refinements; future home dial-ins flow into the O automatically.
- **/work field is BLACK** (fpfade 0 + :has root paint); process field is
  electric blue → the mark runs inverted (white ring/lattice) there.
- **Cross-checkout law**: the worktree shares the repo — memory dir is the
  landing-page project's (shared both sides). Never bare `git stash` (shared
  stack).
- **TRAPS this session**: WebGL `toDataURL` is blind without
  preserveDrawingBuffer (probe via composited screenshots); ResizeObserver
  fires after rAF so setSize-after-render clears what was just painted (the
  home-globe flicker mechanism); html/body root bg paints scale-UP exposure.

## Blockers

None. Environment healthy (dev :4322 here, :4321 in main once folded;
node_modules Dropbox-ignore applied to BOTH checkouts).
