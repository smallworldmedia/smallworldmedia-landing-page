# Session Context — 2026-07-17b (CREATIVE wave NAV-2/FP-1/DP-1 + black-bar + house-curve bake — all merged)

> Auto-generated by `/save-session`. Read by `/load-session`.

## Last Session Summary

Built, verified, and **merged** the CREATIVE ultracode wave: NAV-2 (kinetic-rule
nav promoted to default + fx3 brackets-only alt), FP-1 (enter_world house pulse +
a `?fp1tune` house-curve bench), DP-1 (next-project media-box-to-viewport
transition), plus a pre-existing detail-page black-bar fix. Then Nathan dialed the
house pulse curve on the bench and I **baked it as the shared house token** (reaches
both the enter_world dim and the inquiry brighten). All four PRs (#38–#41) merged
into `feature/v1-launch` and folded back into the working branch — `:4321` serves
the lot.

## Completed Work (This Session)

- **CREATIVE recon + design + build + verify** across two ultracode workflows
  (recovered cleanly from three session-limit interruptions mid-run).
- **NAV-2 (PR #38, merged):** four `?navfx` variants built behind a flag; Nathan
  picked **variant 4 (kinetic rule)** → promoted to the DEFAULT nav (no param) with
  the net-new current-page state (`data-current`+`aria-current`, unconditional;
  `--ease-draw` promoted to a token). **fx3 kept as an alt at `?navfx=3`,
  brackets-only** (`.site-nav__fxcaret` element + CSS deleted). Variants 1 & 2
  removed. Fixed an after-swap re-seat drift (rule measured pre-layout → double-rAF,
  drift now 0). Verified 7/7.
- **FP-1 (PR #39, merged):** enter_world CTA dims on the house pulse (first
  `housePulseLoop` consumer, phase-guarded teardown). Built a **`?fp1tune=1` bench**
  on /work (house-debug idiom: attack/hold/fall/period/dim/rest/on-ratio sliders,
  live SVG plot, readonly `HOUSE_PULSE_PATH`, copy_values/copy_url). Fixed bench
  defects (hydration-safe mount, clamp path monotonic → kills "Invalid CustomEase").
- **HOUSE PULSE CURVE BAKED (in PR #39):** Nathan's dialed values tokenized —
  `HOUSE_PULSE_PATH` = `'M0,0 C0.12,0 0.27,1 0.45,1 L0.45,1 C0.6929,0.8833 0.7571,0.1167 1,0'`,
  `HOUSE_PULSE_PERIOD_S` **2.3**, `HOUSE_PULSE_ON_RATIO` **0.75** (motion.js);
  `--duration-pulse` **2300ms** + both `@keyframes` (`house-pulse` dim floor 0.3,
  `house-pulse-brighten`) re-sampled from the curve (global.css); enter_world
  defaults dim **0.3** / rest **1.05** (WorldCard); bench defaults track the bake
  (`buildHousePulsePath(defaults)` === the token, asserted). **Verified: enter_world
  trough 0.300 @ ~2.3s AND inquiry next-field brighten rides `house-pulse-brighten`
  @2.3s (0.5→0.99).**
- **DP-1 (PR #40, merged):** `.np-band__cover` confines the blue to the media window
  on drag; commit promotes the ORIGINAL media node to fixed and grows it to viewport,
  short `swm:envelop` at `npms−0.12` snaps RouteFill for the swap (handshake/safety
  kept, RouteFill+FeaturedProjectDetail untouched). FP-2 wiring preserved. Verified
  8/8. ⚠️ Two follow-ups noted in PR #40: input-swallow now only the last 0.12s
  (optional transparent shield); navigate-failure safety edge.
- **DH-1 black-bar (PR #41, merged):** the ~29px gap under the blue client-panel was
  the sticky breadcrumb's `display:inline-flex` (line box reserved height, negative
  margin couldn't pull the media flush). Fix: `inline-flex → flex`. Gap now 0px
  desktop+mobile, sticky chip preserved. Pre-existing since 2026-07-01, not DP-1.
- Board rows updated (A1, NAV-2, FP-1, DP-1, DH-1). All CREATIVE worktrees removed;
  `:4343` preview killed.

## Current Branch & Git State

- Branch: **`claude/load-session-wippng`** — pushed, tip `eace8b7` (merge of
  origin/feature/v1-launch) + this save.
- **`feature/v1-launch` tip = `7153049`** (PR #39 merge). PRs #38–#41 all merged
  there; this branch contains all of it (two-way synced).
- Uncommitted: only `.astro/*` (generated) + the standing untracked files
  (`SWM-globe_white.svg`, `breadcrumb_move.png`, `.claude/skills/fable-mode-main/`).
- Build: 23 pages, clean. Dev server `npm run dev` (:4321) on this branch, serving
  the merged work.

## Pending Tasks (Next Session)

1. **Nathan (self-serve, carried over):** Studio drag-reorder on the merged rossi
   project (17 assets; hero = first-in-order). The A2b Lenis dial (`?lerp` /
   `?wheelmult` / `?lenisdur` on any detail page → bake into `LENIS_TUNING`,
   motion.js). The two near-black FP-2 `time=0` posters (`y02EgA7x…` .073,
   `S029xcxq…` .080) — per-asset time or epsilon, his call.
2. **Optional CREATIVE follow-ups (only if Nathan wants):** eyeball the inquiry
   brighten at the new 2.3s cadence (it inherited the bake); DP-1's two PR-noted
   follow-ups (input shield / nav-failure safety).
3. **Process wave:** refine pass 2 → **P5 device pass** (iPhone ≥42–44fps) → **P6
   3-file un-gate** (`/process` launches; home + nav process links stop 302ing).
   Board B10.
4. **v1 residuals:** ws6 device gate → PR `feature/v1-launch` → `main`; F9 inquiry
   submission on a deploy preview; F15 analytics decision; ws3 content pass.
5. Housekeeping: remaining refinements issues (#16 A3 motion-token audit; B1/B2 are
   the process-branch copy/chrome already ✅ in code but their issues are still open —
   close on next pass).

## Key Files Reference

| File | Purpose |
|------|---------|
| `docs/refinements-master-plan.md` | THE status board — all wave items + PR refs |
| `src/lib/motion.js` | House motion authority: **baked pulse curve** (path/2.3/0.75), 500/800 scroll consts, `LENIS_TUNING` (empty, A2b dial-ready) |
| `src/styles/global.css` | `--duration-pulse` 2300ms + `@keyframes house-pulse`/`house-pulse-brighten` (re-sampled); NAV default kinetic-rule + `[data-navfx="3"]` brackets alt |
| `src/components/SiteNav.jsx` | Promoted nav: `.site-nav__fxrule` (default), data-current maintenance, double-rAF after-swap re-seat |
| `src/components/work/WorldCard.jsx` | FP-1 pulse consumer (dim 0.3 / rest 1.05 / period from token) |
| `src/components/work/fp1Tune.js` + `Fp1TunePanel.jsx` + `styles/fp1-tune.css` | `?fp1tune=1` house-curve bench (dev-only/gated) |
| `src/components/work/detail/NextProjectBand.jsx` + `styles/project-detail.css` | DP-1 transition + DH-1 flush breadcrumb + FP-2 band wiring |
| `docs/process-page-plan.md` | P0–P4.7 ✅, P5/P6 ☐ |

## Decisions & Context

- **navfx4 is the shipped nav** now (no param); `?navfx=3` = the earmarked
  brackets-only alt (Nathan's call). Variants 1/2 gone. Promote/bake pattern was
  pre-written in a global.css "BAKE PATH" comment.
- **The house pulse curve is one shared token** — dialing it moved BOTH the
  enter_world dim and the inquiry brighten. Any future curve retune (motion.js path/
  period/on-ratio) must re-sample the two CSS `@keyframes` + `--duration-pulse`.
  Sampling recipe: create the CustomEase, evaluate at t·on-ratio, `dim = 1 −
  y·(1−floor)`, `bright = 0.5 + y·0.5` (a node snippet did this).
- **`?fp1tune` bench stays** (gated, harmless, reusable). Its defaults track the
  baked curve; copy_values emits the block Nathan pasted.
- **Issue-close convention:** PRs target `feature/v1-launch`, so "Closes #N" does NOT
  auto-close — close manually with shipped evidence.
- **Local verify recipe:** scratch playwright-core `/tmp/pw-verify` +
  `chromium_headless_shell-1228`, args `['--use-angle=metal','--autoplay-policy=no-user-gesture-required']`,
  `waitUntil:'load'` never `networkidle`; system Chrome for codec-reliable video.
  Worktree builds need real `npm ci`. Probe scripts live in the scratchpad.
- **Preview convention:** integration previews via a worktree merging the branches +
  `astro preview --port <p>` (4343 was the CREATIVE preview, now removed). `:4321` is
  Nathan's dev server — never kill it.
- **Workflow resilience:** three session-limit interruptions this session were each
  recovered by resuming the Workflow with `resumeFromRunId` (cached agents replay) or,
  when a build agent died mid-write, committing its intact uncommitted work + running
  a follow-up verify workflow.

## Blockers

- None hard. P5 needs Nathan's iPhone. Prod `/process` links 302 home until P6
  (deliberate). Production deploys must come from a ref including PR #34 (CMS merge).
