# Session Context — 2026-07-23 (perf pass: Stage 1 + /work field rework, all committed)

> Auto-generated by `/save-session`. Read by `/load-session`.

## Last Session Summary

Executed the approved perf/bloat plan through **Stage 1 (cheap defaults)** and then a
substantial **`/work` field rework** driven by Nathan's live feedback, followed by a
**baked-tunables + count-linked thumb** pass and a **DPR cap** for memory. Also
**diagnosed the /work 1.1 GB Chrome memory** report — confirmed it's a stable GPU/
video-decode baseline, **not a leak**. Five commits, all local on `feature/v1-launch`
(not yet pushed). Nathan is happy with globe crispness + /work load speed + framing.

## Completed Work (This Session)

- **Committed the approved plan** (`docs/perf-bloat-pass-plan.md`) to resolve drift → `12e5b0e`.
- **Stage 1 — cheap defaults** (`14bf4ce`): globe desktop video 270p→**480p** (pinned
  min==max); fixed stale '30fps'→60fps comment; `/work` `MAX_TILES` 16→12, `MIN_TILES`
  8→6, `BAND_MAX_PAGES` 8→5 (+ `BAND_PAGE_CAP` sync); `WORLD_MAX_LIVE` 3→**4**; scene
  stills `.jpg`→**.webp** (globe/tile/band); `THUMB_SIZE` 1024→640; Mux **preconnect**
  in BaseLayout; deleted dead consts `CURVE_STRENGTH`+`BG_COLOR`.
- **`/work` field rework** (`ee3389e`): **nav-safe framing** — new field safe-region in
  `placeTiles` (`FIELD_OFFSET_Y` down-shift for the fixed nav + `FIELD_SPREAD_X/Y`
  edge-inset), tiles no longer clip under the nav / at edges; the identity card is a DOM
  overlay so it's decoupled. Density `MAX_TILES` 12→8. **Video-first composition**:
  split showcase into video/still, load only up to the play budget of videos
  (`WORLD_MAX_VIDEO_TILES`, defaults = `WORLD_MAX_LIVE`) and pin them to the **front Near
  tier so every loaded video plays** (no frozen posters); stills fill the back tiers; a
  video-less World keeps the original all-tier spread. Added `tiers` param to `placeTiles`.
- **Baked framing + count-linked thumb** (`c7eeaff`): baked Nathan's tuned values —
  `spready=0.79`, `spreadx=0.88`, `fieldy=-0.012`, `max=7` (`min` 5). Replaced flat
  `THUMB_SIZE` with `thumbForCount()` — **linear, fewer tiles → bigger thumb**
  (`THUMB_MAX` 896 at MIN_TILES → `THUMB_MIN` 704 at MAX_TILES), tunable `?thumbmax`/
  `?thumbmin`. Verified: dense COCO (7 tiles) requests width=704 webp.
- **DPR cap** (`ec26afa`): `/work` `DPR_MAX` desktop 2→**1.5** (now a `?dpr` tunable).
  Render targets scale with DPR², so ~44% less render-target memory; visually
  indistinguishable on the lens-warped scene (verified via headless capture).
- **Memory diagnosis** (no code): drove /work through 5 project Turns + idle via the
  `/tmp/pw-verify` harness — JS heap flat 22-55 MB, video pool pinned at 4, single
  canvas, zero growth. **Not a leak.** The 1.1 GB is GPU/video-decode baseline (2×→1.5×
  DPR now helps; the globe is home-only and fully unmounts, so /work isn't doubled).

## Current Branch & Git State

- **Branch: `feature/v1-launch` @ `ec26afa`.** Five NEW local commits this session:
  `ec26afa` (DPR) → `c7eeaff` (bake+thumb) → `ee3389e` (/work rework) → `14bf4ce`
  (Stage 1) → `12e5b0e` (plan doc). Parent was `61ddc92`.
- **⚠️ Unpushed** — `origin/feature/v1-launch` is still at `61ddc92` (5 commits behind).
  Nathan asked for checkpoint + save only, not push. Push next session if desired.
- **Uncommitted:** none tracked. Only the same pre-existing untracked strays
  (`SWM-globe_white.svg`, `Small World Media Logo Inline Blue-01.svg`, `breadcrumb_move.png`,
  `homepage_refine_01.png`, `.claude/skills/fable-mode-main/`).
- **`:4321` dev server is UP** (was restarted this session; `npm run dev`). /work + / 200.

## Pending Tasks (Next Session)

**Plan = `docs/perf-bloat-pass-plan.md` (has a progress header now).** Remaining, in order:

1. **Stage 2 — burst + latency fixes** (the actual fix for the globe drag-stutter Nathan
   first reported): WS1 globe **promote-cap** (`MAX_PROMOTES_PER_UPDATE`, mirror
   `MAX_SWAPS_PER_UPDATE`) + **drag-defer** (thread `controller.dragging` into
   `scheduler.update`); WS5 worldLive **parallel first-fill** + **event-driven promote**.
2. **Stage 3 — structural:** `/work` build-stagger across rAF frames + staggered fan-out;
   **WS4 deck exclusion-rect** (the bottom deck-band still sits low / can overlap tiles —
   it's a force-placed BAND outside the new tile safe-region; fix via an `excludeRects`
   param in `placeTiles` keyed to live `BAND_POS_X/Y`).
3. **Stage 4 — bundle diet:** code-split the 4 dev benches (LenisTunePanel first) + move
   dev CSS into panels, then **lazy-`hls.js`** with the REQUIRED Safari + reduced-motion
   5-surface test matrix as the merge gate.
4. **DEFERRED:** full idle-prebuild of ±1 Worlds (gated on mobile density / HR-6).
5. Optional follow-ups Nathan may want: extend the `.webp` switch to the 5 detail-page
   Mux sites (Lightbox/MediaCard/MediaSlot/BandPager/NextProjectBand); consider a live-res
   540p drop if memory needs to go lower still.

## Key Files Reference

| File | Purpose |
|------|---------|
| `docs/perf-bloat-pass-plan.md` | The plan — Stage 2+ still to do. Has a progress header. |
| `src/components/work/world/worldConfig.js` | All `/work` knobs. NEW: `FIELD_OFFSET_Y/SPREAD_X/SPREAD_Y`, `WORLD_MAX_VIDEO_TILES`, `THUMB_MAX/MIN`+`thumbForCount()`, `DPR_MAX` now `?dpr`. Baked: `max=7`, spready .79, spreadx .88, fieldy -.012. |
| `src/components/work/world/seededLayout.js` | `placeTiles` — nav-inset/edge-buffer remap + `tiers` param (video→Near). |
| `src/components/work/world/useWorldScene.js` | `/work` three.js. NEW: video-first selection + `tierOf` + per-World `thumbForCount(chosen.length)`. WS3 build-stagger target `:296-409`; clean teardown `:648-668`. |
| `src/components/work/world/worldLive.js` | `/work` live scheduler (Near-tier only). WS5 parallel first-fill target `:73,76-80,129-164`. |
| `src/components/globe/LivePanelScheduler.js` | Globe live promote/demote. Stage-2 WS1 promote-cap `:149-153` (pattern `:41`) + drag-defer `:138-153`. |
| `src/components/globe/useGlobeScene.js` | Globe render loop. WS1 drag-defer wiring `:721-724`. |
| `src/components/globe/VideoSlotPool.jsx` | Shared HLS `<video>` pool (globe MAX_LIVE=7 + /work WORLD_MAX_LIVE=4). Fixed-size, `maxBufferLength:10`. |
| `src/components/work/useHls.js` | Shared HLS hook (`enableWorker`, clean destroy). WS4/Stage-4 lazy-load `hls.js` `:28`. |
| `/tmp/pw-verify/vwork_*.mjs` | Headless capture harness (perf/thumb/mem). `WORK_URL` env overrides. |

## Decisions & Context

- **Video-first composition (`/work`):** video is now a **playing front (Near) layer**
  with stills receding on the back tiers — the maximal reading of "prioritize video."
  Loaded video count is capped at the play budget so nothing freezes on a poster.
  **Flip point** if Nathan wants video spread across depths instead: the `tierOf`
  function in `buildSlot` (useWorldScene). All-video projects accept a smaller field
  rather than padding with frozen video.
- **Thumb law direction (confirmed via AskUserQuestion):** **fewer tiles → bigger thumb**
  (inverse) — matches "decreasing tiles → scale up thumb" + keeps bytes flat. Endpoints
  `?thumbmax`(896)/`?thumbmin`(704) tunable.
- **1.1 GB /work memory = expected baseline, NOT a leak** (measured). GPU/video-decode
  dominated. DPR 2→1.5 is the safe lever taken; live-res 720→540 is the next lever if
  needed; `WORLD_MAX_LIVE`=4 kept (Nathan wants the video).
- **Globe = home-only** (in `Hero.jsx`), fully unmounts on nav → /work never carries two scenes.
- **All new `/work` mechanics are `?param`-live:** `?fieldy ?spready ?spreadx ?max ?min
  ?vtiles ?thumbmax ?thumbmin ?dpr` — Nathan dials feel on the real projects.
- **WS4 note:** the bottom deck-band sits low / can overlap; it's force-placed via
  `BAND_POS_Y`, OUTSIDE the tile safe-region — that's the deferred deck-footprint fix.

## Blockers

- None code-side. Everything committed, tree clean, `:4321` up. Only friction is the
  usual `:4321` stopping between sessions (restart + `rm -rf node_modules/.vite` clears
  any stale 504). 5 commits are unpushed (intentional — checkpoint only).
