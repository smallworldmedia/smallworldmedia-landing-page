# Session Context — 2026-07-12 (build session)

> Auto-generated by `/save-session`. Read by `/load-session`.

## Last Session Summary

The process-page BUILD session (remote/cloud): picked up the grilled docs from PR #14 and shipped **P0–P4 of `docs/process-page-plan.md`** — the `/process` page now exists end-to-end behind its gate: route + 3-part gate, ScrollTrigger driver + Lenis bridge, the full five-Stage ProcessScene (belt → Thread → assembly → light-up → emanation → rhythm), copy entrances + arrival choreography + `?debug` panel. Every phase was verified against real headless WebGL with Playwright suites (pixel-sampled stage states, boundary walks in both directions, reduced-motion stills, teardown context release) and committed per phase. **P5 (real-device pass) and P6 (launch un-gate) remain.**

## Completed Work (This Session)

- **Branch repoint (Nathan's call):** `claude/load-session-rkxhn0` reset to the PR #14 tip `e5f04f2` (= `feature/v1-launch` @ `0cb7823` + the grilled docs; v1 has NOT merged to `main`, so mainline = `feature/v1-launch`). Ancestor-checked, pushed.
- **P0 `4fd1e4f`** — gated `process.astro` (dev-viewable redirect + netlify forced 302 + sitemap exclude), `route-process` body class (server-set; keys the scroll unlock + transparent-nav chrome), `ProcessPage.jsx` island, full copy deck in `processContent.js`, `process.css`, token-gap fixes (`--tracking-tight`, `--lh-body`, `--color-near-black` hoist — values mirror PR #13), `GAP_COLOR` comment corrected.
- **P1 `83be9e3`** — the codebase's first ScrollTrigger (island-scoped, ADR-0004): symmetric `[top 60%, bottom 60%]` triggers per Stage section, Lenis bridge with lazy attach, `astro:page-load`/fonts refresh, mid-page arrival sync, machine API behind a layout-phase install.
- **P2 `80d6553`** — `useProcessScene`: S3 zoom-out + cascade light-up (`buildCascadeTimeline` verbatim + color riding `panelDelay`, now exported from `cascade.js`), S4 emanation, S5 `?bpm` rhythm loops (rows ↔ equator-out), interrupt kill + compressed catch-up, contain-fit framing both breakpoints, right-of-center group offset, FPS-gated shared ticker + IO/hidden pause, full teardown with `forceContextLoss()`.
- **P3 `f88e958`** — Fragment belt (seeded phyllotaxis annulus, empty center; `mulberry32`/`hashSeed` exported from `seededLayout.js`), per-shard drift/tumble (geometry re-baked to local origin; home = `centerDir·R·k`), the Thread (screen-space SVG, per-frame reprojection, hop-by-hop trim-path draw, strike blips + drift damping), pull-in assembly with inner-sphere reveal + Thread ride-in/fade, caption pings (`references_folded` → `dots_connected` → `core_assembled`), RM stage-02 still = connected belt with Thread pre-drawn.
- **P4 `8d321b3`** — OS-boot copy entrances (scramble chip → SplitText masked lines → blurb rise; 0.7× leave-back exits; chrome persists), arrival choreography (`swm:fill-release` → `THE_PROCESS` scramble → H1 lines → `materializeBelt()` → cue fade), `?debug` panel (stage jumps, live fps/draws/stage, §9 knob readout).
- **Docs:** plan status table ticked P0–P4 with hashes + Build notes (deviations below).

## Production Scorecard

> Live counts fetched this session (2026-07-12) via the Sanity MCP connector — no `.env.local` needed for reads when the connector is up.

| Metric | Count |
|--------|-------|
| Clients | 59 |
| Service Tags | 15 |
| Media Assets | 776 *(confirms the `8a325c7` correction; the old 784 was the stale carry-forward)* |
| Featured Projects (`isFeatured`) | 16 |
| Manifests | 82 *(fresh — `media/` is present in this workspace)* |
| Affiliations wired | 0 recorded *(probe found 1 doc with a populated `affiliations` array — glance when Phase 13 starts)* |

## Post-save addendum (same session, phone-preview round)

- **`f0208d5`** live-tuning panel · **`eb6c9d7`** mobile groundwork: fixed a real P3-era bug (interrupting the Thread show mid-flight left the drift tick stamping belt transforms over the catch-up morph — judged by live state now, not the pose table), canvas got explicit CSS size (real dpr-3 phones rendered it 3× too large), the blanket mobile scrim became a tight band + text-shadow, the Core drops below the copy band on phones (**`?dropy`** 0.22, 17th live knob), and the ?debug panel collapses to a chip on phones.
- **Phone preview artifact** (Nathan traveling): the island bundled standalone (esbuild, fonts inlined, Iosevka subset 1.5MB→20KB) and published as a private Claude artifact — rebuild via `scratchpad/artifact/` if needed. Netlify does NOT build deploy previews for this repo's PRs (PR #13 has zero checks) — wiring previews needs Netlify settings access.

## Current Branch & Git State

- Branch: **`claude/load-session-rkxhn0`** — based on the PR #14 tip `e5f04f2`, pushed to origin, tip `403b2a0`. Session commits: `4fd1e4f` (P0) → `83be9e3` (P1) → `80d6553` (P2) → `f88e958` (P3) → `8d321b3` (P4) → `018f345` (docs tick + session save) → `403b2a0` (generated-file chore).
- Uncommitted changes: none — working tree clean at save.
- **PR #14** (the docs) still open against `feature/v1-launch`; its base hasn't moved. This build branch **stacks on it** — merge #14 first, then PR this branch (its diff over #14 is purely the build), or fold both.
- **PR #13** (brand-polish P0 tranche) still open; its token fixes are duplicated here **with identical values** — whichever lands second resolves clean, drop the duplicate hunk if rebasing.

## Pending Tasks (Next Session)

1. **P5 — device pass (needs local hardware / real GPU):** live scene on iPhone ≥ the home globe's 42–44fps baseline; RM on-device; mobile contain 0.7 belt fit; headless numbers (SwiftShader 20–28fps, draws 84–85 ≤ 90) only prove budget shape, not the gate.
2. **Feel pass on `/process?debug` — now fully LIVE:** sliders/selects apply without reload (framing/belt/drift/glow instantly; durations/orders on the next transition — jump a stage or hit `↻ replay` to hear them), `copy_url` serializes the dialed values for reload/share/bake-in. Knob set: the §9 twelve plus feel extras `?idlepower` (belt glow), `?pulsemin` (heartbeat depth), `?s3fill`/`?s45fill` (stage framing) — the last four aimed at the two review flags below.
3. **P6 — launch:** nav re-add (desktop slot at `SiteNav.jsx:253` + mobile menu item, `⊙ process`), un-gate = ONE 3-file commit (redirect line + netlify block + sitemap entry), SEO verify, spec §11 acceptance run.
4. Merge strategy per #14/#13 notes above. Elsewhere: v1 launch gate (ws6 real-device pass) still ☐; CMS Phase 13 still not started.

## Active Phase

**Process page (v2): docs ✅ · build P0–P4 ✅ · P5 device pass ☐ · P6 launch ☐.**

## Key Files Reference

| File | Purpose |
|------|---------|
| `docs/process-page-spec.md` | THE build contract (stages, copy deck, tunables, acceptance) |
| `docs/process-page-plan.md` | Status board — P0–P4 ticked, Build notes with deviations |
| `src/components/process/ProcessPage.jsx` | The island: canvas + Thread SVG + copy + CTA + debug mount |
| `src/components/process/useProcessScene.js` | The five-Stage machine: belt/Thread/assembly/light-up/emanation/rhythm |
| `src/components/process/useProcessScrollDriver.js` | ScrollTrigger boundaries + Lenis bridge (island-scoped) |
| `src/components/process/useProcessCopy.js` | OS-boot entrances + arrival choreography |
| `src/components/process/processContent.js` | The copy deck (single source) |
| `src/components/process/processConfig.js` | §9 knobs + stage-pose constants (`PULSE_MIN` note lives here) |
| `src/components/process/ProcessDebugPanel.jsx` | `?debug` chrome |
| `src/components/globe/cascade.js` | Now exports `panelDelay` (process reuses the delay models) |
| `src/components/work/world/seededLayout.js` | Now exports `mulberry32`/`hashSeed` (belt scatter) |

## Decisions & Context

- **Deviation — S5 pulse dips (`PULSE_MIN` 0.7):** spec's `uPower 1.0↔1.12` over-brighten is invisible on lit panels (pure `0x0000ff` saturates blue at power 1; textures gave the home flicker its headroom). The pulse dips first — the visible traveling wave — keeping 1.12 as the peak.
- **Review flag — S3 rest = solid blue orb:** lit color == gap color, so the settled Core reads as a featureless disc (arguably the literal "goes solid"). The spec's `uTint` fallback stands if grading wants structure separated.
- **Shard transform model:** panel geometry re-baked to its own local origin at build → `position`/`quaternion` mean placement/tumble; home pose `centerDir·R·k` + scale `k` decomposes the P2 emanation exactly. The belt and the globe stay the same 84 meshes.
- **ScrambleText trims leading whitespace** — chip separators live in flex `gap`, never in the string.
- **Boundary geometry:** `[top 60%, bottom 60%]` per section makes down- and up-scroll flips land on the same document line for adjacent sections.

## Blockers

- **P5 needs real hardware** — cannot run from a remote container.
- Environment notes for remote sessions: Sanity is reachable via the MCP connector (the egress proxy blocks `*.sanity.io` directly, so `astro build` still can't prerender the Sanity-backed routes here — `/process` itself builds fine); Google Fonts hangs ~13s before reset (abort it in Playwright runs); `with_server.py` leaks `astro dev` zombies — `pkill -f 'astro dev'` between runs.
