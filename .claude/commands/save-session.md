# Save Session

You are capturing a full session snapshot so that a fresh agent in the next conversation can pick up exactly where things left off — with zero ramp-up time.

## Step 1: Gather Project State

Run the following commands and capture their output:

GIT STATUS & BRANCH:

```
git status --short && echo "---" && git branch --show-current && echo "---" && git log --oneline -5
```

SANITY PRODUCTION SCORECARD (run only if `scripts/seed.mjs` or `scripts/ingest.mjs` exist):

```
node -e "
import { createClient } from '@sanity/client'
import fs from 'fs'
import path from 'path'
const envLocal = fs.readFileSync(path.resolve('.env.local'), 'utf-8')
const match = envLocal.match(/SANITY_WRITE_TOKEN=\"?([^\"\n]+)\"?/)
const token = match[1]
const client = createClient({ projectId: 'b60h4u7o', dataset: 'production', apiVersion: '2024-01-01', useCdn: false, token })
const [assets, clients, tags] = await Promise.all([
  client.fetch('count(*[_type == \"mediaAsset\"])'),
  client.fetch('count(*[_type == \"client\"])'),
  client.fetch('count(*[_type == \"serviceTag\"])'),
])
console.log(JSON.stringify({ clients, serviceTags: tags, mediaAssets: assets }))
" 2>/dev/null || echo '{"error": "Could not connect to Sanity"}'
```

MANIFEST COUNT:

```
find media -name "_manifest.md" -type f 2>/dev/null | wc -l | tr -d ' '
```

## Step 2: Read Existing Context

Read the following files to understand the current state:

1. `docs/cms-backend-roadmap.md` — the master roadmap with phase progress
2. Any existing `docs/.session-context.md` — previous session state (if it exists)

## Step 3: Write the Session Context Document

Write the complete session snapshot to `docs/.session-context.md`. Use the following structure:

```markdown
# Session Context — [DATE]

> Auto-generated by `/save-session`. Read by `/load-session`.

## Last Session Summary

[2-3 sentences on what was accomplished this session]

## Completed Work (This Session)

- [Bullet list of concrete deliverables from this session]

## Production Scorecard

| Metric | Count |
|--------|-------|
| Clients | [from GROQ] |
| Service Tags | [from GROQ] |
| Media Assets | [from GROQ] |
| Manifests | [from find] |
| Affiliations wired | [manual count or 0] |

## Current Branch & Git State

- Branch: [branch name]
- Last 5 commits: [from git log]
- Uncommitted changes: [yes/no + summary]

## Pending Tasks (Next Session)

[Copy the next unchecked items from cms-backend-roadmap.md, prioritized]

## Active Phase

Phase [N]: [Name]
- What's done: [summary]
- What's next: [first 3-5 unchecked items]

## Key Files Reference

| File | Purpose |
|------|---------|
| `src/schemas/client.ts` | Client document schema (types, affiliations) |
| `src/schemas/mediaAsset.ts` | Media asset schema (services array, mediaType) |
| `src/schemas/serviceTag.ts` | Service tag taxonomy |
| `scripts/ingest.mjs` | Manifest → Sanity ingestion pipeline |
| `scripts/seed.mjs` | Client + service tag seeder |
| `docs/cms-backend-roadmap.md` | Master roadmap (Phases 8-15) |
| `docs/_manifest-template.md` | Manifest authoring guide (Mode 1/Mode 2) |

## Environment

- Sanity Project: `b60h4u7o` / dataset: `production`
- Token: loaded from `.env.local` (`SANITY_WRITE_TOKEN`)
- Project Directory: `media/` (relative to workspace root, gitignored)

## Decisions & Context

[Any important design decisions, naming conventions, or architectural choices that affect future work. Pull from conversation history.]

## Blockers

[Any known blockers or issues. "None" if clear.]
```

## Step 4: Update the Roadmap

Update `docs/cms-backend-roadmap.md` to reflect any phases completed during this session. Mark completed items with `[x]` and update the Scorecard table at the bottom with current production counts.

## Step 5: Confirm

Tell the user:
- What was saved
- The path to the context file (`docs/.session-context.md`)
- The recommended next action for the following session
